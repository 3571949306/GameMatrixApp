# 测试方案 - GameMatrix App

## 1. 测试概述

### 1.1 测试目标
- 确保项目能够正常构建（Debug 和 Release）
- 确保所有单元测试通过
- 确保核心功能正常运行
- 确保修改后无回归问题

### 1.2 测试范围
- 构建测试：Debug 和 Release 构建
- 单元测试：437+ 个测试用例
- 集成测试：模块加载、更新分发
- 功能测试：核心游戏、模块商店、设置
- 性能测试：APK 体积、启动速度

### 1.3 测试环境
- **开发环境**: Windows 10/11, JDK 17, Android SDK 35
- **构建工具**: Gradle 8.13, AGP 8.13.2
- **测试设备**: 模拟器（x86_64）, 真机（arm64-v8a）
- **测试框架**: JUnit 4.13.2, Robolectric 4.16.1, Espresso 3.6.1

---

## 2. 构建命令

### 2.1 Debug 构建
```bash
# 基础 Debug 构建
.\gradlew.bat :app:assembleDebug

# 清理后构建
.\gradlew.bat clean :app:assembleDebug

# 不自动递增版本号构建
.\gradlew.bat :app:assembleDebug -PautoBumpVersion=false
```

**预期结果**:
- 构建成功，耗时 1-2 分钟
- 生成 APK: `app/build/outputs/apk/debug/app-debug.apk`
- 版本号自动递增（除非指定 `-PautoBumpVersion=false`）

### 2.2 Release 构建
```bash
# 基础 Release 构建（需要 keystore）
.\gradlew.bat :app:assembleRelease

# 完整发布流程（VPS + GitHub）
.\gradlew.bat :app:buildAndUploadToVpsAndGitHub -PupdateChannel=stable

# 仅上传到 VPS（Beta）
.\gradlew.bat :app:buildAndUploadDebugToVps
```

**预期结果**:
- 构建成功，耗时 2-3 分钟
- 生成 APK: `app/build/outputs/apk/release/app-release.apk`
- APK 已签名（需要 keystore.properties）
- R8 混淆启用（阶段一修复后）
- APK 体积减小 20-30%

### 2.3 模块构建
```bash
# 构建单个模块（以 chinesechess 为例）
.\gradlew.bat :module-store:feature:games:games:chinesechess:assembleDebug

# 构建所有模块
.\gradlew.bat assembleDebug
```

**预期结果**:
- 所有模块构建成功
- 模块 APK 生成在各自的 `build/outputs/apk/debug/` 目录

---

## 3. 测试命令

### 3.1 单元测试
```bash
# 运行所有单元测试
.\gradlew.bat :app:test

# 运行 Debug 单元测试
.\gradlew.bat :app:testDebugUnitTest

# 运行 Release 单元测试
.\gradlew.bat :app:testReleaseUnitTest

# 运行特定测试类
.\gradlew.bat :app:testDebugUnitTest --tests "com.gamecenter.app.games.gomoku.GomokuGameTest"

# 运行特定测试方法
.\gradlew.bat :app:testDebugUnitTest --tests "com.gamecenter.app.games.gomoku.GomokuGameTest.testInitialBoard"

# 运行核心模块测试
.\gradlew.bat :core:update:test
.\gradlew.bat :core:network:test
.\gradlew.bat :core:common:test
```

**预期结果**:
- 所有测试通过（437+ 个测试用例）
- 测试报告生成在 `app/build/reports/tests/testDebugUnitTest/index.html`
- 测试覆盖率报告（如果启用 JaCoCo）

### 3.2 Lint 检查
```bash
# Debug Lint 检查
.\gradlew.bat :app:lintDebug

# Release Lint 检查
.\gradlew.bat :app:lintRelease

# 生成 Lint 报告
.\gradlew.bat :app:lint
```

**预期结果**:
- Lint 检查完成
- 报告生成在 `app/build/reports/lint-results.html`
- 无 P0/P1 级别错误（阶段二修复后）

### 3.3 静态分析
```bash
# Detekt 检查（Kotlin 代码）
.\gradlew.bat detekt

# Ktlint 检查（代码风格）
.\gradlew.bat ktlintCheck

# 自动修复代码风格
.\gradlew.bat ktlintFormat
```

**预期结果**:
- Detekt 检查通过（或仅 baseline 中的问题）
- Ktlint 检查通过

---

## 4. 手动测试步骤

### 4.1 应用启动测试
**测试步骤**:
1. 安装 APK 到设备/模拟器
2. 点击应用图标启动
3. 观察启动页（SplashActivity）
4. 进入主界面（MainActivity）
5. 检查底部导航是否正常

**预期结果**:
- 启动页正常显示
- 主界面正常加载
- 底部导航切换正常
- 无崩溃或 ANR

### 4.2 游戏功能测试
**测试步骤**:
1. 进入游戏大厅（GamesFragment）
2. 选择内置游戏（如五子棋、斗地主）
3. 开始游戏并测试核心功能
4. 测试游戏设置（音效、震动）
5. 测试游戏教程

**预期结果**:
- 游戏列表正常显示
- 游戏启动正常
- 游戏逻辑正常
- 音效和震动正常
- 教程弹窗正常

### 4.3 模块商店测试
**测试步骤**:
1. 进入模块商店（ModuleStoreActivity）
2. 浏览模块列表
3. 搜索模块（使用搜索框）
4. 下载并安装模块
5. 打开已安装模块
6. 卸载模块

**预期结果**:
- 模块列表正常显示
- 搜索功能正常
- 下载进度正常显示
- 安装成功
- 模块正常打开
- 卸载成功

### 4.4 更新功能测试
**测试步骤**:
1. 进入设置（SettingsActivity）
2. 检查更新源设置
3. 手动检查更新
4. 下载更新 APK
5. 安装更新

**预期结果**:
- 更新源设置正常（无美国 VPS 选项）
- 更新检查正常
- 下载进度正常
- 安装成功

### 4.5 设置功能测试
**测试步骤**:
1. 进入设置界面
2. 切换主题（浅色/深色/跟随系统）
3. 切换语言（中文/英文/跟随系统）
4. 切换配色方案
5. 开关音效和震动
6. 设置更新策略

**预期结果**:
- 主题切换正常
- 语言切换正常
- 配色方案切换正常
- 音效和震动开关正常
- 更新策略设置正常

### 4.6 联机功能测试
**测试步骤**:
1. 选择支持联机的游戏（如斗地主、五子棋）
2. 创建房间
3. 另一设备加入房间
4. 开始游戏
5. 测试聊天功能

**预期结果**:
- 房间创建成功
- 房间加入成功
- 游戏同步正常
- 聊天功能正常

---

## 5. 自动测试步骤

### 5.1 CI 自动测试流程
```yaml
# GitHub Actions CI 配置
name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: ~/.gradle/caches
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
    
    - name: Build Debug APK
      run: ./gradlew :app:assembleDebug -PautoBumpVersion=false
    
    - name: Run Unit Tests
      run: ./gradlew :app:testDebugUnitTest
    
    - name: Run Lint
      run: ./gradlew :app:lintDebug
    
    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: test-results
        path: app/build/reports/tests/
    
    - name: Upload Lint Results
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: lint-results
        path: app/build/reports/lint-results.html
```

### 5.2 本地自动化测试脚本
```powershell
# test-all.ps1
Write-Host "=== 开始自动化测试 ===" -ForegroundColor Green

# 1. 清理构建
Write-Host "1. 清理构建..." -ForegroundColor Yellow
.\gradlew.bat clean

# 2. Debug 构建
Write-Host "2. Debug 构建..." -ForegroundColor Yellow
.\gradlew.bat :app:assembleDebug -PautoBumpVersion=false
if ($LASTEXITCODE -ne 0) {
    Write-Host "Debug 构建失败!" -ForegroundColor Red
    exit 1
}

# 3. 单元测试
Write-Host "3. 运行单元测试..." -ForegroundColor Yellow
.\gradlew.bat :app:testDebugUnitTest
if ($LASTEXITCODE -ne 0) {
    Write-Host "单元测试失败!" -ForegroundColor Red
    exit 1
}

# 4. Lint 检查
Write-Host "4. Lint 检查..." -ForegroundColor Yellow
.\gradlew.bat :app:lintDebug
if ($LASTEXITCODE -ne 0) {
    Write-Host "Lint 检查失败!" -ForegroundColor Red
    exit 1
}

# 5. 静态分析
Write-Host "5. 静态分析..." -ForegroundColor Yellow
.\gradlew.bat detekt
.\gradlew.bat ktlintCheck

Write-Host "=== 所有测试通过 ===" -ForegroundColor Green
```

---

## 6. 测试验证清单

### 6.1 构建验证
- [ ] Debug 构建成功
- [ ] Release 构建成功
- [ ] APK 签名正常
- [ ] R8 混淆正常（Release）
- [ ] APK 体积符合预期

### 6.2 单元测试验证
- [ ] 所有单元测试通过
- [ ] 测试覆盖率 >= 80%
- [ ] 无测试失败或跳过

### 6.3 Lint 验证
- [ ] Lint 检查通过
- [ ] 无 P0/P1 级别错误
- [ ] Lint baseline 正常

### 6.4 功能验证
- [ ] 应用启动正常
- [ ] 游戏功能正常
- [ ] 模块商店正常
- [ ] 更新功能正常
- [ ] 设置功能正常
- [ ] 联机功能正常

### 6.5 性能验证
- [ ] 启动速度正常
- [ ] 内存使用正常
- [ ] 无内存泄漏
- [ ] APK 体积优化

---

## 7. 测试报告模板

### 7.1 测试报告结构
```markdown
# 测试报告

## 测试时间
2026-06-19

## 测试版本
versionCode=451, versionName=1.4.0

## 测试结果
- 构建: ✅ 通过
- 单元测试: ✅ 通过 (437/437)
- Lint: ✅ 通过
- 功能测试: ✅ 通过

## 测试详情
### 构建测试
- Debug 构建: 成功 (1m 14s)
- Release 构建: 成功 (2m 30s)

### 单元测试
- 总测试数: 437
- 通过: 437
- 失败: 0
- 跳过: 0

### Lint 检查
- 错误: 0
- 警告: 15
- 信息: 42

### 功能测试
- 应用启动: 正常
- 游戏功能: 正常
- 模块商店: 正常
- 更新功能: 正常
- 设置功能: 正常
- 联机功能: 正常

## 问题记录
无

## 结论
所有测试通过，项目状态良好。
```

---

## 8. 测试执行计划

### 8.1 阶段一测试（P0 修复后）
**测试重点**:
- Debug 和 Release 构建
- 证书绑定功能
- R8 混淆功能

**测试命令**:
```bash
.\gradlew.bat :app:assembleDebug
.\gradlew.bat :app:assembleRelease
.\gradlew.bat :app:testDebugUnitTest
```

### 8.2 阶段二测试（P1 修复后）
**测试重点**:
- ABI splits 功能
- Lint 严格模式
- 新增单元测试

**测试命令**:
```bash
.\gradlew.bat :app:assembleRelease
.\gradlew.bat :app:testDebugUnitTest
.\gradlew.bat :app:lintDebug
```

### 8.3 阶段三测试（P2 修复后）
**测试重点**:
- 文档完整性
- 编码规范
- 国际化

**测试命令**:
```bash
.\gradlew.bat :app:lintDebug
.\gradlew.bat :app:testDebugUnitTest
```

### 8.4 最终测试（所有修复完成后）
**测试重点**:
- 全功能测试
- 性能测试
- 兼容性测试

**测试命令**:
```bash
.\gradlew.bat clean
.\gradlew.bat :app:assembleDebug
.\gradlew.bat :app:assembleRelease
.\gradlew.bat :app:testDebugUnitTest
.\gradlew.bat :app:lintDebug
```

---

## 9. 测试工具

### 9.1 构建工具
- **Gradle**: 构建自动化
- **AGP**: Android 构建插件

### 9.2 测试框架
- **JUnit**: 单元测试框架
- **Robolectric**: Android 单元测试
- **Espresso**: UI 测试框架

### 9.3 静态分析
- **Detekt**: Kotlin 静态分析
- **Ktlint**: Kotlin 代码风格检查
- **Lint**: Android Lint 检查

### 9.4 性能工具
- **LeakCanary**: 内存泄漏检测
- **Android Profiler**: 性能分析

### 9.5 UI 自动化测试框架
- **UiAutomator**: 模拟器 UI 自动化测试核心框架
- **GameTestHelper**: 游戏测试通用辅助类（启动、遍历点击、崩溃检测、退出）

---

## 10. 自动化 UI 测试架构

### 10.1 架构概述

自动化 UI 测试采用**按模块拆分**的架构，每个游戏/功能模块拥有独立的测试文件，便于升级和调整。

```
app/src/androidTest/java/com/gamecenter/app/
├── EmulatorTestBase.kt          # 测试基类（权限处理、通用操作）
├── tests/
│   ├── GameTestHelper.kt        # 游戏测试通用辅助类
│   ├── home/
│   │   └── HomeNavigationTest.kt    # 主页导航测试（10个用例）
│   ├── games/
│   │   ├── classics/                # 经典类游戏（8个文件）
│   │   │   ├── GomokuGameTest.kt       # 五子棋
│   │   │   ├── ChineseChessGameTest.kt # 中国象棋
│   │   │   ├── GoGameTest.kt           # 围棋
│   │   │   ├── DouDiZhuGameTest.kt     # 斗地主
│   │   │   ├── BlackjackGameTest.kt    # 21点
│   │   │   ├── CheckersGameTest.kt     # 跳棋
│   │   │   ├── DiceGameTest.kt         # 骰子
│   │   │   └── RockGameTest.kt         # 石头剪刀布
│   │   ├── puzzle/                  # 益智类游戏（10个文件）
│   │   │   ├── Game2048Test.kt         # 2048
│   │   │   ├── SudokuTest.kt           # 数独
│   │   │   ├── KlotskiTest.kt          # 华容道
│   │   │   ├── SokobanTest.kt          # 推箱子
│   │   │   ├── PipelineTest.kt         # 管道
│   │   │   ├── MinesweeperTest.kt      # 扫雷
│   │   │   ├── MatchTest.kt            # 消消乐
│   │   │   ├── MemoryTest.kt           # 记忆翻牌
│   │   │   ├── BreakoutTest.kt         # 打砖块
│   │   │   └── TilesTest.kt            # 拼图
│   │   └── casual/                  # 休闲类游戏（9个文件）
│   │       ├── TetrisTest.kt           # 俄罗斯方块
│   │       ├── SnakeTest.kt            # 贪吃蛇
│   │       ├── FlappyTest.kt           # Flappy Bird
│   │       ├── BrotatoTest.kt          # Brotato
│   │       ├── PlaneTest.kt            # 飞机大战
│   │       ├── ReactionTest.kt         # 反应测试
│   │       ├── GuessTest.kt            # 猜数字
│   │       ├── TicTacToeTest.kt        # 井字棋
│   │       └── WhackTest.kt            # 打地鼠
│   └── features/                    # 功能模块测试（5个文件）
│       ├── SettingsTest.kt            # 设置模块（10个用例）
│       ├── ToolBoxTest.kt             # 工具箱模块
│       ├── AiAssistantTest.kt         # AI助手模块
│       ├── BrowserTest.kt             # 浏览器模块
│       └── ModuleStoreTest.kt         # 模块商店（8个用例）
```

### 10.2 测试用例说明

每个游戏测试文件包含 4 个标准测试用例：

| 用例编号 | 名称 | 说明 |
|---------|------|------|
| test_001 | launchGame | 启动游戏 Activity，验证不崩溃 |
| test_002 | clickAllButtons | 遍历点击页面上所有可见可点击元素 |
| test_003 | gameInteraction | 模拟游戏特定交互（滑动、点击等） |
| test_004 | exitGame | 退出游戏返回大厅 |

### 10.3 运行自动化测试

```bash
# 构建测试 APK
.\gradlew.bat :app:assembleDebug :app:assembleDebugAndroidTest

# 运行所有自动化测试
adb shell am instrument -w -r \
  -e class com.gamecenter.app.tests.* \
  com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 运行特定游戏测试
adb shell am instrument -w -r \
  -e class com.gamecenter.app.tests.games.classics.GomokuGameTest \
  com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 运行主页测试
adb shell am instrument -w -r \
  -e class com.gamecenter.app.tests.home.HomeNavigationTest \
  com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner
```

### 10.4 权限弹窗处理

自动化测试通过两层机制处理首次启动权限弹窗：

1. **TEST_MODE 特性开关**（BuildConfig）：Debug 包默认开启，跳过权限说明弹窗
2. **EmulatorTestBase 兜底**：`dismissPermissionDialogIfShown()` 方法自动检测并点击弹窗按钮
3. **ADB 预授权**：`grantAppPermissions()` 在测试前通过 `pm grant` 授予运行时权限

### 10.5 新增模块测试规范

**当新增游戏或功能模块时，必须同步创建对应的自动化测试文件。**

#### 新增游戏测试步骤：

1. 在对应分类目录下创建 `XxxGameTest.kt`
2. 继承 `EmulatorTestBase`
3. 使用 `GameTestHelper` 辅助方法
4. 实现 4 个标准测试用例
5. 运行测试验证通过
6. 更新本文档的文件列表

#### 模板：

```kotlin
@RunWith(AndroidJUnit4::class)
@FixMethodOrder(MethodSorters.NAME_ASCENDING)
class XxxGameTest : EmulatorTestBase() {

    companion object {
        private const val TAG = "XxxGameTest"
    }

    private val activityClass = "com.gamecenter.app.games.xxx.XxxActivity"

    @Test
    fun test_001_launchGame() { ... }

    @Test
    fun test_002_clickAllButtons() { ... }

    @Test
    fun test_003_gameInteraction() { ... }

    @Test
    fun test_004_exitGame() { ... }
}
```

### 10.6 模块与测试文件对应关系

| 模块 | 测试文件 | 用例数 |
|------|---------|--------|
| 主页导航 | HomeNavigationTest.kt | 10 |
| 五子棋 | GomokuGameTest.kt | 4 |
| 中国象棋 | ChineseChessGameTest.kt | 4 |
| 围棋 | GoGameTest.kt | 4 |
| 斗地主 | DouDiZhuGameTest.kt | 4 |
| 21点 | BlackjackGameTest.kt | 4 |
| 跳棋 | CheckersGameTest.kt | 4 |
| 骰子 | DiceGameTest.kt | 4 |
| 石头剪刀布 | RockGameTest.kt | 4 |
| 2048 | Game2048Test.kt | 4 |
| 数独 | SudokuTest.kt | 4 |
| 华容道 | KlotskiTest.kt | 4 |
| 推箱子 | SokobanTest.kt | 4 |
| 管道 | PipelineTest.kt | 4 |
| 扫雷 | MinesweeperTest.kt | 4 |
| 消消乐 | MatchTest.kt | 4 |
| 记忆翻牌 | MemoryTest.kt | 4 |
| 打砖块 | BreakoutTest.kt | 4 |
| 拼图 | TilesTest.kt | 4 |
| 俄罗斯方块 | TetrisTest.kt | 4 |
| 贪吃蛇 | SnakeTest.kt | 4 |
| Flappy Bird | FlappyTest.kt | 4 |
| Brotato | BrotatoTest.kt | 4 |
| 飞机大战 | PlaneTest.kt | 4 |
| 反应测试 | ReactionTest.kt | 4 |
| 猜数字 | GuessTest.kt | 4 |
| 井字棋 | TicTacToeTest.kt | 4 |
| 打地鼠 | WhackTest.kt | 4 |
| 设置 | SettingsTest.kt | 10 |
| 工具箱 | ToolBoxTest.kt | 3 |
| AI助手 | AiAssistantTest.kt | 3 |
| 浏览器 | BrowserTest.kt | 3 |
| 模块商店 | ModuleStoreTest.kt | 8 |
| **合计** | **34个文件** | **145个用例** |

### 10.7 自动化测试结果（2026-06-22）

**测试版本**: versionCode=466, versionName=1.4.1

| 测试模块 | 用例数 | 结果 | 耗时 |
|---------|--------|------|------|
| 主页导航测试 | 10 | ✅ 全部通过 | - |
| 经典类游戏（8个） | 32 | ✅ 全部通过 | - |
| 益智类游戏（10个） | 40 | ✅ 全部通过 | 426.9s |
| 休闲类游戏（9个） | 36 | ✅ 全部通过 | 315.8s |
| 功能模块测试（5个） | 27 | ✅ 全部通过 | 572.6s |
| **总计** | **145** | **✅ 全部通过** | **~22分钟** |

**测试结论**: 所有自动化UI测试通过，无崩溃、无失败用例。

---

## 11. 测试注意事项

### 11.1 构建注意事项
1. 确保 JDK 17 已安装
2. 确保 Android SDK 35 已安装
3. 确保 keystore.properties 配置正确（Release 构建）
4. 确保 local.properties 配置正确

### 11.2 测试注意事项
1. 单元测试不需要真机，可在模拟器或本地运行
2. UI 测试需要真机或模拟器
3. 联机测试需要两台设备和网络连接
4. 性能测试需要在真机上进行
5. 自动化 UI 测试需要先安装 Debug APK 和 androidTest APK

### 11.3 常见问题
1. **构建失败**: 检查 JDK 版本、SDK 版本、依赖版本
2. **测试失败**: 检查测试代码、Mock 配置
3. **Lint 失败**: 检查 Lint 配置、修复 Lint 问题
4. **R8 失败**: 检查 ProGuard rules、保留规则
5. **权限弹窗阻塞**: 确保 Debug 包已开启 TEST_MODE

---

**测试方案版本**: 2.0  
**创建时间**: 2026-06-19  
**更新时间**: 2026-06-22  
**适用版本**: versionCode=451, versionName=1.4.0
