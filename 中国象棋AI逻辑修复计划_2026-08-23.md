# 中国象棋 AI 逻辑修复计划与执行记录

日期：2026-08-23
审计输入：`D:\Developmment\中国象棋AI逻辑审计报告_2026-08-23.md`
运行时真源：`module-store/feature/games/games/chinesechess/src/main/`

## 一、修复目标

1. 非法着法永远不能进入真实棋盘，来源包括玩家、AI、提示、联机单步消息和联机状态重放。
2. AI 的走法、局面哈希、终局规则必须与游戏逻辑层保持同一口径。
3. 长将、普通重复、将死、困毙和飞将具有可重复执行的回归测试。
4. 后续 Agent 修改时能够从仓库规范和 CI 同时获知并验证这些不变量。
5. 专项验证不得改写模块 APK、模块目录清单或版本号。

## 二、分阶段计划

### P0：阻断非法状态写入（已完成）

- 在 `ChineseChessGame` 建立 `isMoveLegal` 与原子 `commitMove`；真实落子统一完成“校验 → 落子 → 记录 → 切换 → 终局判定”。
- 将未校验的 `makeMoveSafe` 收紧为 `private`，仅允许逻辑类内部模拟/撤销。
- 玩家和 AI 落子统一调用 `commitMove`；AI 非法候选先解析为真实合法兜底着法，再用同一坐标执行动画和落子。
- 联机单步消息重新校验；同步历史先在临时棋局逐手 `commitMove`，全部通过后才替换当前棋局。
- 联机同步增加 1000 手上限、坐标/走棋方/终局/胜方一致性校验；和棋使用 `winner=-1`，避免空胜方崩溃。

验收标准：生产 UI/联机层不存在 `makeMoveSafe`、`recordPosition`、手工 `switchSide` 调用；非法着法不改变棋盘。

### P1：统一 AI 与规则状态（已完成）

- 修正开局库坐标，候选同时校验棋子归属、基本走法与走后不送将。
- 开局库只在 90 格与标准初始棋盘逐格相等时启用。
- 游戏层棋子类型哈希从枚举 `0..6` 改为与 AI 一致的 `1..7`，两层都把下一走棋方写入最终哈希。
- AI 增加绝对 `MAX_SEARCH_PLY=24`，将军延伸只在前 12 ply 生效，静态搜索同样受绝对上限约束。
- AI 生成飞将吃将；己方将/帅缺失时视为不可解被将，不再继续生成“无王着法”。
- 提示在展示前再次通过当前棋局 `isMoveLegal`，拒绝过期或非法 AI 结果。

验收标准：开局库随机抽样全部合法；初始及走子后的 AI/游戏哈希一致；AI 返回着法全部能被 `commitMove` 接受。

### P1：重复局面与长将（已完成）

- `positionHistory` 同步记录每个局面的走子方和将军方。
- 三次重复先定位最近三次相同局面，再扫描完整区间。
- 只有同一方每次走棋都造成将军、且对方没有反将，才判该长将方负；其他循环判和。
- 撤销、重置和深拷贝同步维护三组历史，避免历史错位。

验收标准：无将军循环三次判和；单方连续将军循环判长将方负；撤销后历史仍对齐。

### P1：Agent 防回归机制（已完成）

- 根目录新增 `AGENTS.md`，声明运行时真源、镜像同步、受保护生成物、坐标契约和完成标准。
- `scripts/verify_agent_contract.py` 静态验证中央落子闸门、联机重放、开局库、哈希、搜索上限及两份 AI 镜像同步。
- `scripts/verify_chinese_chess.py` 使用纯 Java 编译运行专项测试，不触发 Gradle 模块打包。
- CI 在 lint/test 前运行上述两个门禁。

验收标准：任一调用方绕过闸门、移除绝对搜索上限或只改一份 AI，CI 立即失败。

### P2：规则完整性增强（待后续独立迭代）

- 长捉/一将一捉：需先确定采用《世界象棋规则》还是产品简化规则，再设计受攻击棋子、根与保护、允许逃脱等状态模型。
- 哈希碰撞：当前 64 位滚动哈希风险低但非零；可升级为双 64 位 Zobrist，并在判重复前做棋盘快照二次确认。
- 规则去重：合并 AI `addLineMoves`、`addCannonMoves` 与游戏层直线棋子生成逻辑，或抽取无 Android 依赖的共享规则内核。
- 联机对局恢复：为同步协议加入 schemaVersion、整局摘要和拒绝后的显式重新同步请求。

P2 不阻塞本轮非法落子与长将高风险问题上线，但应在增加排名、积分或正式竞技模式前完成。

## 三、审计问题逐项映射

| # | 严重度 | 审计问题 | 本轮状态 | 处理 |
|---|---|---|---|---|
| 1/4 | Critical | AI 落子绕过合法性 | 已修复 | 中央 `commitMove`；未校验方法私有化；AI 兜底坐标一致 |
| 2 | Critical | 开局库含非法着法且无校验 | 已修复 | 修正棋谱、精确初始盘面、双重合法校验 |
| 3 | High | 联机移动/同步不重验 | 已修复 | 单步与临时重放均走 `commitMove`，原子替换 |
| 5 | Medium | 长将归责证据不足 | 已修复 | 完整区间单方连续将军判定 |
| 6 | Medium | 长捉未实现 | 延后 P2 | 需先确定正式规则版本与产品范围 |
| 7 | Medium | AI/Game 哈希不一致 | 已修复 | 统一类型、阵营、坐标和下一走棋方编码 |
| 8 | Medium | 将军延伸缺绝对上限 | 已修复 | 12 ply 延伸门限 + 24 ply 绝对上限 |
| 9 | Low | 提示可能展示非法着法 | 已修复 | UI 展示前按当前棋局二次验证 |
| 10 | Low | 历史列表对齐脆弱 | 已修复 | 同步维护局面/走子方/将军方，撤销/重置/深拷贝覆盖 |
| 11 | Low | 哈希碰撞理论风险 | 部分缓解 | 编码已统一；双哈希与快照确认列入 P2 |
| 12 | Low | 飞将吃将未生成 | 已修复 | 游戏层和 AI 均生成并测试 |
| 13 | Low | 搜索深度注释不一致 | 已修复 | 文档改为实际 1/2/3/4/5 |
| 14 | Low | 直线走法逻辑重复 | 延后 P2 | 共享规则内核独立重构，避免与本轮行为修复混杂 |

## 四、验证证据

- `python scripts/verify_agent_contract.py`：通过。
- `python scripts/verify_chinese_chess.py`：51 项断言全部通过。
- 随机性验证：精确初始盘面开局库随机抽样 200 次，全部合法。
- 交叉验证：AI 双方对弈 40 手，非法着法 0。
- Android 定向编译：`:module-store:feature:games:games:chinesechess:compileDebugJavaWithJavac -PautoBumpVersion=false` 通过。
- 发布资产：验证前后核对 `catalog.json`、`modules.json`、3 个预装模块 APK 和 `version.properties` 的 SHA-256；不得因本轮专项验证改变。

## 五、上线与回滚建议

1. 先在内部渠道完成三类手测：人机首局/撤销/提示、联机正常同步、注入非法联机坐标与非法历史。
2. 观察 AI 求解超时、联机同步拒绝计数、非法 AI 候选兜底计数；这些指标应接近零，出现持续增长即暂停放量。
3. 回滚应以本次中国象棋源文件为最小单位；不得回滚根目录其他未提交工作，也不得手工替换预装 APK 而跳过目录签名流程。
