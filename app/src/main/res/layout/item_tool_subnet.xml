<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="@dimen/gm_spacing_3">

        <EditText
            android:id="@+id/et_subnet_ip"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:hint="IP/CIDR（如192.168.1.1/24）"
            android:inputType="textUri"
            android:text="192.168.1.1/24"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="14sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_calc_subnet"
            style="@style/Widget.GameMatrix.Button.Tonal"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="@dimen/gm_spacing_2"
            android:text="计算" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_subnet_result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/gm_spacing_3"
        android:background="?attr/colorSurfaceVariant"
        android:fontFamily="monospace"
        android:lineSpacingExtra="4dp"
        android:padding="@dimen/gm_spacing_3"
        android:text="输入IP/CIDR后点击计算"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="14sp" />
</LinearLayout>