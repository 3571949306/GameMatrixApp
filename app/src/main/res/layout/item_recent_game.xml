<?xml version="1.0" encoding="utf-8"?>
<!-- Feature C: 首页最近游玩横向滚动小卡片 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:layout_marginEnd="10dp"
    android:background="@drawable/bg_home_recent_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/iv_recent_game_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:scaleType="centerInside"
        android:contentDescription="@string/visual_refresh_game_icon"
        tools:src="@drawable/ic_game" />

    <TextView
        android:id="@+id/tv_recent_game_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:maxWidth="72dp"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="11sp"
        tools:text="五子棋" />
</LinearLayout>
