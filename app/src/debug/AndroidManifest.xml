<?xml version="1.0" encoding="utf-8"?>
<!--
    All games, including Go, are loaded through DynamicGameActivity and the
    external module entry point after the hot-update flip.  Keep this Debug
    manifest available for future debug-only declarations, but do not retain a
    stale host GoActivity reference after its source was removed.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
