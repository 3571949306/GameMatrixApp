# 问题清单 - GameMatrix App

## 问题统计
- **P0 (严重)**: 3 个问题
- **P1 (重要)**: 5 个问题
- **P2 (中等)**: 4 个问题
- **P3 (轻微)**: 3 个问题
- **总计**: 15 个问题

---

## P0 - 严重问题（必须立即修复）

### P0-1: 美国 VPS 残留配置未完全清理
**问题等级**: P0  
**涉及文件**: 
- `app/build.gradle` (第 199, 211 行)
- `core/update/build.gradle` (第 81 行)
- `core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java` (第 62 行)
- `core/common/src/main/java/com/gamecenter/app/SettingsManager.java` (第 81 行)
- `local.properties.template` (第 32 行)

**问题描述**: 
美国 VPS 已于 2026-06-19 下线，但代码中仍保留大量 `SERVER_URL_FALLBACK`、`MODULE_FALLBACK_HOST`、`UPDATE_SOURCE_VPS_US` 等废弃配置。虽然标记为 `@Deprecated`，但这些残留配置：
1. 增加了代码复杂度和维护成本
2. 可能误导开发者以为还有备用源
3. 部分 UI 可能仍显示已废弃的选项

**可能原因**: 
下线美国 VPS 时采用了向后兼容策略，保留空值而非完全删除

**建议修复方式**: 
1. 完全移除 `SERVER_URL_FALLBACK` 相关代码
2. 移除 `MODULE_FALLBACK_HOST` 配置
3. 移除 `UPDATE_SOURCE_VPS_US` 常量（或保留但不在 UI 展示）
4. 更新 `local.properties.template` 移除相关注释
5. 在 `UpdateChecker.buildUpdateUrls()` 中移除 US VPS 分支

**影响范围**: 更新分发模块、设置模块

---

### P0-2: 证书绑定被禁用，存在安全风险
**问题等级**: P0  
**涉及文件**: 
- `core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt`

**问题描述**: 
为兼容模拟器 SIGSEGV 问题，证书绑定（Certificate Pinning）被临时关闭。这导致：
1. 中间人攻击风险增加
2. 用户数据可能被窃听
3. 不符合安全最佳实践

**可能原因**: 
模拟器环境下证书绑定导致 SIGSEGV 崩溃

**建议修复方式**: 
1. 调查模拟器 SIGSEGV 根本原因
2. 仅在 Debug 构建中禁用证书绑定
3. Release 构建必须启用证书绑定
4. 添加证书绑定配置开关

**影响范围**: 所有网络请求安全

---

### P0-3: R8 代码混淆未启用
**问题等级**: P0  
**涉及文件**: 
- `app/build.gradle` (第 238-239 行)

**问题描述**: 
Release 构建配置 `minifyEnabled false` 和 `shrinkResources false`，导致：
1. APK 体积大（未压缩约 30%）
2. 代码易被反编译
3. 未移除未使用的资源和代码

**可能原因**: 
BreakoutActivity$1 重复类问题导致 R8 崩溃，临时关闭

**建议修复方式**: 
1. 修复 ProGuard rules 解决 BreakoutActivity$1 问题
2. 启用 R8 混淆和资源收缩
3. 测试 Release 构建确保功能正常

**影响范围**: APK 体积、代码安全

---

## P1 - 重要问题（应尽快修复）

### P1-1: ABI Splits 关闭导致 APK 体积增大
**问题等级**: P1  
**涉及文件**: 
- `app/build.gradle` (第 226 行)

**问题描述**: 
测试阶段关闭了 ABI 拆分（`enable false`），导致 APK 同时包含 arm64-v8a 和 x86_64，体积增大约 75%。

**可能原因**: 
模拟器需要 x86_64 架构，测试阶段需要 universal APK

**建议修复方式**: 
1. 发版时启用 ABI splits（`enable true`）
2. 仅包含 arm64-v8a（覆盖 95%+ 设备）
3. 为模拟器测试保留 universal APK 构建变体

**影响范围**: APK 体积

---

### P1-2: Lint 宽松模式导致问题积累
**问题等级**: P1  
**涉及文件**: 
- `app/build.gradle` (第 254-259 行)

**问题描述**: 
配置 `abortOnError false` 和 `warningsAsErrors false`，导致：
1. Lint 错误不阻塞构建
2. 问题可能持续积累
3. 代码质量下降

**可能原因**: 
历史遗留问题过多，严格模式会阻塞 CI

**建议修复方式**: 
1. 逐步修复 Lint 问题
2. 设置 Lint baseline 锁定现有问题
3. 新代码必须通过严格 Lint 检查
4. 逐步启用 `abortOnError true`

**影响范围**: 代码质量

---

### P1-3: 部分 UI 可能仍显示已废弃的美国 VPS 选项
**问题等级**: P1  
**涉及文件**: 
- `app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java`

**问题描述**: 
虽然 `SettingsManager` 中 `UPDATE_SOURCE_VPS_US` 标记为废弃，但 UI 可能仍展示该选项，导致用户选择后无法使用。

**可能原因**: 
UI 未同步更新

**建议修复方式**: 
1. 检查 `AppSettingsDialog` 中的更新源选项
2. 移除或隐藏美国 VPS 选项
3. 若用户已选择 US VPS，自动回退到 AUTO

**影响范围**: 用户体验

---

### P1-4: 模块商店动态加载缺少详细错误处理
**问题等级**: P1  
**涉及文件**: 
- `app/src/main/java/com/gamecenter/app/modules/ModuleDownloader.kt`
- `core/moduleloader/src/main/kotlin/com/gamecenter/app/moduleloader/ModuleLoaderV2.kt`

**问题描述**: 
模块下载和加载失败时，错误信息不够详细，用户难以理解问题原因。

**可能原因**: 
错误处理逻辑简单，未提供详细错误码和解决方案

**建议修复方式**: 
1. 定义模块加载错误码枚举
2. 提供详细的错误信息和解决建议
3. 添加重试机制

**影响范围**: 用户体验

---

### P1-5: 部分游戏模块缺少单元测试
**问题等级**: P1  
**涉及文件**: 
- `app/src/test/java/com/gamecenter/app/games/` (部分游戏缺少测试)

**问题描述**: 
虽然总测试用例 437+，但部分核心游戏模块（如斗地主联机、围棋 AI）缺少单元测试，存在回归风险。

**可能原因**: 
测试覆盖率不足

**建议修复方式**: 
1. 为斗地主联机逻辑添加测试
2. 为围棋 AI 添加测试
3. 为核心模块（ModuleLoader, ModuleManager）添加测试
4. 设置测试覆盖率目标（如 80%）

**影响范围**: 代码质量、回归风险

---

## P2 - 中等问题（建议修复）

### P2-1: 文档分散，缺少统一索引
**问题等级**: P2  
**涉及文件**: 
- `docs/` 目录下多个 MD 文件
- `README.md`
- `CHANGELOG.md`

**问题描述**: 
项目文档分散在多个文件中，缺少统一的文档索引，新开发者难以快速上手。

**可能原因**: 
文档逐步积累，未整理

**建议修复方式**: 
1. 创建 `docs/INDEX.md` 统一文档索引
2. 按主题分类文档（架构、发布、测试、维护等）
3. 在 README 中链接到文档索引

**影响范围**: 开发效率

---

### P2-2: Java/Kotlin 代码风格不统一
**问题等级**: P2  
**涉及文件**: 
- 全项目 Java 和 Kotlin 文件

**问题描述**: 
项目 60% Java + 40% Kotlin，部分代码风格不统一：
1. 命名规范不一致
2. 注释风格不一致
3. 错误处理方式不一致

**可能原因**: 
逐步迁移到 Kotlin，未统一规范

**建议修复方式**: 
1. 制定 Java/Kotlin 混合编码规范
2. 在 `CODE_WIKI.md` 中记录规范
3. 使用 Ktlint 和 Checkstyle 强制规范

**影响范围**: 代码可维护性

---

### P2-3: 部分 TODO/FIXME 注释未处理
**问题等级**: P2  
**涉及文件**: 
- `app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java`
- `app/src/main/java/com/gamecenter/app/utils/SoundManager.java`
- `app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java`
- `app/src/main/java/com/gamecenter/app/monitor/PerfMonitor.java`
- `app/src/main/java/com/gamecenter/app/games/whack/WhackActivity.java`
- `app/src/main/java/com/gamecenter/app/modules/ModuleDependencyResolver.java`
- `app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuLogHelper.java`
- `app/src/main/java/com/gamecenter/app/games/doudizhu/AIBot.java`

**问题描述**: 
代码中存在 8 个文件包含 TODO/FIXME/XXX/HACK/BUG 注释，表明存在未处理的问题或待完成的功能。

**可能原因**: 
开发过程中标记的待办事项未及时处理

**建议修复方式**: 
1. 逐个检查 TODO/FIXME 注释
2. 评估优先级并处理
3. 完成后删除注释或创建 Issue 跟踪

**影响范围**: 代码质量

---

### P2-4: 部分字符串未本地化
**问题等级**: P2  
**涉及文件**: 
- `app/src/main/res/values/strings.xml` (568 个缺失翻译)

**问题描述**: 
虽然项目支持中英文双语，但 `values-en/strings.xml` 缺少 568 个字符串翻译，导致英文环境下显示中文。

**可能原因**: 
国际化工作未完成

**建议修复方式**: 
1. 逐步补全英文翻译
2. 使用翻译工具或人工翻译
3. 在 CI 中检查翻译完整性

**影响范围**: 国际化体验

---

## P3 - 轻微问题（可选修复）

### P3-1: 部分资源文件命名不规范
**问题等级**: P3  
**涉及文件**: 
- `app/src/main/res/drawable/` (部分文件)

**问题描述**: 
部分资源文件命名不符合 Android 规范（如包含大写字母、特殊字符）。

**可能原因**: 
资源文件命名未严格检查

**建议修复方式**: 
1. 使用 Lint 检查资源命名
2. 重命名不符合规范的资源文件
3. 更新代码中的引用

**影响范围**: 代码规范

---

### P3-2: 部分依赖版本较旧
**问题等级**: P3  
**涉及文件**: 
- `app/build.gradle`
- `build.gradle`

**问题描述**: 
部分依赖版本较旧，可能存在已知漏洞或性能问题：
- `androidx.appcompat:appcompat:1.7.1` (最新 1.7.1)
- `com.google.android.material:material:1.12.0` (最新 1.12.0)

**可能原因**: 
依赖更新不及时

**建议修复方式**: 
1. 定期检查依赖更新
2. 使用 Dependabot 自动检查
3. 评估升级风险

**影响范围**: 安全性、性能

---

### P3-3: 部分代码注释使用中英文混合
**问题等级**: P3  
**涉及文件**: 
- 全项目 Java 和 Kotlin 文件

**问题描述**: 
代码注释中英文混合使用，部分注释全中文，部分全英文，部分中英混合。

**可能原因**: 
开发团队习惯不同

**建议修复方式**: 
1. 制定注释语言规范（建议英文或中文统一）
2. 逐步统一注释语言
3. 使用翻译工具辅助翻译

**影响范围**: 代码可读性

---

## 问题优先级排序

| 优先级 | 问题数 | 修复建议 |
|--------|--------|----------|
| P0 | 3 | 立即修复，阻塞发布 |
| P1 | 5 | 尽快修复，影响质量 |
| P2 | 4 | 建议修复，提升体验 |
| P3 | 3 | 可选修复，优化细节 |

---

## 下一步行动

1. **立即处理 P0 问题**: 清理美国 VPS 残留配置、启用证书绑定、启用 R8 混淆
2. **优先处理 P1 问题**: 启用 ABI splits、严格 Lint 模式、补全测试
3. **逐步处理 P2/P3 问题**: 文档整理、代码规范、国际化

**审计时间**: 2026-06-19  
**审计版本**: versionCode=451, versionName=1.4.0
