# 华容道游戏优化说明

## 优化日期
2026-05-09

---

## 优化内容

### 1. 游戏规则规范化 ✅

**经典华容道布局**：
- 棋盘大小：4×5 网格
- 曹操：2×2 大方块（红色）
- 四个 1×2 竖方块（张飞、赵云、马超、黄忠）
- 关羽：1 个 2×1 横方块
- 四个小兵：4 个 1×1 小方块
- 出口：底部中央（曹操需要到达的位置）

**胜利条件**：
- 曹操移动到棋盘底部中央位置（坐标 x=1, y=3）即为获胜
- 符合经典华容道"曹操逃出华容道"的规则

---

### 2. 智能提示系统增强 💡

#### 核心算法改进

**KlotskiGame.java** 新增功能：
- `getHint()`: BFS 算法求解最短路径，返回第一步走法和总步数
- `getSolutionPath()`: 获取完整解法路径，用于验证和调试

```java
/**
 * 提示结果类
 */
public class HintResult {
    public final int blockId;      // 要移动的方块 ID
    public final int dx;           // 移动方向 X
    public final int dy;           // 移动方向 Y
    public final int totalSteps;   // 到达出口所需总步数
}
```

**算法特点**：
- 使用 BFS（广度优先搜索）保证找到最短路径
- 支持最多 900,000 个状态搜索，并对同形竖将/小兵做状态归一化，减少重复搜索
- 自动识别无解局面并返回 null

#### UI 提示显示

**KlotskiView.java** 新增功能：
- `showHint(HintResult)`: 显示当前局面的下一步提示，带绿色箭头和步数预估
- `clearHint()`: 清除提示显示

**提示视觉效果**：
- 🟢 绿色箭头指示移动方向
- 📊 显示"约 X 步"到达出口
- 🎯 箭头从方块中心指向移动方向
- ✨ 动态箭头头部，美观清晰

#### 交互逻辑优化

**KlotskiActivity.java** 改进：
- **点击提示**：从当前棋盘重新计算到出口的下一步
- **玩家移动后再次点击提示**：按新局面继续计算下一步
- **移动方块后**：自动清除提示，避免误导
- **提示文字**：显示"向 X 滑动「角色名」" + "预计还需 X 步到达出口"

**示例提示**：
```
💡 提示：向下滑动「关羽」
预计还需 15 步到达出口

按箭头移动后，再点提示查看下一步
```

---

### 3. 视觉效果提升 🎨

**方块样式**：
- 渐变色背景（从上到下由亮到暗）
- 白色圆角边框（3px）
- 12px 圆角设计
- 每个角色独特颜色：
  - 曹操：红色 (#D32F2F)
  - 张飞：蓝色 (#1976D2)
  - 赵云：绿色 (#388E3C)
  - 马超：橙色 (#F57C00)
  - 黄忠：紫色 (#7B1FA2)
  - 关羽：青色 (#00796B)
  - 小兵：多种颜色区分

**出口标识**：
- 黄色高亮区域
- "出口"文字标识
- 向下箭头图标

**操作优化**：
- 滑动阈值：0.3 倍方块大小
- 支持水平/垂直滑动
- 点击时自动清除提示

---

### 4. 游戏功能完善 🎮

**步数统计**：
- 实时显示当前步数
- 获胜时显示总步数

**存档系统**：
- 暂停时自动保存游戏进度
- 重新打开时恢复进度
- 获胜后自动清除存档
- 重新开始后清除存档

**教程帮助**：
- 教程按钮调用 GameTutorialHelper
- 显示游戏规则和操作说明

---

## 文件修改清单

| 文件 | 修改内容 |
|------|----------|
| `KlotskiGame.java` | 重写棋盘模型、合法移动校验、BFS 求解和状态恢复校验 |
| `KlotskiView.java` | 添加当前局面提示显示，修复移动动画方向，优化触摸操作 |
| `KlotskiActivity.java` | 每次提示从当前棋盘重新求解，添加步数显示和移动状态刷新 |
| `activity_klotski.xml` | 添加步数显示控件 |

---

## 技术细节

### BFS 求解器实现

```java
public HintResult getHint() {
    if (isWon()) return null;
    
    // 初始化 BFS
    BFSState init = new BFSState(copyBlocks());
    Set<Long> visited = new HashSet<>();
    Queue<BFSState> queue = new LinkedList<>();
    
    // 四个方向：下、右、左、上
    int[][] dirs = {{0,1}, {1,0}, {-1,0}, {0,-1}};
    
    while (!queue.isEmpty()) {
        BFSState cur = queue.poll();
        
        // 尝试移动每个方块
        for (int i = 0; i < cur.blocks.size(); i++) {
            // 尝试四个方向
            for (int[] d : dirs) {
                // 检查是否可移动
                if (canMove(cur, i, d[0], d[1])) {
                    BFSState next = cur.apply(i, ...);
                    if (next.isGoal()) {
                        return new HintResult(...);
                    }
                    if (visited.add(next.hash())) {
                        queue.add(next);
                    }
                }
            }
        }
    }
    return null; // 无解
}
```

### 状态键

使用紧凑的 64 位状态键，并把同形竖将和小兵的位置排序归一化，避免重复搜索等价局面：
```java
long stateKey(int[] positions) {
    int[] verticals = {cell(1), cell(2), cell(3), cell(4)};
    int[] soldiers = {cell(6), cell(7), cell(8), cell(9)};
    Arrays.sort(verticals);
    Arrays.sort(soldiers);
    long key = cell(0) | (cell(5) << 5);
    for (...) {
        key |= groupCell << shift;
    }
    return key;
}
```

---

## 使用说明

### 游戏操作
1. **滑动方块**：手指在方块上滑动，向任意方向移动
2. **查看提示**：点击"💡 提示"按钮
   - 显示当前局面到出口的下一步走法
   - 按箭头移动后再次点击，会按新局面继续提示
3. **重新开始**：点击"重新开始"按钮
4. **查看教程**：点击"📖 教程"按钮

### 提示系统使用技巧
- **卡关时**：点击一次提示，查看第一步
- **需要完整思路**：按提示移动一步后继续点击提示，跟随箭头指引
- **想自己思考**：移动任意方块后，提示自动清除

---

## 性能优化

- **搜索限制**：最多搜索 900,000 个状态，防止内存溢出
- **状态去重**：使用 HashSet 记录已访问状态，并归一化同形棋子
- **紧凑状态**：棋子坐标用整数数组表示，减少对象分配
- **异步执行**：求解在后台线程运行，完成前禁用提示按钮

---

## 测试建议

1. **基础功能测试**：
   - 滑动所有方块，确保移动正常
   - 测试胜利条件（曹操到达出口）
   
2. **提示功能测试**：
   - 初始状态点击提示，验证箭头方向
   - 连续点击提示，验证路径连贯性
   - 移动方块后，验证提示清除
   
3. **边界测试**：
   - 测试无解局面（如果可能）
   - 测试已获胜状态点击提示

4. **存档测试**：
   - 游戏中途退出，重新进入验证恢复
   - 获胜后重新进入，验证已清除存档

---

## 未来改进方向

1. **多布局支持**：添加其他经典华容道布局（如"横刀立马"、"层层设防"等）
2. **步数优化**：显示最少步数解法，并对比玩家步数
3. **计时功能**：添加游戏计时器，支持速度挑战
4. **提示优化**：显示完整路径动画演示
5. **难度分级**：根据初始布局难度分类

---

**优化完成时间**：2026-05-07  
**构建版本**：versionCode=11  
**测试状态**：✅ 编译通过，待实机测试
---

## 2026-05-14 文档同步：文字适配与应用语言

- 新增全局按钮文字适配样式，统一提升 MaterialButton 与平台 Button 的最小高度、内边距和两行显示能力，减少“进入游戏”“发送”等按钮文字被裁切的问题。
- 设置弹窗新增应用语言选项：跟随系统、中文、English；应用启动时会恢复已选择语言。
- AI 任务下拉改为资源字符串，切换 English 后可显示 Chat、Summary、Translate 等英文选项。
- 发布前检查需覆盖中文/英文两种语言、深色/浅色主题、游戏大厅卡片按钮、AI 发送按钮、工具箱小按钮和斗地主操作按钮。
## 2026-05-15 文档同步：Dependabot 与 CI 修复

- Dependabot 安全告警已处理：升级 Android Gradle Plugin 到 8.13.2、Gradle Wrapper 到 8.13、Kotlin 到 2.2.21、Hilt 到 2.57.2。
- 构建 classpath 已强制解析到安全版本：Netty 4.1.133.Final、BouncyCastle 1.84、commons-compress 1.26.0、jose4j 0.9.6、jdom2 2.0.6.1。
- GitHub Actions 已改为验证型 CI：使用 JDK 21，执行 debug 构建与单元测试，不在云端构建 release 包，避免暴露或依赖 release 签名文件。
- CI 命令统一添加 `-PautoBumpVersion=false`，避免自动修改 `version.properties`。
- `.gitignore` 的 `data/` 规则已收窄为 `/data/`，防止误忽略 `app/src/main/java/com/gamecenter/app/ai/data/` 源码。
- 最新 GitHub Actions `CI/CD Pipeline` 已通过；正式签名、R8 混淆和 VPS/GitHub Release 发布仍以本机发布流程为准。
