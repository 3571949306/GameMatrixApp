# 浏览器板块改造计划（2026-08-28；2026-08-29 复核修订）

> 责任人：WorkBuddy（浏览器板块）
> 并行方：另一 AI（AI 模块 UI / 模块加载器隔离重构 / 共享资源 `strings.xml`·`colors.xml`）
> 依据：`AGENTS.md`、既有 `浏览器模块审计与改造方案_2026-08-26.md`、以及本次对浏览器代码和未提交改动的逐项核对。
> 文中行号、文件数量和“已完成”状态都可能随并行改动变化；复核时以符号名、当前 `git diff` 和验证命令输出为准，不以固定行号代替证据。

---

## 0. 一句话结论

浏览器是**宿主内代码**（非动态 APK 模块）。上一轮审计中的一部分问题已经有代码修补，但仍有“骨架、半接线、已实现未验证”并存的情况；旧文档不能直接当作完成证明。**当前剩余问题主要分为三类**：

1. **长会话退化型缺陷**（线程泄漏、拦截链路同步 IO、危险文件落盘、假无痕）——不修不会立刻崩，会随使用时长缓慢劣化；
2. **半接线 / 双实现并存**（单 WebView 死分支、`UrlUtils` 与 `UrlInputHelper` 双套、AdBlocker 与 TrackerBlocker 各一套匹配逻辑）；
3. **测试仍然不足**（当前新增的多数测试是纯逻辑/脚手架，尚未覆盖 Fragment、WebView、播放器控制器和资源链接），导致每一次重构都缺少完整安全网。

因此本方案的第一原则是：**先补网（测试 + 专项校验脚本），再做零行为变更加固，最后才动结构**。

> **2026-08-28 追加（Phase G，初版代码已接入）**：缺陷修补只是"别变坏"，产品还要"变强"。按你的要求补齐了**夸克式能力**的第一块——
> **内置视频播放器**（接管网页自带播放器 + 长按屏幕倍速快进）。它目前仍必须经过覆盖层层级、站点兼容性和真机回归闸门，详见 §5 Phase G。

### 当前进度总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| — | 深度审计、边界与原则确立 | ✅ 完成 |
| — | `scripts/verify_browser.py` 静态契约检查（防越界 / 中英文案对齐 / 注入单点漏斗） | ✅ 完成 |
| — | 浏览器纯 JVM 回归（播放器数学 19 + 播放器脚本 4 + 下载/安全 11） | ✅ **34 个测试已独立编译并通过；Android `Uri` 相关测试仍需资源/设备或 Robolectric 证据** |
| **G** | **内置视频播放器（接管 + 长按快进 + 小窗 + 全屏）** | 🟡 **初版代码已接入；覆盖层层级、站点兼容性、资源链接和真机行为未形成当前轮证据** |
| **0** | 播放器真机验收与自愈（兼容性矩阵、接管失败降级、设置项接线、探测节流） | 🟡 **部分代码已写；0-1/0-5 仍待实机，不能称为可用** |
| A | 补网 + 零行为变更加固（线程泄漏、拦截链路 IO、上下文安全、Toast 统一） | 🟡 **A-3/A-4/A-5 已有首轮修补；Toast 仍保留 11 项既有告警，需后续按生命周期逐项收敛** |
| B | 下载安全 | 🟡 **扩展名 + 基本危险 MIME 已接入私有目录路由；公共导出、URI/同名策略和实机验证未完成** |
| C | 无痕与会话隔离 | 🟡 **已接入“按 Tab、不记历史”的弱语义；真正 Cookie/存储隔离未实现且不能伪称完成** |
| D | 生命周期重绑 | 🟡 **主要切 Tab/重建路径已有接线；统一契约抽取和权限回调回归未完成** |
| E / F | 架构瘦身 / God Class 拆分 | ⚪ **只有接口骨架；必须等行为闸门通过后再拆** |
| H | 播放器能力增强（投屏、后台音频、直链下载、手势自定义） | 🟡 **H-1 直链下载 / H-4 长按开关 / H-5 续播已实现（JVM+编译证据，未真机验收）；H-2 后台音频、H-3 投屏 deferred** |

**关键提醒**：Phase G 目前最多能称为“初版代码存在/局部验证过”，不是“真机可用”。接管类功能的成败取决于各站点 DOM、WebView 合成层级和播放器生命周期，**必须完成 Phase 0 的兼容性矩阵才能对外宣称可用**。

### 框架骨架预置说明（2026-08-28 收尾）

按你的要求，A–H 的**代码框架已先行搭好**：纯逻辑类（无 Android 依赖，可 JVM 单测）、契约/接口（为未来从 `BrowserFragment` 抽离预留稳定签名）、以及 6 个测试脚手架文件。具体实现与实机验证 deferred 到你这边补测时推进，这样后续可以直接在某个契约上"填空"而无需重构。

已落地的骨架文件（均在 `app/src/main/java/com/gamecenter/app/browser/core/` 下）：

| 阶段 | 骨架文件 | 角色 |
|---|---|---|
| A | `app/src/test/.../browser/UrlUtilsTest` `SanitizeFileNameTest` `SecurityPolicyTest` `JsBridgePolicyTest` | 纯 JVM 测试脚手架（A-2 已落地） |
| B | `security/FileNameSanitizer.java`、`security/DownloadSecurityValidator.java` | 文件名净化 + 危险扩展名 → 私有目录 / `.gmdownload` 隔离 |
| C | `incognito/IncognitoProfileManager.java` | 按 Tab 无痕判定 + `applyProfile` 占位 |
| D | `lifecycle/BrowserRebindContract.java` | `rebindHelpers` + `RebindTarget.onActiveWebViewChanged` 契约 |
| E/F | `framework/BrowserFragmentDelegates.java` | 5 个 delegate 接口（URL Bar/下载/权限/Chrome/菜单），为 God Class 拆分预留签名 |
| H | `player/enhancement/PlayerEnhancementContracts.java` | 5 个增强接口（直链下载/后台音频/投屏/手势自定义/播放历史） |

> ⚠️ 这些文件本身仍是“占位 + 契约”，不应被当作业务完成证明：例如 `IncognitoProfileManager.applyProfile` 仍返回 `false`。当前工作区另有部分 B/C/D 逻辑直接接在现有类中，但尚未全部接到这些契约上；补齐实现时必须以运行时调用链和各 Phase 验收项为准，不能只让占位方法返回 `true`。

Phase 0 的 0-1 兼容性矩阵模板已生成：`浏览器播放器兼容性矩阵_模板.md`（8 类站点 × 6 项指标），待装包实机后逐格填写。

## 0.1 2026-08-29 复核结论（本节覆盖前文同名状态）

这份计划的总体方向正确，但原文有以下会导致误判或返工的缺口，已按本节修订后再继续推进：

1. **完成度分级不够严格。** “接口骨架”“代码已接线”“编译通过”“纯 JVM 测试通过”“真机可用”是五个不同状态。后续统一使用 `⚪ 未开始`、`🟢 仅骨架`、`🟡 已实现但未验收`、`✅ 已验证` 四级；没有对应命令输出或设备记录，不得写成“全绿”。
2. **专项脚本的职责被写大了。** 当前 `scripts/verify_browser.py` 是纯静态契约检查，不会编译 Java，也不会运行 JUnit。JVM 单测必须单独执行窄范围命令或等价的纯 JVM harness；脚本通过不能替代单测、资源链接、编译或真机验收。
3. **播放器缺少 P0 层级不变量。** `player_overlay_container` 是静态覆盖层，而 WebView 会在运行时加入；必须明确要求所有 WebView 插入到覆盖层之前（或显式 `bringToFront` 并在每次新建 Tab 后复核），同时验证覆盖层不被硬件合成的 WebView 压住。仅验证 JS 接管成功不够。
4. **下载安全不能把“私有落盘”写成“完整功能”。** 当前轮已补扩展名 + 常见危险 MIME 的目标目录路由；仍要单独设计下载记录中的 URI、同名策略、用户主动“导出到公共目录”的 SAF/授权流程，以及“下载完成后不自动安装/打开”的确认策略。没有这些，不能宣称“危险下载闭环完成”。
5. **无痕的技术边界必须诚实。** `WebView.setDataDirectorySuffix` 是进程级、启动早期约束，不能在同一个进程里按 Tab 来回切换。Cookie 快照/恢复也存在异步竞态和跨 Tab 污染风险；在独立进程或经过专门设计前，只提供“不保存历史/搜索”的弱语义，并在 UI 明示 Cookie/网站存储不隔离，绝不通过 `removeAllCookies()` 伪造隔离。
6. **`MAX_ACTIVE_WEBVIEWS=5` 与逻辑 Tab 上限不是自动矛盾。** 5 个活跃 WebView、10 个逻辑 Tab 是有意的内存策略，但会丢失未提交 DOM 状态；不能随意改成 8。应先做内存/恢复体验测量，再决定提高上限、降低逻辑上限或在 Tab UI 明示“页面可能重建”。
7. **结构重构不能先于行为锁定。** 删除单 WebView 分支、合并 URL/拦截器实现、拆 `BrowserFragment` 都会放大并行冲突和回归面；必须保留当前 fallback，先完成稳定性与回归闸门，再逐个 delegate 抽离。
8. **播放器能力描述需降级为可验证假设。** 接管 DOM 可能保留 MSE/HLS 的页面上下文，但不保证所有 DRM、Shadow DOM、iframe、Canvas/WebGL 播放器都可用；简单的 `devicePixelRatio` 换算也必须通过多密度设备/缩放场景实测。

### 复核后的推进闸门

| 闸门 | 必须证明 | 未通过时的动作 |
|---|---|---|
| G-0 | WebView 位于原生播放器覆盖层之下；新建/切 Tab 后仍成立 | 保留网页播放器，禁止继续扩展小窗/全屏能力 |
| S-1 | 所有宿主导航入口只接受有效 `http/https`；外部协议只允许顶层、按策略确认 | 阻断导航，不以异常兜底代替策略 |
| S-2 | 危险下载真实进入 app 私有目录，记录可打开的 URI；公共导出必须由用户主动触发 | 暂不提供“导出公共目录”，不得声称闭环完成 |
| L-1 | 页面销毁、切 Tab、重建 WebView 后，旧 JS/权限/线程回调不能触达新页面 | 先补代次和取消机制，再做结构重构 |
| T-1 | 纯逻辑测试、静态契约、资源/编译证据互相独立记录 | 只报告已通过的层级，不用一项结果覆盖全部 |
| R-1 | 设备矩阵覆盖播放器接管、手势、小窗、权限、旋转、下载和无痕弱语义 | 功能保持可关闭或内部试用，不对外宣称稳定 |

## 0.2 当前轮执行记录（2026-08-29）

> ⚠️ 本节为**上一轮**（播放器加固轮）的记录，保留作历史。最新一轮见 §0.3。

- 已落地：播放器 WebView/原生覆盖层插入顺序、接管脚本动作白名单与可逆状态、`devicePixelRatio` 坐标换算、播放器重绑/销毁；危险扩展名和常见危险 MIME 改走 app 私有目录；初始 URL 与控制器导航双重校验；无痕切换不再清空进程级 Cookie；WebViewClient 统计池改为共享可过期 best-effort 队列；设置拦截开关改为内存快照；媒体、地理位置、文件选择回调增加生命周期取消/代次保护。
- 已验证：`python scripts/verify_browser.py` 通过（11 项裸 Toast 仅告警）；`python scripts/verify_agent_contract.py` 通过；5 个纯 JVM 测试类共 34 个用例通过；6 个浏览器 XML 文件可解析；受保护资产 34 个文件快照前后未变化；浏览器核心 Java 用 Android SDK 桩编译通过。
- Gradle 证据：已尝试 `:app:compileDebugJavaWithJavac :app:processDebugResources -PautoBumpVersion=false --no-daemon`，并在 `--max-workers=1 -Dorg.gradle.parallel=false -Djava.net.preferIPv4Stack=true` 的收敛参数下重试；两次均在 Gradle 初始化阶段因 `Unable to establish loopback connection` 失败，未进入源码编译/资源链接。该失败不能记作编译失败，也不能记作通过，后续需在 Gradle/系统 loopback 环境恢复后补跑。
- 尚未宣称：未完成 Android 主工程编译/资源链接、安装包冷启动、真机播放器/权限/下载/无痕矩阵；因此 G-0 的静态层级约束已具备，但 R-1 仍为待验收，不能称为“真机稳定”。

## 0.3 第二轮执行记录（2026-08-29，播放器能力补全轮）

本轮目标：按用户要求"把浏览器全部的功能都做出来"，完成 Phase H 中可代码验证的播放器能力增强（H-1/H-4/H-5）与 B14/B19/B20 收尾。无设备连接（`adb devices` 为空），Phase 0 真机矩阵仍阻塞，本轮所有"已验证"均限于静态/JVM/编译层级。

### 已落地

| 项 | 内容 | 文件 |
|---|---|---|
| H-4 | 长按快进手势开关：`long_press_fast_forward_enabled` 设置 + 手势层真停用（关闭时正在快进会立即结束）+ 设置页开关行 + 总开关联动置灰 | `BrowserSettingsManager`、`BrowserPlayerGestureHelper`、`BrowserPlayerOverlay`、`BrowserFragment`、`BrowserSettingsActivity`、`activity_browser_settings.xml`、中英 `strings_browser.xml` |
| H-1 | 播放器直链下载按钮：仅 `isDirectPlayable()`（http/https 直链，blob:/data: 自动隐藏）可见；文件名取 URL 末段经 `FileNameSanitizer` 净化、无扩展名补 `.mp4`；复用 `BrowserDownloadManager`（危险文件自动路由私有目录），并透传 WebView UA | `BrowserPlayerOverlay`（Host 新增 `onDownloadVideo`）、`BrowserFragment`、`overlay_browser_player.xml`（复用 `ic_browser_download`） |
| H-5 | 播放历史与续播：**不新增 Room 表**（DB 迁移属 AGENTS.md §4 高风险且本轮无法跑迁移回归），改用 SharedPreferences(`browser_play_history`)+JSON 快照、键为页面 URL（视频源多为会话级 blob: 不能作键）；接管后时长探明一次性恢复（片头 <5s、片尾 10s 内、直播不恢复）；节流落盘（5s）+ 退出接管 force 落盘 + Fragment 销毁同步落盘（绕过即将 shutdownNow 的 worker） | 新增 `core/player/BrowserPlayHistoryStore.java`（纯逻辑可 JVM 测）、`BrowserFragment` 接线 |
| B14 | 下载同名去重：enqueue 前对**真实落盘目录**（公共 Downloads / app 私有 Downloads）做存在性检查，冲突追加 " (n)" 保留扩展名；并发窄竞态 documented | `BrowserDownloadManager.dedupeFileName` |
| B19 | 翻译跳转补 `resolveActivity` 前置校验，无目标时提示可读文案（原为裸 Throwable.getMessage） | `BrowserTranslateHelper` |
| B20 | 无障碍：`item_bookmark`/`item_reading_list` 装饰性图标与未读圆点标记 `importantForAccessibility="no"` | 两个 item layout |

### 已验证（本轮证据）

- `python scripts/verify_browser.py` 通过（告警仍为 11 项 Toast 基线，未新增）；`verify_agent_contract.py` PASS；`verify_protected_assets.py verify` PASS（34 文件不变，构建前后各查一次）。
- 纯 JVM 测试：javac + JUnitCore harness（android.jar 仅编译期；运行期以 Gradle 缓存 `org.json:json:20250107` 覆盖 SDK stub 的 org.json）。**8 类 47 用例全绿**，含新增 `BrowserPlayHistoryStoreTest`（记录/恢复门槛、编解码回环、同 URL 覆盖、坏 JSON 回落、LRU 淘汰、实例 record/resume）。
- `UrlUtilsTest` 3 例仍失败于 `android.net.Uri`/`android.util.Log` 的 SDK stub（RuntimeException: Stub!），与 §0.2 记录一致属**既有已知限制**，非本轮改动引入。
- Gradle：`:app:compileDebugJavaWithJavac` 与 `:app:processDebugResources` 均 `BUILD SUCCESSFUL`（`-PautoBumpVersion=false --max-workers=1 -Dorg.gradle.parallel=false -Djava.net.preferIPv4Stack=true --offline`；PowerShell 下 `-D` 参数必须整体加引号，否则被拆词）。R.jar 复核确认 `browser_player_resumed`/`browser_settings_long_press_ff(+_summary)`/`browser_translate_open_failed`/`browser_player_download_failed` 字符串与 `sw_long_press_ff`/`player_btn_download` ID 均已生成。
- `git diff --check` 对本轮触碰文件仍报 CRLF 基线告警（计划 §8 已记录）；Python 逐行复核确认**零真实行尾空白**。

### 尚未宣称 / 遗留风险

1. **全部播放器新能力未经真机验收**：直链下载的实际落盘与通知、续播在真实站点的触发、长按开关的手感，均待 R-1 设备矩阵（当前 `adb devices` 为空）。
2. H-1 的"原生播放器兜底（VideoView）"未实现：接管模式已覆盖大多数场景，独立 VideoView Activity 的收益/复杂度比待真机接管成功率数据后再评估。
3. H-2 后台音频（前台服务）与 H-3 投屏（新依赖）维持 deferred，理由见 §Phase H。
4. H-5 选择 prefs 而非 Room：若后续要接入统一数据清理/云同步，需迁移；当前"设置-清除浏览数据"不覆盖播放历史（历史仅 100 条、无隐私敏感字段，均为 URL+进度；如需清理入口另开小任务）。
5. 并发同名下载的窄竞态（dedupe 检查与 enqueue 之间）属 best-effort，已注释说明。

---

## 1. 代码层面的项目理解

### 1.1 它在系统里的位置

| 维度 | 事实 | 证据 |
|---|---|---|
| 模块入口 | `BrowserModuleEntryPoint.kt`：`ModuleInterface` + `FeatureModule`，底部导航槽位，`order=20`，`icon=R.drawable.ic_browser` | `ui/BrowserModuleEntryPoint.kt:44-55` |
| 宿主装载方式 | `BottomNavigationManager` 用 **show/hide**（非 replace）装载 Fragment | `modules/BottomNavigationManager.kt:108-117,160-173` |
| 第二入口 | `BrowserActivity` 以 `replace` + `ARG_URL` 装载，被书签 / 历史 / 阅读列表复用 | `ui/BrowserActivity.java:52-59`；`BookmarkActivity:90`、`HistoryActivity:91`、`ReadingListActivity:89` |
| 导出面 | 全部 10 个浏览器 Activity 均 `android:exported="false"` | `AndroidManifest.xml:174-183` |
| 打包事实 | 源码直编进 `:app`；catalog 浏览器 APK 元数据已清空、冗余 APK 已删 | 上一轮 Phase 0 执行记录 |

**装载方式的推论（很重要）**：show/hide 意味着**切底部导航不会走 `onDestroyView`**。所有"WebView 池保留会话"的设计成立前提是它；反过来说，任何写在 `onDestroyView` 里的销毁动作（关闭 `ioExecutor`、`controller.destroy()`）都只会在退出/销毁/配置变更时触发——这既是保护，也是一个潜在的重建陷阱（见 B7）。

### 1.2 代码地图

> 文件数、行数和固定行号只用于定位当时的审计基线，不作为完成标准；并行改动后必须重新用 `rg --files`、符号名和当前 diff 核对。

| 层 | 文件数 | 行数 | 职责 | 健康度 |
|---|---|---|---|---|
| `ui/` | 基线值 | 基线值 | Fragment / Activity / Adapter | ⚠️ `BrowserFragment` 仍是 God Class，当前行数以工作区为准 |
| `core/` | 基线值 | 基线值 | WebView 池、控制器、设置、Tab、手势、查找、阅读模式、离线缓存 | ⚠️ 单/多 Tab 双分支并存；播放器/安全骨架需区分接线状态 |
| `data/` | 16 | ~1,432 | Room（v4，5 实体）+ 4 Repository + 下载管理器 | ⚠️ DownloadManager 直操 DAO，绕过 Repository |
| `security/` | 5 | ~584 | 安全策略、广告拦截、追踪拦截、统计、JS 桥策略 | ✅ 门控清晰 |
| `bridge/` | 1 | 46 | JS Bridge（默认关闭） | ✅ 域白名单 + flag 双门控 |
| `util/` | 1 | 104 | URL 处理 | ⚠️ 与 `UrlInputHelper` 双实现 |

### 1.3 旧审计结论核对（**旧文档已部分过期，以本表为准**）

| 旧编号 | 问题 | 当前真实状态 |
|---|---|---|
| A1 旋转丢会话 | ✅ **已修** | `BrowserWebViewPool.saveAllStates/collectStateMap/restoreStateMap` + `BrowserFragment.onSaveInstanceState:1689-1710` |
| A2 多 Tab ChromeClient | ✅ **已修** | `BrowserWebViewPool.chromeClientConfigurator:61-79` + `BrowserFragment.configureChromeClient():1783` |
| A3 缓存误删全应用 | ✅ **已修** | `BrowserCacheManager:57-64` 限定 `browser_cache` 子目录 |
| A4 Pool 线程安全 | ✅ **已修** | `ConcurrentHashMap` + `ReentrantLock`（`BrowserWebViewPool:50-58`） |
| A5 Bridge 跨 Tab | ✅ **已修** | `jsBridgeInjectedTabs` Map 按 tabId（`BrowserController:41-42,358-396`） |
| A6 切 Tab 重绑 | ✅ **已修** | `switchToTabById:1400-1411` 重绑 Find/Reader |
| A7 Toast NPE | ⚠️ **半修** | `safeToast` 已存在（`:1603-1612`），但全模块仍有 **50 处** `Toast.makeText`，其中若干在异步 lambda 内未判 `isAdded()` |
| A8 TabManager 内存 | ✅ **已修** | `CopyOnWriteArrayList` + favicon `LruCache(100)` |
| A13 WebSettings 重复 | ✅ **已修** | `BrowserSettingsManager.applyCommonSettings()` 统一 |
| S1 下载路径穿越 | ✅ **已修** | `sanitizeFileName()`（`BrowserDownloadManager:146-178`） |
| S3 初始 URL 清洗 | 🟡 **当前工作区已有修补，待验证** | 初始入口已尝试走 `UrlUtils.processInput`，且控制器增加 `http/https` 防线；需覆盖 `ARG_URL`、搜索词和危险 scheme 回归 |
| S4 策略关闭仍拦危险协议 | ✅ **已修** | `BrowserSecurityPolicy:78-86` |
| S5 混合内容 | ✅ **已修** | `applyCommonSettings():363` `MIXED_CONTENT_NEVER_ALLOW` |
| D4 危险文件拦截 | 🟡 **当前工作区已接私有目录路由，闭环未完成** | 危险扩展名和常见危险 MIME 已可路由到 app 私有 Downloads；URI 记录、同名策略、主动导出公共目录和实机检查仍待完成 |
| D5/D6 Room 迁移 | ✅ **已修** | `fallbackToDestructiveMigration()` + v1→v4 三级迁移 |
| U1 阅读列表桩 | ✅ **已修** | `initHomePage():422-427` 已接 `ReadingListActivity.start()` |
| U2 DiffUtil | ✅ **已修** | 8 个 Adapter 全部 `DiffUtil`（`notifyDataSetChanged` 出现 **0 次**） |
| U4 导航图标 | ✅ **已修** | `getIconResId() = R.drawable.ic_browser` |
| — | — | **以下为本次新发现** ↓ |

### 1.4 必须保持的架构不变量（改动红线）

1. **WebView 生命周期**：任何 WebView 必须先 `removeView` 再 `destroy()`（防 Chromium 渲染进程泄漏，单次 30-80MB）。
2. **事件归属 tabId**：`BrowserWebViewClient` / `BrowserChromeClient` 携带 `tabId`，`BrowserFragment.isCallbackForActiveTab()` 过滤后台 Tab 事件——**前台 UI 绝不能被后台 Tab 驱动**。
3. **安全门控层级**：`shouldOverrideUrlLoading` → `BrowserSecurityPolicy.checkUrlPolicy` → `BLOCKED_SCHEMES` 恒拦（不受 Feature Flag 影响）。
4. **JS Bridge 双门控**：`BROWSER_JS_BRIDGE_ENABLED`（当前 `false`）+ `JsBridgePolicy` 域名白名单，缺一不可。
5. **SSL 错误一律 `handler.cancel()`**（`BrowserWebViewClient:93-96`），不得为业务便利放行。
6. **无痕 = 不写历史**：`saveHistoryIfNeeded` / `saveSearchHistoryIfNeeded` 的 `isIncognitoMode` 短路。
7. **受保护资产**：`catalog.json`、`modules.json`、`assets/modules/*.apk`、`version.properties` 非发布任务不得改。
8. **浏览器字符串只进 `strings_browser.xml`**（详见 §2 边界表）。

---

## 2. 处理边界（我 / 并行 AI / 共享区）

| 路径 | 归属 | 规则 |
|---|---|---|
| `app/src/main/java/com/gamecenter/app/browser/**`（59 文件） | **我，独占** | 可自由重构，但每阶段保持可编译 |
| `app/src/main/res/layout/{fragment_browser,activity_browser*,layout_browser_*,activity_bookmark,activity_download,activity_history,activity_reading_list,activity_offline_cache,activity_privacy_dashboard,activity_tab_manager,activity_bottom_bar_customize,item_bookmark,item_download,item_history*,item_reading_list,item_offline_cache,item_bottom_bar_customize,dialog_add_bookmark}.xml` | **我** | 浏览器专属布局 |
| `app/src/main/res/menu/browser_more_menu.xml`、`drawable/ic_browser*.xml`、`drawable/ic_nav_browser.xml`、`drawable/module_category_browser_gradient.xml` | **我** | 浏览器专属资源 |
| `app/src/main/res/values/strings_browser.xml`、`values-en/strings_browser.xml` | **我** | 新增字符串**只写这里**，含中英双写 |
| `app/src/main/assets/browser/**` | **我** | 起始页 HTML |
| `app/src/test/java/com/gamecenter/app/browser/**` | **我** | 新增单测 |
| ⛔ `values/strings.xml`、`values/colors.xml`、`values-en/strings.xml` | **并行 AI 正在改** | **绝对不碰**（含其中的 2 条孤立 browser 字符串，见 B10） |
| ⛔ `module-store/feature/tools/ai/**`、`layout/fragment_ai.xml`、`layout/item_ai_message*.xml`、`drawable/bg_ai_message_*.xml` | 并行 AI | 不碰 |
| ⛔ `core/moduleloader`、`core/module-host`、`core/modulestore`、`app/.../modules/**` | 并行 AI（加载器隔离重构） | 不碰，不评论，不格式化 |
| ⚠️ `app/build.gradle` | 共享 | 只改 `BROWSER_*` buildConfigField 区段（约 `:378-406`），scoped patch |
| ⚠️ `app/src/main/AndroidManifest.xml` | 共享 | 只改浏览器 Activity 区块（`:174-183`），且不动 `exported=false` |
| ⚠️ `scripts/` | 共享 | 只**新增** `verify_browser.py`，不改既有脚本 |
| ⛔ `catalog.json` / `modules.json` / `version.properties` / `assets/modules/*.apk` | 受保护 | 非发布任务不碰；构建前后跑 `verify_protected_assets.py snapshot/verify` |

---

## 3. 原则性红线（不可让步）

1. **不制造第二套实现**：浏览器已是宿主内代码，不得再引入 DexClassLoader / 模块 APK 加载路径（AGENTS.md §8）。
2. **不改签名、清单权限、数据库迁移、网络协议、模块加载器、Gradle 发布任务**——这些属高风险变更，必须单独走兼容策略 + 针对性测试（AGENTS.md §4）。
3. **每个缺陷修复必须带"修复前失败、修复后通过"的回归测试**（AGENTS.md §5）。
4. **绝不触碰并行 AI 的脏文件**，不 `git checkout --`、不 `git reset --hard`、不顺手格式化无关文件（AGENTS.md §1、§5）。
5. **小补丁、可审查**：禁止无关重命名、全仓格式化、大规模机械改写。
6. **字符串零冲突**：所有新增文案进 `strings_browser.xml`；确需新颜色则新建 `values/colors_browser.xml`（新文件，零冲突），**不写 `colors.xml`**。
7. **验证优先**：每阶段跑 `verify_agent_contract.py` + `verify_protected_assets.py snapshot/verify` + 浏览器专项脚本；**不跑宽泛 Gradle 任务**（`testDebugUnitTest` 会经 `mergeDebugAssets` 重写受保护资产）。
8. **如实标注未验证项**：跑不了的验证必须写明原因与风险，禁止"应该可以"（AGENTS.md §5）。
9. **编译通过 ≠ 完成**：涉及布局/主题/资源 ID 的改动，必须装打包后的宿主 APK 做一次冷启动验收（AGENTS.md §6）。
10. **交付只报本次实际改动**，不把工作区既有改动冒充成果。

---

## 4. 剩余缺陷清单（逐条核实，NEW = 本次新发现）

> 本节表格是 2026-08-28 的问题基线；本工作区已经有并行中的浏览器修补，因此不要直接把表中的“未修”当作当前事实。当前状态以 §0.1、各阶段的验收记录和最新 `git diff` 为准。

### P1 —— 会在长会话中真实退化

| ID | 位置 | 问题 | 证据 | 影响 |
|---|---|---|---|---|
| **B1** 🆕 | `BrowserWebViewClient:53` | **线程泄漏**：每个 client 实例 `newSingleThreadExecutor()`，**全模块无任何 shutdown**。池内 WebView 每次 `releaseTab`/`trimToActiveOnly` 后重建 → 每轮泄漏 1 条非守护线程 | 全仓 `Executors.new` 共 11 处，无对应 `shutdown` | 长会话线程数单调增长 → OOM/ANR |
| **B2** 🆕 | `BrowserWebViewClient:128-143` | **拦截链路同步 IO**：`shouldInterceptRequest` 在 Chromium IO 线程上对**每个子资源**读 `SharedPreferences`（`isDataSaverEnabled()`，`:138`），并对每个拦截请求 `execute()` 提交任务 | `:136-138` 无缓存 | 页面加载变慢，资源越多越明显 |
| **B3** | `BrowserDownloadManager:100` | **危险文件仍落公共目录**：`isDangerousFile()` 结果只 `setDangerous()` 入库（`:123,135`），`setDestinationInExternalPublicDir` 无条件执行 | 注释声称"走私有目录"，代码未实现 | .apk/.exe/.js 直落 `Downloads/`，可被其他应用直接拉起安装 |
| **B4** 🆕 | `BrowserFragment:1346-1361` | **假无痕 + 误清全局 Cookie**：`isIncognitoMode` 只是 Fragment 布尔位，与 Tab 无绑定；`BrowserTabManager.createIncognitoTab` 存在但菜单从不调用。切换无痕时 `clearWebViewData()` + `removeAllCookies(null)` **清空全部 Cookie** | `BrowserController:312-327` 全局 `CookieManager` | 关一次无痕 = 全站登录态丢失；"无痕"实际只不记历史 |
| **B5** 🆕 | `BrowserFragment:204-218` | **关闭全部 Tab 后辅助类未重绑**：`__all__` 分支 `controller.destroy()` + `initWebView()` 重建池，但 `findInPageHelper` / `readerModeHelper` / `gestureHelper` 仍指向**已销毁的 WebView** | 对比 `switchToTabById:1391-1411` 有重绑 | 关全部 Tab 后：查找、阅读模式、边缘手势全部静默失效 |
| **B6** | `BrowserFragment:334` | **`initialUrl` 未过安全清洗**：直接 `controller.loadUrl(initialUrl)`，绕过 `UrlUtils.processInput` | 对比 `:210-213` `loadInput()` 才清洗 | 内部调用方可借 `javascript:` 在浏览器上下文执行任意 JS（纵深防御；`exported=false` 已堵外部面） |

### P2 —— 稳健性 / 一致性 / 技术债

| ID | 位置 | 问题 |
|---|---|---|
| **B7** 🆕 | `BrowserFragment:160` + `:1737-1739` | `ioExecutor` 声明为 `final` 却在 `onDestroyView` `shutdownNow()`。一旦 Fragment 实例存活而 View 重建（replace / 回退栈复用场景），后续 `ioExecutor.execute()` 在 `try` 之外抛 `RejectedExecutionException` → **崩溃**（书签、历史、阅读列表写入全部走它）。应改为非 final + 懒重建（照 `BrowserDownloadManager.getExecutor():40-45` 模式） |
| **B8** | 全模块 50 处 `Toast.makeText` | 已有 `safeToast()`/`showFeedback()` 两套安全封装但只用了一部分。`:234,238,989,1029,1065,1206,1208,1304,1430,1811` 等处在异步 lambda / `mainHandler.post` 内直接 Toast，未判 `isAdded()` |
| **B9** 🆕 | `BrowserFragment:1811,1844,1890,1926` 等 | `configureChromeClient` 内 4 处 `requireContext()`：文件选择 / 地理位置 / 媒体权限 / 全屏回调在 Fragment detach 后触发 → `IllegalStateException` |
| **B10** 🆕 | `values/strings.xml:35-36` | `browser_loading`、`browser_initial_url` 是**孤儿字符串**（全仓无引用），却躺在并行 AI 正在编辑的共享文件里。**暂不动**，登记待其稳定后迁出或删除 |
| **B11** | `app/src/test/.../browser/` | **测试覆盖率近乎为零**：仅 `SearchHistoryDaoTest.kt`（88 行）。文件名净化、URL 清洗、安全策略判定这些纯逻辑**一行测都没有** |
| **B12** 🆕 | `BrowserWebViewClient:99-114` | `shouldOverrideUrlLoading` 未判 `request.isForMainFrame()`：iframe / 子资源触发外部 scheme 也会弹确认框 → 网页可用 iframe 刷弹窗（点击劫持骚扰面）。另未判 `request.hasGesture()` |
| **B13** 🆕 | `BrowserWebViewPool:42` vs `BrowserTabManager:26` | `MAX_ACTIVE_WEBVIEWS=5` 与 `MAX_TABS=10` **不一致**：第 6~10 个 Tab 静默降级为 `saveState/restoreState`（JS 上下文、未提交表单、播放器进度丢失），用户无感知 |
| **B14** 🆕 | `BrowserDownloadManager:158-161` | `sanitizeFileName` 中 `.replace(">", "\|")` 是笔误（应为 `"_"`），虽被后续 `.replace("\|","_")` 兜住结果正确，但语义误导；且未处理 NUL/控制字符；同名文件静默覆盖 |
| **B15** 🆕 | `BrowserWebViewClient:118-145` + `security/AdBlocker` + `security/BrowserTrackerBlocker` | 广告拦截与追踪拦截是**两套几乎相同的 host 匹配实现**（`AdBlocker.shouldBlock:33-62` / `BrowserTrackerBlocker`），每加一条规则要改两处 |
| **B16** | `BrowserController:36-40,78-106,164-185`、`BrowserFragment:1424-1475` | `BROWSER_REAL_MULTI_TAB=true` 恒定 → **单 WebView 整条分支是死代码**（`webView`、`webViewClient`、`chromeClient`、`saveSingleWebViewState`、`restoreSingleWebViewState`、`showTabList()`），且 `getChromeClient()` 在多 Tab 下恒返回 null，是持续误导 |
| **B17** | `util/UrlUtils` vs `core/UrlInputHelper` | 两套 URL/搜索处理实现并存（`processInput` vs `UrlInputHelper.processInput`），`saveSearchHistoryIfNeeded` 里还在做双分支 fallback（`:948-974`） |
| **B18** | `BrowserCacheManager:26-32` | 为清缓存 `new WebView(context)` 用完即毁（A12 未修），白付一次内核初始化 |
| **B19** 🆕 | `BrowserTranslateHelper:52-56` | `ACTION_VIEW` 打开翻译页未 `resolveActivity` 校验（靠 `Throwable` 兜底），与 `BrowserSecurityPolicy.canExternalAppHandle` 的既有做法不一致 |
| **B20** | `item_bookmark.xml`（img 2 / desc 1）、`item_reading_list.xml`（img 2 / desc 1） | 无障碍 `contentDescription` 少量缺失（`fragment_browser.xml` 15/14 差 1） |
| **B21** 🆕 | `BrowserVideoController.startProbing()` | **探测无节流**：Fragment 存活期间每 1.2s 对每个页面注入探测脚本，纯文本页也一直跑。应"连续 N 次无结果即暂停，页面加载完成再唤醒" |
| **B22** 🆕 | `BrowserVideoController.takeOver()` | **接管无效果校验**：只判断"脚本执行成功"，不代表视频真的被提到最前。站点重渲染会导致接管后页面空白/视频消失，需要接管后校验 + 自动降级 `TAKE_OVER_STYLE_ONLY` |
| **B23** 🆕 | `BrowserPlayerOverlay.show()` | 同步调用 `syncVideoRectToOverlay()` 时布局未完成，宽高可能为 0 → 向页面下发 0 尺寸矩形，视频缩成一点（已改为 `post`，待实机确认） |
| **B24** 🆕 | `BrowserSettingsActivity` | **设置项未接线**：`video_player_enabled` / `fast_forward_rate` 读写接口已就绪，但设置页无入口 → 用户无法关闭内置播放器、也无法调长按倍速。属半接线 |

---

## 5. 改造路线图（2026-08-28 晚间重排版）

> 排序原则：**先补网 → 再零行为变更加固 → 最后动结构**。每阶段独立可回滚、独立可验收。
>
> **本次重排的原因**：Phase G（内置播放器）已落地并通过编译，但它**尚未经过真机验证**。
> 一个未验证的高风险能力如果压在一堆重构之上，出问题时会分不清是"播放器接管失败"还是"重构引入的回归"。
> 因此把**真机验收 + 播放器自愈**提到最前，成为 Phase 0。

### Phase 0 — 播放器真机验收与自愈（🔴 立即，最高优先）

| # | 任务 | 说明 |
|---|---|---|
| 0-0 | **覆盖层层级与触摸闸门** | 确认所有 WebView 动态插入在 `player_overlay_container` 之前；新建 Tab、池释放后重建 WebView 仍成立；覆盖层能收到按钮/手势触摸，播放器未显示时普通页面仍可滚动 |
| 0-1 | **站点兼容性矩阵** | 装包后逐站点实测并记录接管成功率：普通 mp4 页、B 站、腾讯视频、爱奇艺、YouTube、网易云音乐 MV、含 iframe 播放器的站点。每个站点记录「能否探测 / 能否接管 / 进度是否同步 / 倍速是否生效 / 退出是否还原」 | 🔴 **待实机（无设备，当前阻塞）** |
| 0-2 | **B22 接管失败自愈** | 接管后约 1.2s 内校验视频仍可见且尺寸正常；失败则自动降级 `TAKE_OVER_STYLE_ONLY`，再失败则回滚并提示。只有控制器回调、脚本回归测试和真机页面都通过后才标记 ✅ | 🟡 **代码已写，当前轮证据待重跑** |
| 0-3 | **B24 设置项接线** | `video_player_enabled` / `fast_forward_rate` 有读写接口和设置页入口；需验证开关关闭后不再注入脚本、重建 Fragment 后设置仍一致，且状态变更不触发错误的全页重载 | 🟡 **代码已写，当前轮资源/行为证据待重跑** |
| 0-4 | **B21 探测节流** | 连续 6 次空探测即 `probeSuspended`，`scheduleNextProbe` 直接 return；`probeOnce()`/`resumeProbingLocked()` 为唯一唤醒入口，避免对纯文本页持续每 1.2s 注入脚本 | ✅ **代码完成（编译+单测绿）** |
| 0-5 | 实测补齐 `ic_browser_player_*` 的暗色主题表现 | 播放器 UI 固定用 `@color/white`，需确认在浅色主题下对比度是否够（必要时加描边/阴影） | 🔴 **待实机** |

**验收**：0-0 是播放器继续扩展的前置闸门；0-2/0-3/0-4 需分别记录静态契约、纯逻辑/脚本回归、必要时的资源链接或编译结果；0-1/0-5 输出《接管兼容性矩阵》后才允许标记“可用”。`verify_browser.py` 只能证明静态契约，不得代替其它层级。
**风险**：0-2 的“可见性校验”依赖 JS 返回值（`BrowserVideoJs.VERIFY` 返回 `ok:true`，解析失败一律保守判失败），若站点阻止探测需退化为“用户手动重试”提示；不要为了提高成功率而放宽安全失败条件。

### Phase A — 补网 + 零行为变更加固（建议首批，低风险）

> 🟡 **首轮补丁已写入当前工作区**：A-2 的 5 个纯 JVM 测试类共 34 个用例已通过；`verify_browser.py` 仍是静态检查，不会编译或运行这些测试。A-3/A-4/A-5 已完成首轮加固，A-6 只处理了高风险异步 Toast，剩余告警不做机械清零。

| # | 改动 | 文件 |
|---|---|---|
| A-1 | 维护 `scripts/verify_browser.py`（只做浏览器静态契约、资源边界和脚本安全检查；不冒充 Java 编译或 JUnit runner） | 新增 |
| A-2 | 新增纯 JVM 单测：`UrlUtilsTest`（危险协议、host 提取）、`SanitizeFileNameTest`（路径穿越/长度/边界）、`SecurityPolicyTest`（BLOCKED_SCHEMES 恒拦、未知协议拦截）、`JsBridgePolicyTest`（必须断言真实域白名单；仅“类可加载”的脚手架不算完成） | `app/src/test/.../browser/` |
| A-3 | **B1 线程泄漏**：`BrowserWebViewClient` 使用可过期的共享 best-effort 统计池；池释放 WebView 时不创建与 Tab 数量成正比的线程 | `core/BrowserWebViewClient.java` |
| A-4 | **B2 拦截链路**：`dataSaver` / `adBlock` / `trackerProtection` 由设置中心维护 volatile 内存快照，client 在创建时绑定设置中心；`shouldInterceptRequest` 不再读取 SharedPreferences，统计允许丢弃但不得阻塞导航 | `core/BrowserWebViewClient.java`、`core/BrowserSettingsManager.java` |
| A-5 | **B7/B9 上下文安全**：`ioExecutor` 按 View 生命周期懒重建；Chrome/权限异步回调在显示前和回写前校验 Fragment、View、WebView、Tab/请求代次，地理位置、媒体、文件选择均有销毁/切换取消路径，禁止用 `requireContext()` 作为异步兜底 | `ui/BrowserFragment.java` |
| A-6 | **B8 反馈收敛**：优先修复异步回调、Fragment detach、后台线程回主线程的裸 Toast；Activity 内同步 Toast 可保留，禁止为“数量归零”做无关机械改写 | 浏览器 UI / core |

**验收**：静态检查、纯 JVM 测试、必要的编译/资源检查分开记录；`verify_agent_contract.py`、受保护资产校验和 scoped `git diff --check` 不能互相替代。不得把脚本 PASS 写成“单测绿”。
**回滚点**：A-3/A-4 为独立提交，可单独 revert。

### Phase B — 下载与文件安全（B3 / B14 / B19）

> 🟡 **部分已接线到当前工作区**：`FileNameSanitizer` / `DownloadSecurityValidator` 已有纯逻辑测试，危险扩展名和常见危险 MIME 已接入 `BrowserDownloadManager` 的 app 私有目录路由；URI 持久化一致性、主动导出公共目录、同名策略和实机验证仍 deferred。

- 危险扩展名（`.apk/.exe/.bat/.sh/.cmd/.vbs/.js/.msi`）或常见危险 MIME 改落 **app 私有目录** `getExternalFilesDir(DIRECTORY_DOWNLOADS)`；下载列表先支持安全打开/删除，若以后提供“另存到公共目录”，必须使用 SAF/授权 URI，并由用户主动触发；在此之前不得声称公共导出已完成。
- `sanitizeFileName`：修 `>` 笔误、清 NUL/控制字符；同名去重需在真实落盘/SAF 层实现，不能只靠字符串工具宣称完成。
- 翻译跳转补 `resolveActivity` 校验。
- **验收**：新增 `DownloadSafetyTest`（路径穿越用例：`..%2F..%2Fevil.apk`、`a/../../b`、超长名、纯点号名）；装包后手动验一次"下载 apk → 不在公共目录"。

### Phase C — 无痕与会话隔离（B4 / B13）

> 🟡 **弱语义已接线，强隔离仍未实现**：`Tab.isIncognito` 已参与路由，历史/搜索不写入；`IncognitoProfileManager.applyProfile` 仍是占位，真实 Cookie/网站存储隔离需要独立进程或经专项设计的方案，不能用同进程切换 `setDataDirectorySuffix` 代替。

- 把无痕从"Fragment 布尔位"下沉为**按 Tab 维度**：`BrowserTabManager.Tab.isIncognito` 真正参与路由，`menu_incognito` 改为"新建无痕 Tab"。
- **禁止切换时清空全局 Cookie**；当前只承诺“无痕 Tab 不写历史/搜索”，UI 必须明示 Cookie/网站存储与普通 Tab 共享。独立进程方案若要启动，需另行评估进程通信、内存和清理时机。
- `MAX_ACTIVE_WEBVIEWS` 与 `MAX_TABS` 不做任意统一；先测量 LRU 释放后的恢复体验与内存，再决定上限或在 Tab UI 明示页面可能重建。
- **验收**：`IncognitoRoutingTest` 验证路由与“不写历史”语义；实机确认开/关无痕不会清空普通 Tab 登录态，并明确记录“Cookie 未隔离”的现状。

### Phase D — 生命周期与重绑统一（B5 / B6）

> 🟡 **主要路径已有接线，统一契约仍未落地**：切 Tab、关全部 Tab 重建和新建 Tab 已有局部重绑逻辑；`BrowserRebindContract` 仍是预留接口，权限/播放器/建议框等异步回调还需单独回归。

- 抽出 `rebindHelpers(@NonNull WebView newWebView)`（手势 + Find + Reader + 下载监听 + ChromeClient 配置器），在**每一处 active WebView 变化点**调用：`switchToTabById`、关全部 Tab 重建、新建 Tab；抽取前先保留现有局部接线，避免一次性移动大段逻辑。
- `initialUrl` 和地址栏最终统一到一个可测试的 URL resolver；入口先拒绝危险 scheme，再由控制器做 `http/https + host` 纵深校验，并记录 `BROWSER_URL_REJECTED` 日志。不要让 `UrlInputHelper`、`UrlUtils` 两套规则长期漂移。
- **验收**：实机冒烟：多 Tab → 关全部 → 新建 Tab → 验证查找栏 / 阅读模式 / 边缘手势可用；`adb shell` 传 `javascript:` 串不执行。

### Phase E — 架构瘦身（B16 / B15 / B17 / B18）

> 🟢 **只有接口骨架**：`framework/BrowserFragmentDelegates.java`（URL Bar / 下载 / 权限 / Chrome / 菜单 5 个 delegate 接口 + `Registry` 持有器）已落地，但没有业务接线；具体类（如 `BrowserUrlBarController`）deferred。

- **在确认所有构建变体都不再需要后，才评估删除单 WebView 分支**。当前 `BROWSER_REAL_MULTI_TAB` 只是本构建的配置事实，不等于永久 API 契约；在删除前必须保留 fallback 并验证 BrowserActivity、配置变更和测试变体。
- 合并 `AdBlocker` / `BrowserTrackerBlocker` 的 host 匹配为共用 `DomainMatcher`；规则表合一处维护。
- 从 `UrlUtils` / `UrlInputHelper` 提取一个无 Android/数据库副作用的 `BrowserUrlResolver` 作为规则真源；两者先委托它，再逐步删除重复分支。不要让带 UI/数据库职责的 `UrlInputHelper` 直接成为所有调用方的底层依赖。
- `BrowserCacheManager` 优先接收并复用当前活跃 WebView；没有活跃 WebView 时不要为了清缓存盲目初始化一个内核，若平台 API 无法做到无实例清理，应保留现状并记录限制，而不是用未验证的替代路径。
- **验收**：`verify_browser.py` + 构建通过 + 实机冷启动 + 浏览器全菜单巡检。

### Phase F — God Class 拆分与体验收尾（B9 结构项 / B20）

> 🟢 **只有接口骨架**：拆分目标类的签名已锁定在 `framework/BrowserFragmentDelegates.java`（见 Phase E）；逐项抽离与收尾 deferred。

`BrowserFragment` 按职责外移，**顺序为“先抽无状态 delegate，后抽有状态 controller”**；行数只作观察指标，不设“为达 700 行而拆分”的硬验收门槛：

| 新类 | 移出内容 | 行数估算 |
|---|---|---|
| `BrowserUrlBarController` | URL Bar、建议 popup、搜索引擎选择、粘贴并前往、搜索历史 | ~350 |
| `BrowserDownloadDelegate` | `handleDownload`、危险文件确认、下载列表入口 | ~60 |
| `BrowserPermissionsDelegate` | 文件选择、全屏视频、地理/媒体权限 | ~180 |
| `BrowserChromeBinding` | `configureChromeClient` + 全屏视图管理 | ~120 |
| `BrowserMenuController` | `showMoreMenu` 及其 20 个分支 | ~140 |

- 补 `item_bookmark.xml` / `item_reading_list.xml` 缺失的 `contentDescription`。
- 每拆一类跑一次 `verify_browser.py` + 编译，**禁止一次性大改**。
- **验收**：每个 delegate 有单一职责、明确生命周期、无对 Fragment/View 的反向泄漏；逐项实机回归（菜单每一项、权限弹窗、全屏、下载、建议框）。`BrowserFragment` 行数下降是结果，不是单独的质量指标。

---

### Phase G — 内置视频播放器（本次已实现）

> 对应诉求："浏览器有内置播放器，能跳过网页自己的播放器；长按屏幕就快进"。

#### G.1 关键架构决策：为什么不做"重新缓冲"的原生播放器

| 方案 | 做法 | 结论 |
|---|---|---|
| ❌ 原生播放器直连 | 拿视频 URL 交给 VideoView/ExoPlayer 重新播放 | **否决**。B 站/腾讯/爱奇艺等主流站点是 MSE/HLS，源为 `blob:` 或分片 m3u8，脱离页面上下文拿不到完整流，直连成功率极低；项目也没有 ExoPlayer/Media3 依赖，引入会动 `dependencies.gradle`（并行 AI 的共享文件） |
| ✅ **接管页面 video 元素**（初版已实现） | 视频仍由网页内核解码，用 JS 把 `<video>` 提到最前铺满，再叠一层原生 UI 控制它 | **采用**。不重新缓冲，有机会保留 blob/MSE/HLS 的页面上下文；不保证所有 DRM、Shadow DOM、iframe、Canvas/WebGL 播放器；零新增依赖，最终以兼容性矩阵为准 |

#### G.2 已落地文件

| 文件 | 职责 |
|---|---|
| `core/player/BrowserVideoJs.java` | 注入脚本唯一来源：`DETECT` 探测打分选视频、`select` 锁定元素、`action()` 下发控制、`TAKE_OVER`/`RELEASE` 接管与还原、`setRect` 小窗 |
| `core/player/BrowserVideoState.java` | 状态快照 + JSON 解析，**按不可信输入处理**（脏数据一律回落安全默认值） |
| `core/player/BrowserVideoController.java` | 周期性探测、控制下发、倍速状态机、接管态管理；代次校验保证旧 Tab 回调不污染新 Tab |
| `core/player/BrowserPlayerMath.java` | 纯计算（倍速钳制/时间格式化/seek 换算/档位轮转），**无 Android 依赖 → 可纯 JVM 单测** |
| `core/player/BrowserPlayerGestureHelper.java` | 手势状态机：长按快进 / 双击暂停 / 单击显隐 / 横向 seek / 两侧调音量亮度 |
| `ui/BrowserPlayerOverlay.java` | 原生控件层（播放/进度/±10s/倍速/全屏/小窗/退出）+ 手势接线 |
| `res/layout/overlay_browser_player.xml`、`drawable/ic_browser_player_*`、`values/colors_browser.xml` | 播放器 UI 资源 |
| `values*/strings_browser.xml` | 播放器文案（中英双写） |

#### G.3 交互规格

| 手势 | 行为 |
|---|---|
| **长按画面（不动）** | **进入倍速快进**，默认 2x；按住超过 3 秒自动升到 3x；松手恢复用户倍速 |
| 单击 | 显示/隐藏控制条 |
| 双击 | 播放 / 暂停 |
| 横向拖动 | 快进/快退（单次最多 50% 时长），实时显示 `+15 秒 / 03:20 / 42:00` |
| 左半屏纵向拖 | 调亮度 |
| 右半屏纵向拖 | 调音量 |

控件：播放/暂停、进度条拖动、±10 秒、倍速（0.5x→3.0x 七档轮转）、全屏（隐去浏览器顶/底栏）、小窗（可拖动，视频同步缩放）、退出接管（还原页面 DOM）。

**判定优先级**：手指移动超过 touchSlop 就取消长按 → 转为 seek/音量手势，杜绝"想拖进度却误触发倍速"。

#### G.4 安全与健壮性设计

- **脚本零注入面**：JS 只接受 Java 侧构造的数值与固定枚举，页面可控字符串永不拼进脚本（`verify_browser.py` 有强制检查）。
- **单点漏斗**：播放器脚本只允许 `BrowserVideoController.execJs` 一处下发（同上，脚本强制检查）。
- **双门控**：`BuildConfig.BROWSER_VIDEO_PLAYER`（编译期）+ 用户设置 `video_player_enabled`（运行期），关闭后一次脚本都不注入。
- **代次校验**：切 Tab / 重建 WebView 后，旧 WebView 的探测结果不会写到新 Tab 的 UI。
- **倍速守序**：站点在切集/插播广告后常把 `playbackRate` 重置为 1，探测到漂移自动重新下发用户倍速。
- **降级路径**：移动 DOM 会触发少数站点重渲染，提供 `TAKE_OVER_STYLE_ONLY`（只改样式不移节点）；当前有初步自动重试/回滚代码，但必须用脚本回归和真机验证其状态机，不得仅凭返回值宣称成功。
- **生命周期**：Fragment 暂停/切模块时撤销长按倍速并停止探测，防止"手指离开屏幕仍在倍速"。

#### G.5 已知限制（诚实交代）

1. 只接管标准 `<video>` 元素；Canvas/WebGL 渲染的流（少数加密流）探测不到。
2. 小窗坐标依赖 WebView 与覆盖层同屏换算；切到其它 Tab 会退出小窗。
3. 倍速最终由 WebView 内核实现，个别站点会在特定时机强制重置（已有守序机制缓解）。
4. **尚未做实机验证**：编译已通过，但真机上的接管成功率需要装包后逐个站点试 —— 这正是 Phase 0 的 0-1 项任务（待设备）。
5. **设置项未接线（B24）**：🟡 当前工作区已有设置页开关与「长按快进倍速」选择行，实时驱动代码也已接入；仍需验证关闭后无脚本注入、配置变更和 Fragment 重建场景。

### Phase H — 播放器能力增强（2026-08-29 第二轮部分落地）

> 🟡 **H-1 / H-4 / H-5 已实现**（详见 §0.3 执行记录；JVM 测试 + 编译 + 资源证据齐备，**真机验收未做**——设备缺失）。H-2 / H-3 维持 deferred。

| # | 能力 | 状态 |
|---|---|---|
| H-1 | 直链视频（mp4）**下载**：覆盖层按钮仅 `isDirectPlayable()` 直链显示（blob:/data: 隐藏）；文件名 URL 末段净化 + 无扩展名补 `.mp4`；复用 `BrowserDownloadManager`（危险文件私有路由）并透传 WebView UA。"原生播放器兜底（VideoView）"未做（理由见 §0.3） | 🟡 已实现待真机 |
| H-2 | 后台音频播放（息屏/切模块后继续放声音） | ⚪ deferred（需前台服务，高风险） |
| H-3 | 投屏（DLNA / Cast） | ⚪ deferred（需新依赖，先调研 `dependencies.gradle` 冲突面） |
| H-4 | 手势与倍速自定义：长按快进**总开关** + 长按**倍速**档位（既有）均接设置页；关闭时正在进行的快进立即结束、总开关关时行置灰 | 🟡 已实现待真机 |
| H-5 | 播放历史与续播：prefs+JSON（键=页面 URL，100 条 LRU）；5s 节流 + 退出接管/Fragment 销毁终值落盘；恢复门槛 >5s、距片尾 >10s、非直播；**未用 Room**（规避 AGENTS.md §4 高风险迁移且当前无法跑迁移回归） | 🟡 已实现待真机 |

---

## 6. 验证体系

| 层级 | 工具 | 命令 | 频率 |
|---|---|---|---|
| 静态契约 | `verify_agent_contract.py` | `python scripts/verify_agent_contract.py` | 每阶段 |
| 受保护资产 | `verify_protected_assets.py` | 构建前 `snapshot` / 构建后 `verify`；不构建时也要确认未触碰受保护文件 | 涉及构建时必跑 |
| 浏览器专项 | `verify_browser.py` | `python scripts/verify_browser.py`；只证明静态契约，不编译、不运行 JUnit | 每阶段 |
| JVM 单测 | JUnit（纯 JVM，无需 Robolectric） | 优先窄范围运行浏览器测试；若使用 Gradle，跑前后遵守受保护资产快照规则 | 修改逻辑时 |
| 编译 | Gradle | `:app:compileDebugJavaWithJavac`；仅在需要编译证据时运行，并记录 versionCode 副作用 | 改动 Java/资源且允许时 |
| 资源链接 | Gradle | `:app:processDebugResources`（校验 XML/drawable/字符串引用）；资源改动必须说明是否因轻量验证未执行 | 改动资源时 |
| 实机冒烟 | adb | 旋转 / 多 Tab 切换 / 下载 / 外部链接 / 无痕 / 全屏 / 权限 | Phase B/C/D/F 末 |
| **播放器兼容性矩阵** | adb + 人工 | 见 Phase 0 的 0-1，逐站点记录六项指标 | **Phase 0（当前）** |

**播放器专项冒烟（每次装包必跑；没有设备时只能标记待验收）**：
1. 打开含视频的页面 → 左下角是否浮出"内置播放器"入口；
2. 点入口 → 视频铺满、网页自带控件被盖住、Toast 提示已接管；
3. **长按画面 → 是否出现"2.0x 快进中"、松手是否恢复 1.0x**；
4. 按住超过 3 秒 → 是否升到 3.0x；
5. 双击 → 播放/暂停；横向拖 → 进度提示与跳转；
6. 点小窗 → 视频是否缩到右下角且可拖动、声音是否正常；
7. 点关闭 → 页面是否还原到原样（视频回到原来的位置、网页控件恢复）。

**实机冒烟固定清单（每次装包必跑；未装包不得写成通过）**：
1. 冷启动 → 底栏进浏览器 → 起始页正常；
2. 打开 3 个 Tab 各加载一页 → 互相切换，滚动位置/登录态保留；
3. 旋转屏幕 → 会话不丢；
4. 下载一个文件 + 下载一个 `.apk` → 检查落盘位置；
5. 打开外链（如 `weixin://`）→ 弹确认，无目标应用时不崩；
6. 全屏视频 → 进出正常，横竖屏恢复；
7. 无痕开/关 → 普通 Tab 登录态保留；
8. 退出浏览器 → 再进入 → 无残留 Toast/PopupWindow。

---

## 7. 并行协作 SOP（避免与另一 AI 冲突）

1. **开工前**：`git status --short`，把非我范围的改动视为用户资产，记录基线，**不碰、不回滚、不格式化**。
2. **改中**：只 `git add` 我范围内的路径；提交信息统一前缀 `fix(browser):` / `refactor(browser):` / `test(browser):`，便于并行方识别。
3. **资源零冲突**：文案只进 `strings_browser.xml`（中英双写）；新颜色新建 `colors_browser.xml`；绝不编辑 `strings.xml` / `colors.xml`。
4. **构建规避**：不运行 `:app:testDebugUnitTest`、`:app:assembleDebugWithPreinstalledModules` 等会重写受保护资产的任务（AGENTS.md §4）。
5. **冲突处理**：若 `git status` 出现我范围内的非预期改动（疑似并行方误入），**先暂停并报告**，不擅自 revert。
6. **每阶段交付**：列出实际改动文件、验证命令与结果、未验证项与风险、与并行方重叠的预警。

---

## 8. 风险与未验证项

| 项 | 状态 | 说明 |
|---|---|---|
| 文档前半部分行号 | 🟡 **仅作历史定位** | 基于 HEAD `8461604` 的审计基线；并行改动后必须以符号名和当前 diff 复核 |
| **编译 / 资源链接（历史记录）** | 🟡 **不能代替当前轮证据** | 曾记录 `processDebugResources` / `compileDebugJavaWithJavac` 成功，但当前工作区新增修补后需重新决定是否执行窄范围检查，并记录 versionCode 副作用 |
| **单测（当前轮）** | 🟡 **待独立运行** | `verify_browser.py` 仅是静态检查；必须另行运行浏览器纯 JVM 测试，不能把脚本 PASS 当成单测结果 |
| **契约 / 受保护资产（当前轮）** | 🟡 **待复核** | 应重新运行 `verify_agent_contract.py` 和受保护资产检查；不得只沿用上一轮输出 |
| **B21 / B22 / B24 代码** | 🟡 **已写但未完成验收** | 探测节流、接管失败自愈、设置项开关与倍速选择已有代码；当前轮需补回归测试、资源/编译证据和真机行为记录 |
| scoped `git diff --check` | ⚠️ 仓库级 CRLF 基线 | 仓库无 `.gitattributes` 且 HEAD 两个 XML 均为 CRLF；`git diff --check` 默认把行尾 CR 当 trailing-whitespace，且仅检查 diff 内 `+` 行，故本次新增的 `activity_browser_settings.xml` 44 行被标红。内容本身无多余空格（Grep + Python `rstrip` 双重确认）。未改写全文件以免产生巨 diff 与并行方冲突 |
| **播放器真机行为** | 🔴 **未验证（当前最大风险）** | 接管成功率、长按快进在各站点的实际表现、小窗坐标换算、暗色/浅色主题对比度，全部需要装包实机验证 |
| B1/B2 线程与 IO 影响 | ⚠️ 静态推断 | 需长会话 profile 佐证（`adb shell dumpsys meminfo` + `StrictMode` 抓磁盘读） |
| B4 无痕隔离 | 🟡 **弱语义已定，强隔离待定** | 当前只承诺不写历史/搜索；`WebView.setDataDirectorySuffix` 需进程启动前设置，与“按 Tab 隔离”存在技术冲突，除非采用独立进程，否则不承诺 Cookie 隔离 |
| Phase E/F 拆分 | ⚠️ 高风险 | 涉及 `BrowserFragment` 大量方法移动，必须逐类拆、逐类验，不得合并为一次大改 |
| B10 孤儿字符串 | ⚠️ 阻塞 | 位于并行 AI 编辑中的 `strings.xml`，待其稳定后再处理 |
| 全局 `git diff --check` 基线问题 | ⚠️ 仓库 CRLF 基线 | 仓库无 `.gitattributes`、XML 均为 CRLF；`git diff --check` 默认把行尾 CR 视为 trailing-whitespace。我的新增行（如 `activity_browser_settings.xml`）被标红属此基线，内容无多余空格；为不扰动并行方未改写历史行 |

---

## 9. 建议的下一步（按优先级）

1. **先完成 G-0 / S-1 / L-1 的当前轮轻量检查**：确认 WebView 与播放器覆盖层的真实层级、宿主导航入口的 scheme 防线、旧权限/线程/JS 回调的代次和取消逻辑。
2. **再完成 Phase A 的低风险加固**：先收敛线程和异步上下文，再处理拦截策略快照；Toast 只修真实生命周期风险，不做数量驱动的机械重写。
3. **随后完成 Phase B 的危险下载最小闭环**：下载记录 URI、同名去重和显式打开/删除策略；“导出公共目录”另立 SAF 任务，不在同一补丁里偷渡。
4. **Phase C 先锁定“无痕弱语义”产品文案和测试**；强 Cookie/网站存储隔离要等独立进程/数据目录方案评审，不用快照竞态冒充实现。
5. **Phase D → E → F 逐阶段推进**：行为和回归网通过后，再抽 `rebindHelpers`、合并规则、拆 delegate；保留单 WebView fallback，直到所有构建变体有明确结论。Phase H 仍视 R-1 矩阵结果决定。

**当前不需要额外拍板的事项**：播放器在设备矩阵完成前保持可关闭/内部试用；B10 孤儿字符串继续避开并行 AI 的共享文件，待其稳定后另行处理；`attrs.xml` 兼容垫片只记录为共享资源风险，不在本浏览器补丁中改动。
