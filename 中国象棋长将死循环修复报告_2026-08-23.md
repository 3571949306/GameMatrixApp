# 中国象棋长将/死循环修复报告

## 问题描述

玩家反馈局面：红方帅被黑方车连续将军，红帅只能不断移动躲避，黑车不断追将，游戏陷入看似无解的循环。

**本质**：黑方在走「长将」（连续将军），按中国象棋规则属于禁着，应判长将方负。原游戏逻辑未实现重复局面/长将检测，导致可以无限循环。

## 修复方案

### 1. 游戏层：重复局面与长将判定

文件：`module-store/feature/games/games/chinesechess/src/main/java/com/gamecenter/app/chinesechess/ChineseChessGame.java`

- 新增 `RepetitionResult` 枚举：
  - `NONE`：未出现重复
  - `DRAW`：三次重复局面，和棋
  - `LOSS_BY_PERPETUAL_CHECK`：三次重复且当前方被将军 → 对方长将判负
- 新增 `positionHistory` 局面指纹历史，每次真实走棋并切换走棋方后调用 `recordPosition()` 记录。
- `checkGameOver()` 在原有「将杀 / 困毙」检测之后，追加重复局面检测：
  - 同一局面出现 3 次且当前方被将军 → 判对方长将负，当前方获胜。
  - 同一局面出现 3 次但无将军 → 和棋。
- `reset()`、`undoLastMoves()`、`deepCopy()` 同步维护局面历史。

### 2. UI 层：记录局面与正确提示

文件：`module-store/feature/games/games/chinesechess/src/main/java/com/gamecenter/app/chinesechess/ChineseChessModuleFragment.java`

- 玩家走棋后：先 `switchSide()`，再 `recordPosition()`，然后 `checkGameOver()`。
- AI 走棋后：同样 `switchSide()` + `recordPosition()`，再判断胜负。
- 胜负提示区分三种结果：
  - 和棋
  - 红方获胜（含「AI 长将判负」提示）
  - AI 获胜
- AI 思考与提示前调用 `ai.setPositionHistory(game.getPositionHistory())`，让 AI 知道当前对局历史。

### 3. AI 层：避免主动走入重复局面

文件：

- `module-store/feature/games/games/chinesechess/src/main/java/com/gamecenter/app/chinesechess/ChineseChessAI.java`
- `app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessAI.java`（按同步说明保持算法一致）

- 新增 `setPositionHistory(List<Long>)` 接口。
- 新增局面指纹计算 `computeBoardHash()`，与 `ChineseChessGame` 的指纹规则保持一致。
- 在根节点候选着法评估时，若某着法导致已经出现过的局面，对 AI 施加 50000 分惩罚（小于将死分、接近一车价值），鼓励 AI 打破循环，而不是消极长将。

## 编译验证

| 任务 | 结果 |
|---|---|
| `javac` 单独编译修改的 `ChineseChessGame.java`、`ChineseChessAI.java` | 通过 |
| `:app:compileDebugJavaWithJavac` | BUILD SUCCESSFUL |
| `:module-store:feature:games:games:chinesechess:compileDebugJavaWithJavac` | BUILD SUCCESSFUL |

## 后续建议

- 连接 A52 真机或启动模拟器执行冒烟测试：`adb install -r` → `am start` → logcat 无 FATAL。
- 注意 release 设备安装 debug 包可能报 `UPDATE_INCOMPATIBLE`，需先 uninstall（会清数据）。
- 可考虑在「提示」或「教程」中增加「长将判负」规则说明，减少玩家困惑。
