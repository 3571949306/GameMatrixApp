# 修改记录 - GameMatrix App


## 文档同步：循环 17-24 全量文档更新（2026-07-06）

### 时间
2026-07-06

### 背景
项目经历循环 17-24 的维护更新（浏览器原生重构、wrongbook 模块预装、错题本全面推进、宿主 Kotlin 迁移、Netty 安全修复）后，全部活动 MD 文档严重过时，需要根据项目当前实际状态进行全量同步更新。

### 修改内容
更新 33 个文档 + 新建 2 个文档 = 共 35 个文件，分 4 批并行执行：

**批次 A - 核心文档（7 个）**：
- README.md / CHANGELOG.md / CODE_WIKI.md
- docs/AI_CONTEXT.md / docs/PROJECT_STATUS.md / docs/DOCUMENTATION_INDEX.md
- docs/game_center_app_ai_roadmap.md
- 更新：版本号 466/500→567，包名 GameMatrix→gamecenter，文件路径 .java→.kt，循环 16→24，补充 wrongbook/tts/hall 模块

**批次 B - 安全规范（4 个）**：
- docs/SECURITY.md：新增安全更新历史章节（7 CVE + Dependabot 状态）
- docs/NETWORK_LAYER.md：新增循环24 Netty 安全修复章节
- docs/DONT_DO_THIS.md：追加 3 条新禁止事项（Kotlin 回退/Netty 降级/告警重开）
- docs/AI_CODING_STANDARDS.md：补充 Kotlin 文件放置规则 + Netty 版本约束 + 错题本预装状态

**批次 C - 模块文档（11 个，含 2 个新建）**：
- 新建：docs/module-docs/错题本模块说明.md
- 新建：docs/modules/模块变更日志/错题本模块变更日志.md
- 更新：游戏/浏览器/工具箱/VPN 模块说明（路径、包名、循环19重构、迁移记录）
- 更新：AI/工具箱/VPN/浏览器/游戏 模块变更日志（迁移版本、完整工具列表）
- 更新：MODULE_STORE_POLICY（错题本分类）

**批次 D - 重构发布（11 个）**：
- 5 个 refactor 文档（路径 Y:\→d:\Developmment\、循环19-24进展、里程碑更新）
- docs/PUBLISH_GUIDE.md + docs/modules/PUBLISH_GUIDE.md（版本示例、CI/CD、预装流程、编码修复）
- CLOUD-BUILD.md（android_ci.yml + dependabot.yml）
- MODULE_DEVELOPMENT_GUIDE.md（Kotlin 示例、实际路径、预装流程）
- MODULE_STORE_REDESIGN_PLAN.md（wrongbook 迁移、实施进度）
- docs/项目改进建议书.md（版本、循环19-24快照、Kotlin化、分发架构）

### 关键更新点
- 版本号：versionCode 466/500 → 567，versionName 1.4.1
- 包名：com.GameMatrix.app → com.gamecenter.app
- 项目路径：Y:\GameMatrixApp / d:/kaifa/GameCenterApp → d:\Developmment\GameMatrixApp
- 文件路径：App.java/MainActivity.java/GameRegistry.java → .kt（kotlin/ 目录）
- 模块列表：补充 hall/game2048/tts/wrongbook，共 9 个动态模块
- 循环记录：16 轮 → 24 轮
- 新增循环 17-24 详细变更记录
- 新增 Netty 安全修复记录（7 CVE，3 high + 4 medium）
- 新增 GitHub Actions CI/CD 和 Dependabot 配置说明
- 新增 wrongbook 模块完整文档（说明 + 变更日志）

### 影响范围
- 仅文档变更，无代码改动
- 无版本号变更（versionCode 保持 567）
- 无 APK 产物变更
- 不影响构建和运行

### 风险评估
- **风险等级**: 无（纯文档更新）
- **影响启动**: 否
- **影响核心功能**: 否
- **影响构建**: 否

### 回滚方法
```powershell
# 回滚全部 35 个文件（含 2 个新建文件需单独删除）
git checkout HEAD -- README.md CHANGELOG.md CODE_WIKI.md CLOUD-BUILD.md
git checkout HEAD -- docs/AI_CONTEXT.md docs/AI_CODING_STANDARDS.md docs/DONT_DO_THIS.md
git checkout HEAD -- docs/DOCUMENTATION_INDEX.md docs/NETWORK_LAYER.md docs/PROJECT_STATUS.md
git checkout HEAD -- docs/PUBLISH_GUIDE.md docs/SECURITY.md docs/game_center_app_ai_roadmap.md
git checkout HEAD -- docs/module-docs/ docs/modules/ docs/refactor/ docs/项目改进建议书.md
git checkout HEAD -- 修改记录.md
# 删除新建文件
Remove-Item docs/module-docs/错题本模块说明.md
Remove-Item docs/modules/模块变更日志/错题本模块变更日志.md
```
或使用 `git revert <commit-hash>` 回滚本次文档同步 commit。

### 实际修改文件列表（35 个）

**批次 A - 核心文档（7 个修改 + 修改记录）**：
- `README.md` / `CHANGELOG.md` / `CODE_WIKI.md`
- `docs/AI_CONTEXT.md` / `docs/PROJECT_STATUS.md` / `docs/DOCUMENTATION_INDEX.md`
- `docs/game_center_app_ai_roadmap.md`

**批次 B - 安全规范（4 个修改）**：
- `docs/SECURITY.md` / `docs/NETWORK_LAYER.md` / `docs/DONT_DO_THIS.md` / `docs/AI_CODING_STANDARDS.md`

**批次 C - 模块文档（9 个修改 + 2 个新建）**：
- 新建：`docs/module-docs/错题本模块说明.md`
- 新建：`docs/modules/模块变更日志/错题本模块变更日志.md`
- 修改：`docs/module-docs/{游戏,浏览器,工具箱,VPN}模块说明.md`
- 修改：`docs/modules/模块变更日志/{AI助手,工具箱,VPN,浏览器,游戏}模块变更日志.md`
- 修改：`docs/modules/MODULE_STORE_POLICY.md`

**批次 D - 重构发布（11 个修改）**：
- `docs/refactor/{MASTER-REFACTOR-PLAN,track-ai,track-vpn,track-tools,track-platform}.md`
- `docs/PUBLISH_GUIDE.md` / `docs/modules/PUBLISH_GUIDE.md`
- `docs/modules/{MODULE_DEVELOPMENT_GUIDE,MODULE_STORE_REDESIGN_PLAN}.md`
- `CLOUD-BUILD.md` / `docs/项目改进建议书.md`

**未更新（归档/第三方/历史）**：
- `docs/archive/*`（历史归档）
- `项目审计_2026-06-19/*`（历史审计）
- `scripts/*/README.md` / `工具/jadx/README.md`（第三方工具）
- `.github/ISSUE_TEMPLATE/*` / `TEST_ISSUES_*`（模板/历史）
- `core/*/README.md`（模块小 README，内容简单无需更新）


## 循环24：修复 GitHub Dependabot 7 个 Netty 安全漏洞

### 时间
2026-07-06

### 背景
GitHub Dependabot 报告 7 个开放的安全警报（3 high + 4 medium），全部来自 Netty 系列库 4.1.134.Final。

### 漏洞清单

| 严重度 | CVE | 模块 | 摘要 |
|---|---|---|---|
| high | CVE-2026-50010 | netty-handler | 信任管理器绕过主机名验证 |
| high | CVE-2026-45416 | netty-handler | SNI 处理器预分配 16MiB 内存 |
| high | CVE-2026-44249 | netty-handler | IPv6 子网过滤器绕过 |
| medium | CVE-2026-50560 | netty-codec-http2 | HTTP/2 Reset 攻击 |
| medium | CVE-2026-50020 | netty-codec-http | HttpObjectDecoder 跳过控制字符 |
| medium | CVE-2026-48043 | netty-codec-http2 | ByteBuf 引用计数泄漏 |
| medium | CVE-2026-47244 | netty-codec-http2 | MAX_CONCURRENT_STREAMS 未强制 |

### 修复方案
根 `build.gradle` 在 `buildscript.classpath` 和 `allprojects.configureEach` 两个 resolutionStrategy 块中 force 了 11 个 netty 子模块到 4.1.134.Final。将全部 22 处 `4.1.134.Final` 替换为 CVE 官方修复版本 `4.1.135.Final`。

### 影响范围
- Netty 是 Gradle 插件 / MediaPipe / Robolectric 的传递依赖，**不进入 APK runtime**（`:app:dependencies` 已确认 runtime classpath 无 netty）
- 仅影响构建期和测试期，不影响发布 APK 的运行时安全
- 修复后 Dependabot 会自动检测到新 commit 并 dismiss 全部 7 个警报

### 风险评估
- **风险等级**: 极低（仅版本号变更，无 API 改动）
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

### 验证结果
- ✅ `:app:assembleDebug` 构建成功
- ✅ `build.gradle` 中无 `4.1.134` 残留
- ✅ APK 预装产物 SHA-256: `4374213CFD671802711D11A5E88B054E0ED124568BE1D68E3487A77BB5632CAD`（8,669,192 bytes）

### 回滚方法
```powershell
git revert <commit-hash>
```
或手动将 `build.gradle` 中 `4.1.135.Final` 全部替换回 `4.1.134.Final`（不推荐，会重新触发 7 个警报）

### 实际修改文件列表
- `build.gradle`（修改：22 处 `4.1.134.Final` → `4.1.135.Final`）
- `修改记录.md`（修改：追加本节）
- `app/src/main/assets/modules/feature_wrongbook_v100.apk`（构建产物更新）

---



## 循环21-23：AI 错题本重构与功能全面化升级

### 时间
2026-07-06

### 背景
按照循环21-23三阶段计划，重构错题本的数据层、UI基础、功能集成与交互体验，彻底解决由于 Material Design styleable 属性冲突导致的动态 APK 崩溃，增强多语言、批量操作、自定义图表统计、分步扫码向导、图片缩放与手势手写等交互。

### 改动内容

#### 1. 数据层升级 (Room Database V2)
- `QuestionEntity.kt`：新增 `isFavorite` (是否收藏)、`sortOrder` (手动排序)、`tags` (用户自定义标签) 字段，升级 Room schema 版本自 1 至 2。
- `WrongBookDatabase.kt`：定义并注册 `MIGRATION_1_2` 字段迁移脚本。
- `WrongBookDao.kt` 与 `WrongBookRepository.kt`：新增支持批量删除、批量修改科目、按掌握度/时间/难度多维排序筛选、模糊全局文本与标签搜索等复杂 SQL 语句及序列化备份方法。

#### 2. UI 基础与样式重构
- 迁移全部硬编码色彩到 `colors.xml` / `values-night/colors.xml`；规范控件间距与圆角参数至 `dimens.xml`。
- 新增 `wrongbook_card_background` (卡片水波纹容器)、`wrongbook_chip_background` (知识点药丸状态背景)、`wrongbook_star_filled`/`wrongbook_star_empty` (向量评分星级)。
- 自定义滑动 Tab 指示器 (配合 ViewPager2 `translationX` 动画) 与 ImageView 自定义圆形 FAB，完全规避宿主 `TabLayout` 和 `FloatingActionButton` 的 `R.styleable` 加载崩溃。
- 设计独立微光 shimmer 骨架占位动画效果 `wrongbook_skeleton_questions` 替换常规 ProgressBar；针对错题列表、看板、复习三个 Tab 设计独立插画、文案与 CTA 唤醒按钮。
- **ClassLoader 资源与类加载修复**：重构了 `ModuleContextHelper` 中 `ModuleContextWrapper` 内的 `getClassLoader()` 接口，使其返回模块的动态 ClassLoader（`ModuleLoader.getClassLoader(MODULE_ID)`），从而根本解决宿主 Context 在加载模块自定义 Canvas 图表 View (`WeeklyTrendChartView` 等) 时的 `ClassNotFoundException` 闪退。


#### 3. 页面功能集成与交互优化
- **搜索界面 (`SearchFragment`)**：全新 DialogFragment 全屏浮层设计，集成模糊输入框、科目快捷筛选、六向排序单选及纯收藏内容切换。
- **科目管理 (`SubjectManagementFragment`)**：支持追加、重命名与科目删除合并。集成了 native `ItemTouchHelper` 手势拖拽排序功能。
- **错题详情 (`QuestionDetailDialogFragment`)**：支持显示完整字段。提供双向“编辑-预览”切换模式，可编辑科目、掌握度 SeekBar、手动星级评分与手写 tags。
- **图片缩放 (`FullscreenImageDialogFragment`)**：点击错题图片即可唤起独立全屏预览，集成 `ScaleGestureDetector` 原生缩放与滚动手势。
- **复习遮挡模式 (`ReviewFragment` & `ReviewAdapter`)**：支持复习卡片默认遮挡解析答案，点击展开时附带平滑淡入动效，显示“跳过”与“完成”按钮，联动今日复习进度百分比。
- **confetti 庆祝特效 (`CelebrationAnimationView`)**：当今日全部复习计划清空时，触发炫丽的 Canvas 彩色碎纸屑粒子重力喷射特效。
- **数据导入导出**：在设置页中添加数据库备份 JSON 导入导出按钮，自动持久化读写于 `filesDir/wrongbook_export/wrongbook_backup.json`。
- **扫码添加向导 (`CaptureDialogFragment`)**：重构扫码逻辑为 5 步 Wizard 分步向导式引导 (选图 → 识别 → 编辑 → 分析 → 保存预览)，大幅优化图像采集和 AI 分析阶段的骨架屏提示。因动态加载模块无法直接加载宿主未注册之 Activity 类（会报 `ActivityNotFoundException`），将其完全迁移重构为 `CaptureDialogFragment`（全屏 Dialog 承载），完美绕过宿主清单注册限制。

### 影响范围
- 健壮性：完全通过自包含 Context 与 native 基础控件规避 `TabLayout` 等 styleable 属性的动态 APK 崩溃痛点。
- 用户交互：全面补齐批量长按选择、智能纠错、拖拽排序、自信复习、动画反馈等 Premium 体验。

### 风险评估
- **风险等级**: 低 (改动完全限制在 `:module-store:feature:tools:wrongbook` 子模块中，已编译并测试通过)。

### 验证结果（2026-07-06 补充）
- ✅ `:module-store:feature:tools:wrongbook:assembleDebug` 构建成功
- ✅ `:app:bundlePreinstalledModules` 已将 `feature_wrongbook_v100.apk` (9,858,750 bytes, SHA-256: C141D354B81605489ECE14A168AD663A36F8C5D38F71159E6F6B41FF8E09A134) 复制到 `assets/modules/`
- ✅ `:app:assembleDebug` 构建成功
- ✅ 模拟器 emulator-5554 安装成功（82MB streamed）
- ✅ 错题本底部导航 Tab 渲染正常，4 个子 Tab（错题/看板/复习/设置）切换无 FATAL
- ✅ CaptureDialogFragment 5 步向导弹出正常（替代原 CaptureActivity，规避动态模块 Activity 启动限制）
- ✅ 设置页 OCR/AI 配置项渲染完整
- ✅ logcat 无 `FATAL EXCEPTION` / `InflateException` / `Resources$NotFoundException` / `ClassNotFoundException` / `ActivityNotFoundException`

### 循环23.1：死代码清理（2026-07-06）
- 清理 `module-store/feature/tools/wrongbook/src/main/AndroidManifest.xml` 中两个不存在的 Activity 声明：
  - `.ui.WrongBookActivity`（类不存在，原 CaptureActivity 已迁移为 CaptureDialogFragment）
  - `.ui.AnalysisActivity`（类不存在，从未实现）
- 清理后重新构建验证通过，APK 预装产物 SHA-256 未变化（清单空 Activity 声明不影响 dex）

### 回滚方法
1. **回滚循环21-23 业务改动**：
   ```powershell
   git revert <commit-hash>
   ```
   注意：会一并回滚错题本数据层 v2 schema、UI 重构、所有新增 DialogFragment 和自定义 View，错题本功能会回到循环20 的初始版本
2. **单步回滚循环23.1 死代码清理**：
   手动恢复 `module-store/feature/tools/wrongbook/src/main/AndroidManifest.xml` 中两个 Activity 声明（不推荐，因类已不存在）
3. **Room schema 回滚**：
   - 卸载 App 清除数据库：`adb shell pm clear com.gamecenter.app`
   - 或修改 `WrongBookDatabase.kt` version 回 1，删除 `MIGRATION_1_2`

### 实际修改文件列表（41 个文件 + 8 个新增文件 = 49 个）
**修改（41 个）**：见 `git diff --stat HEAD` 输出
**新增 Kotlin（8 个）**：
- `app/src/main/kotlin/com/gamecenter/app/App.kt`（替代 App.java）
- `app/src/main/kotlin/com/gamecenter/app/MainActivity.kt`（替代 MainActivity.java）
- `app/src/main/kotlin/com/gamecenter/app/games/GameRegistry.kt`（替代 GameRegistry.java）
- `module-store/feature/tools/wrongbook/.../ui/CaptureDialogFragment.kt`
- `module-store/feature/tools/wrongbook/.../ui/QuestionDetailDialogFragment.kt`
- `module-store/feature/tools/wrongbook/.../ui/SearchFragment.kt`
- `module-store/feature/tools/wrongbook/.../ui/SubjectManagementFragment.kt`
- `module-store/feature/tools/wrongbook/.../ui/FullscreenImageDialogFragment.kt`
- `module-store/feature/tools/wrongbook/.../ui/CelebrationAnimationView.kt`
- `module-store/feature/tools/wrongbook/.../ui/MasteryDistributionView.kt`
- `module-store/feature/tools/wrongbook/.../ui/SubjectPieChartView.kt`
- `module-store/feature/tools/wrongbook/.../ui/WeeklyTrendChartView.kt`

---

## 循环20.1：工作区临时文件清理与循环20 改动提交

### 时间
2026-07-06

### 背景
循环20（AI 错题本模块预装集成）完成后，工作区残留大量调试临时文件，且循环20 的源码改动未提交，存在丢失风险。本次任务清理临时文件并提交循环20 全部改动。

### 改动内容

#### 1. 删除调试临时文件（28 个文件 + 3 个目录）
- 截图 17 个：`screen_*.png`
- Window dump 4 个：`window.xml`、`window_current.xml`、`window_dump.xml`、`window_dump2.xml`
- 日志 1 个：`logcat_wrongbook.txt`
- 压缩包 3 个：`base_installed.zip`、`base_new.zip`、`wrongbook_module.zip`
- 解压目录 3 个：`base_extracted/`、`base_new_extracted/`、`wrongbook_extracted/`

#### 2. 补充 `.gitignore` 规则
在 `.gitignore` 末尾新增「AI 调试临时文件」段落，加入以下模式，避免同类文件再次混入仓库：
```
screen_*.png
window*.xml
logcat_*.txt
*_extracted/
base_installed.zip
base_new.zip
wrongbook_module.zip
```

#### 3. 提交循环20 改动（git commit）
- 新增模块源码：`module-store/feature/tools/wrongbook/`
- 新增预装 APK：`app/src/main/assets/modules/feature_wrongbook_v100.apk`
- 新增导航图标：`app/src/main/res/drawable/ic_nav_wrongbook.xml`
- 新增 AI 规范文档：`AGENTS.md`、`docs/AI_CODING_STANDARDS.md`
- 修改 17 个文件：build.gradle / AndroidManifest / MainActivity / ModuleShellFragment / 底部导航 menu / navigation graph / strings(中/英) / modules.json / ModuleResourceLoader / UpdatePresenter / settings.gradle / version.properties / jacoco.gradle / lint-baseline.xml / PerfMonitorTest / .gitignore / 修改记录.md

### 影响范围
- 仓库清洁度：临时文件全部清除，工作区仅保留循环20 业务改动
- 后续防护：`.gitignore` 新规则会自动拦截同类调试产物
- 业务功能：无功能改动，仅为清理 + 提交

### 风险评估
- **风险等级**: 极低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

### 回滚方法
1. **回滚临时文件删除**：临时文件为调试产物，无需回滚；如需找回可从模拟器重新抓取
2. **回滚 .gitignore 规则**：
   ```powershell
   git revert <commit-hash>
   ```
   或手动删除 `.gitignore` 末尾「AI 调试临时文件」段落
3. **回滚循环20 commit**：
   ```powershell
   git revert <commit-hash>
   ```
   注意：循环20 的错题本模块功能会一并回滚，bottom_nav_menu 第 5 项需同步删除

### 实际修改文件列表
- `.gitignore`（修改：新增 11 行规则）
- `修改记录.md`（修改：追加本节）

---


## 循环20：AI 错题本模块预装集成

**修改时间**: 2026-07-06
**修改目标**: 将 `module-store:feature:tools:wrongbook` 错题本模块接入主应用底部导航，完成模块预装打包、编译修复与 OCR/AI 双模式扩展点确认。
**问题编号**: AI 错题本模块集成

### 修改内容

#### 1. 主应用导航接入
- **MainActivity.java**：
  - 导入 `BuildConfig`。
  - 在 `setupDynamicNavigation()` 中新增错题本底部导航项，受 `BuildConfig.ENABLE_WRONGBOOK` feature flag 与模块安装状态双重控制。
  - 在 `handleNavTabIntent()` 中补充 `"wrongbook"` 分支，支持外部/通知跳转到底部导航错题本页。

#### 2. 模块编译修复
- **module-store/feature/tools/wrongbook/build.gradle**：
  - 将 `appcompat`、`material`、`recyclerview`、`fragment-ktx`、`lifecycle` 等依赖由 `compileOnly` 改为 `implementation`，确保模块资源可独立链接。
  - Kotlin 协程版本升级到 `1.10.1`，与宿主保持一致。
  - Room 升级到 `2.7.1` 并补充 `room-ktx`，修复 KSP 编译期“Coroutine features require room-ktx”错误。
  - KSP Room 编译器同步升级到 `2.7.1`。
  - 扩展 `ksp` 任务对 `:app:packageAppClasses` / `:app:processDebugResources` 的显式依赖，修复 Gradle 8 隐式依赖警告。
- **module-store/feature/tools/wrongbook/src/main/res/values/styles.xml**（新增）：
  - 定义 `Theme.GameMatrixApp`，继承 `Theme.MaterialComponents.DayNight.NoActionBar`，补齐模块 AndroidManifest 中 Activity 引用的主题，并支持浅色/深色切换。

#### 3. 模块注册与预装
- `WrongBookModuleEntryPoint.kt`、`modules.json`、底部导航 `bottom_nav_menu.xml`、导航图 `mobile_navigation.xml`、`ModuleShellFragment.kt` 中的 `wrongbook` 路由均已在前序步骤配置完成，本次验证状态正常。
- `app/build.gradle` 中 `bundlePreinstalledModules` 任务已包含 wrongbook；运行 `:app:assembleDebug` 后成功将 `feature_wrongbook_v100.apk` 复制到 `app/src/main/assets/modules/`。

#### 4. OCR/AI 双模式扩展点
- `OcrEngine` / `OcrService`：已支持本地 ML Kit 与可注册云端引擎（Scnet）双模式，当前默认本地模式。
- `AiAnalysisService`：已支持 `cloud` / `local` 模式切换，云端模式使用 OpenAI 兼容接口；本地模式预留待后续接入端侧模型。
- 用户提出的“是否直接复用现有能力”问题保留：当前以双模式扩展点占位，具体复用策略待后续确认后替换实现。

#### 5. 本地化和主题
- 错题本模块 `values/strings.xml` 与 `values-en/strings.xml` 已对齐。
- 布局文件均使用 `?attr/colorSurface`、`?attr/colorOnSurface` 等主题属性，支持浅色/深色主题。

### 修改文件列表

1. `app/src/main/java/com/gamecenter/app/MainActivity.java` - 添加错题本导航入口与 intent 分支
2. `module-store/feature/tools/wrongbook/build.gradle` - 依赖方式与版本修复
3. `module-store/feature/tools/wrongbook/src/main/res/values/styles.xml` - 新增模块主题
4. `app/src/main/assets/modules/feature_wrongbook_v100.apk` - 预装模块 APK（构建产物）
5. `version.properties` - 构建自动升级版本号（551 → 553）

### 验证结果

- ✅ `:module-store:feature:tools:wrongbook:assembleDebug` 构建成功
- ✅ `:app:assembleDebug` 构建成功
- ✅ `bundlePreinstalledModules` 成功将 wrongbook APK 复制到 `assets/modules/`
- ✅ OCR/AI 双模式扩展点代码已就位
- ✅ 新增字符串已进行中英文本地化
- ✅ 新增 UI 使用主题属性，支持浅色/深色模式

### 回滚方法

```bash
# 回滚代码改动（保留 APK 预装产物，可手动删除）
git checkout HEAD -- \
    app/src/main/java/com/gamecenter/app/MainActivity.java \
    module-store/feature/tools/wrongbook/build.gradle \
    module-store/feature/tools/wrongbook/src/main/res/values/styles.xml

# 如需完全移除预装 APK
git checkout HEAD -- app/src/main/assets/modules/feature_wrongbook_v100.apk

# 回滚版本号（如需）
git checkout HEAD -- version.properties
```

### 影响范围

- 主应用底部导航在 `ENABLE_WRONGBOOK=true` 且模块已安装时显示“错题本”入口。
- Debug APK 体积因预装 wrongbook 模块增加约 8.5 MB（后续可通过资源/依赖精简优化）。
- 不影响现有游戏、浏览器、AI、VPN 等模块功能。

---

## 循环19：Git 提交 - 浏览器模块重构与项目改动归档

**修改时间**: 2026-07-06
**修改目标**: 将工作区中浏览器模块重构、游戏修复、模块优化等大量未提交改动统一归档提交，清理临时文件干扰。
**问题编号**: 项目状态整理

### 修改内容

#### 1. 提交已跟踪文件的修改与删除
- 包含循环 12-18 期间所有已跟踪的代码/资源/文档改动
- 包含旧 Flutter 浏览器模块、HallScreen、RePluginModuleLoader、旧 BrowserTest 等删除项
- 包含 `version.properties` 版本号升级（当前 vc=551）

#### 2. 提交新增文件
- 原生浏览器模块源码：`app/src/main/java/com/gamecenter/app/browser/`
- 原生浏览器资源：drawable/layout/menu/values/values-en 下的浏览器相关文件
- 浏览器预装资源：`app/src/main/assets/browser/`
- GitHub 模板：ISSUE_TEMPLATE、PULL_REQUEST_TEMPLATE.md、CODEOWNERS
- 测试问题记录：`TEST_ISSUES_2026-06-27.md`
- 计划文档：`docs/superpowers/plans/`

#### 3. 排除不进入版本库的文件
- 根目录临时截图：`screen_*.png`
- 根目录 UI dump：`ui_*.xml`
- 待删除清单：`.tmp_files_to_delete.txt`

#### 4. 清理已执行（2026-07-06 补充）
- 删除根目录 46 个临时截图 `screen_*.png`
- 删除根目录 2 个测试截图 `test_screen*.png`
- 删除根目录 32 个 UI dump `ui_*.xml`
- 删除 `.tmp_files_to_delete.txt` 临时清单
- 工作区现已无未跟踪临时文件

### 修改文件列表

详见 `git diff --stat` 输出，主要涉及：
- `app/build.gradle`、`settings.gradle`、`version.properties`
- `app/src/main/java/com/gamecenter/app/browser/`（新增）
- `app/src/main/java/com/gamecenter/app/games/`（多个游戏修复）
- `app/src/main/java/com/gamecenter/app/modules/`（模块下载/加载）
- `app/src/main/res/` 下大量浏览器资源
- `core/network/`、`core/security/`
- `.github/`、`docs/superpowers/plans/`、`TEST_ISSUES_2026-06-27.md`
- `修改记录.md`

### 验证结果

- ✅ `git status` 检查完成
- ✅ 临时文件已排除在提交之外
- ✅ 提交成功

### 回滚方法

```bash
# 回滚本次提交（保留工作区改动）
git reset --soft HEAD~1

# 完全撤销本次提交并丢弃改动
# git reset --hard HEAD~1
```

### 影响范围

- 完整归档 2026-06-19 至 2026-07-06 期间的项目改动
- 不影响未纳入版本控制的临时文件

---

## 循环18：浏览器模块收尾 - 功能闭环与编译修复

**修改时间**: 2026-06-27
**修改目标**: 完成浏览器模块 v3-v8 及完美计划的中低优先级遗留项，修复编译错误，补齐设置页/首页/搜索历史/无痕隔离/文案本地化等功能闭环。
**问题编号**: 浏览器模块全面升级收尾

### 修改内容

#### 1. 修复编译错误
- **strings_browser.xml / values-en/strings_browser.xml**：补充 `browser_settings_load_images`、`browser_settings_auto_play_media`、`browser_settings_home_url`、`browser_settings_search_engine`、`browser_settings_reset`、`browser_tab_icon_desc` 等缺失字符串，并同步添加重置确认/完成文案与搜索历史清空文案。
- **BrowserFragment.java**：补全 `BrowserSecurityPolicy` 导入，修复因外部协议处理引入的编译错误。

#### 2. 设置页功能闭环
- **BrowserSettingsActivity.java**：
  - 绑定 `btn_reset_settings`，弹窗确认后调用 `BrowserSettingsManager.resetToDefaults()` 并刷新 UI。
  - 新增 `btn_clear_search_history` 按钮，调用 `SearchHistoryRepository.deleteAll()` 清空搜索历史。
  - 将 `et_home_url` / `et_search_engine` 提升为成员变量，支持 `onPause` 自动保存输入内容。
- **BrowserSettingsManager.java**：将 `DEFAULT_HOME_URL` 改为 `public`，供 `BrowserFragment` 安全降级使用。

#### 3. 首页与搜索引擎读取设置
- **BrowserFragment.java**：
  - 初始加载与首页按钮统一从 `BrowserSettingsManager.getHomeUrl()` 读取，不再硬编码 `https://www.baidu.com`。
  - 保存搜索历史时读取当前设置的搜索引擎，并增加 `isIncognitoMode` 判断，无痕模式下不保存搜索历史。
  - 收藏按钮增加 600ms 去抖，避免快速双击导致重复插入/删除。

#### 4. 布局调整
- **activity_browser_settings.xml**：在“清空历史记录”按钮后增加“清空搜索历史”按钮。

### 修改文件列表

1. `app/src/main/res/values/strings_browser.xml` - 补充缺失中文文案
2. `app/src/main/res/values-en/strings_browser.xml` - 补充缺失英文文案
3. `app/src/main/res/layout/activity_browser_settings.xml` - 新增清空搜索历史按钮
4. `app/src/main/java/com/gamecenter/app/browser/ui/BrowserFragment.java` - 补导入、设置首页、搜索历史隔离、收藏去抖
5. `app/src/main/java/com/gamecenter/app/browser/ui/BrowserSettingsActivity.java` - 重置按钮、清空搜索历史、onPause 保存输入
6. `app/src/main/java/com/gamecenter/app/browser/core/BrowserSettingsManager.java` - DEFAULT_HOME_URL 公开

### 验证结果

- ✅ Debug 构建成功（BUILD SUCCESSFUL）
- ✅ APK 安装到 Pixel_7_API_35 AVD 成功
- ✅ Monkey 稳定性测试通过（500 事件，无应用崩溃）
- ⚠️ connectedDebugAndroidTest 执行完成但存在既有失败：RePlugin 初始化测试、AI 集成测试、Room 迁移测试、PerfMonitor 测试、GoActivity NPE 等，均与本次浏览器模块改动无关

### 回滚方法

```bash
git checkout HEAD -- \
    app/src/main/res/values/strings_browser.xml \
    app/src/main/res/values-en/strings_browser.xml \
    app/src/main/res/layout/activity_browser_settings.xml \
    app/src/main/java/com/gamecenter/app/browser/ui/BrowserFragment.java \
    app/src/main/java/com/gamecenter/app/browser/ui/BrowserSettingsActivity.java \
    app/src/main/java/com/gamecenter/app/browser/core/BrowserSettingsManager.java
```

### 影响范围

- 浏览器设置页 UI 与行为（新增重置、清空搜索历史、首页/搜索引擎保存时机）
- 浏览器首页加载逻辑（从设置读取首页 URL）
- 搜索历史保存逻辑（按设置搜索引擎、无痕模式隔离）
- 收藏按钮快速点击体验（去抖）

---

## 循环17：浏览器模块收尾 - 数据层统一与遗留项清理

**修改时间**: 2026-06-27
**修改目标**: 完成浏览器模块 v3-v8 及完美计划的遗留项清理，重点统一历史/收藏数据层，确保编译与基础稳定性。
**问题编号**: 浏览器模块全面升级收尾

### 修改内容

#### 1. 废弃旧数据管理器，统一走 Room
- **HistoryManager.java**：从 `SharedPreferences + JSON` 改为 Room 兼容门面（Deprecated），所有读写委托给 `BrowserDatabase.historyDao()`。
  - `saveHistory` 同一 URL 会更新访问次数与最近访问时间。
  - `getAllHistory` / `searchHistory` / `getCount` 从 Room 查询并映射为旧 `HistoryItem`。
  - `deleteHistory(id)` / `deleteByUrl(url)` / `clearAll()` 均操作 Room。
- **BookmarkManager.java**：同样改为 Room 兼容门面（Deprecated），所有读写委托给 `BrowserDatabase.bookmarkDao()`。
  - `addBookmark` 先去重再插入。
  - `getAllBookmarks` / `searchBookmarks` / `isBookmarked` / `getCount` 从 Room 查询。
  - `removeBookmark(id)` / `removeByUrl(url)` / `clearAll()` 均操作 Room。

#### 2. 前序版本计划遗漏项核查
- 核对 `browser_module_next_stage_plan_20260627_v3~v8.md` 及 `browser_module_perfect_plan_20260627.md`。
- 确认以下项已完成或在当前主流程中已生效：
  - 外部 scheme 安全策略（BrowserSecurityPolicy + 弹窗确认）
  - WebView 调试开关（Debug 构建可开启，Release 强制关闭）
  - DownloadActivity 硬编码中文文本本地化
  - FileProvider authority 与 `file_paths.xml` 配置
  - 清除缓存不误删 Room 数据库
  - 文件上传 / 全屏视频 / 网页权限回调接入
  - 历史/收藏/下载 Room 数据层迁移

### 修改文件列表

1. `app/src/main/java/com/gamecenter/app/browser/data/HistoryManager.java` - 改为 Room 兼容门面
2. `app/src/main/java/com/gamecenter/app/browser/data/BookmarkManager.java` - 改为 Room 兼容门面
3. `修改记录.md` - 本记录

### 验证结果

- ✅ Debug 构建成功（BUILD SUCCESSFUL），版本号 547 → 548
- ✅ APK 安装到 Pixel_7_API_35 AVD 成功
- ✅ Monkey 稳定性测试通过（500 事件，无应用崩溃，应用进程存活）
- ⚠️ 单元测试：`308 tests completed, 1 failed, 1 skipped`
  - 失败项：`PerfMonitorTest.testCompleteMonitoringFlow`（冷启动时间断言失败），与本次浏览器改动无关，系既有模块的独立问题
- ❌ connectedDebugAndroidTest 未执行：项目 `build-logic/jacoco.gradle` 配置异常（`No such property: buildTypes for class: java.lang.String`），与本次改动无关

### 回滚方法

```bash
git checkout HEAD -- \
    app/src/main/java/com/gamecenter/app/browser/data/HistoryManager.java \
    app/src/main/java/com/gamecenter/app/browser/data/BookmarkManager.java
```

### 影响范围

- 浏览器模块历史/收藏数据层（仅旧门面 API 行为变更，实际数据已统一至 Room）
- 若仍有代码通过 `HistoryManager` / `BookmarkManager` 访问数据，现会自动落到 Room，不再读写 `browser_history` / `browser_bookmarks` SharedPreferences

---

## 循环16：模块商店语言与UI更新修复

**修改时间**: 2026-06-25
**修改目标**: 解决下载模块后UI不刷新、出现重复游戏、语言变英文以及多项环境污染漏洞。
**问题编号**: 模块化架构遗留Bug修复

### 修改内容

#### 1. 语言与重复条目修复
- **JSON解析翻译拦截 (ModuleManifest.kt)**：在 `fromJson` 时，匹配已知 ID（如 games_hall, browser, tools, ai, checkers 等），动态翻译并覆盖为中文名称与介绍，彻底解决下载远端英文 `modules.json` 导致的界面语言污染。
- **游戏注册查重 (GameRegistry.java)**：修改 `getCategories` 逻辑。动态加载时如果该游戏 ID 已存在于内置的静态游戏列表中，则自动丢弃。确保界面上只展示静态中文条目，解决跳棋、骰子等游戏重复出现的问题。

#### 2. UI与刷新问题修复
- **列表无响应修复 (ModuleAdapter.kt)**：修复 `updateInstalledIds` 时缺少 `notifyDataSetChanged()` 导致的列表无法刷新的 Bug，现在下载完成后会正确通知 Adapter 更新。
- **Activity恢复不刷新 (MainActivity.java)**：增加 `onResume` 时的 `setupDynamicNavigation` 调用，保证在其他 Activity（如 ModuleStoreActivity）下载安装新模块并返回时，底部导航栏和主界面能够实时重建并显示新模块。
- **下载回调脱节修复 (ModuleStoreActivity.kt)**：将注册回调从 `onCreate` 移至 `onResume` 或在下载后主动重建已安装集合，确保状态变更及时反射到 UI。

#### 3. 底层漏洞修复
- **配置污染修复 (ModuleResourceLoader.java)**：修改创建模块资源的逻辑，使用 `new Configuration(mainResources.getConfiguration())` 隔离配置更改，防止下载模块拉起时污染宿主的全局 Language 配置导致整个 App 变成英文。
- **覆盖安装校验冲突 (ModuleDownloader.kt)**：重写安全文件名机制，通过增加时间戳后缀解决同版本覆盖下载时的文件锁与 SHA-256 校验错误问题，彻底规避因本地测试重复覆盖导致的崩溃和签名校验失败。


#### 4. 预装模块逻辑与占位符清理
- **安全检查兼容性修复 (ModuleDownloader.kt)**：修复了由于路径穿越安全校验写死 `.apk` 后缀，导致无法识别提取出的预装模块（如 `feature_browser_v100.apk`）的 Bug。现在 `getModuleFile` 安全地提取 `manifest.fileName` 进行匹配，成功激活预装的本地模块。
- **构建冗余模块剔除 (app/build.gradle)**：修复了 `bundlePreinstalledModules` 任务中错误地将 `tools`、`ai` 和 `tts` 模块打包进初始安装包的问题，现在只保留了 `browser` 模块作为初始预装，彻底解决了首次进入 App 时底部导航栏出现“尚未安装”占位符的问题，确保用户按需去模块商店下载。


## 循环15：设置功能重做 + 5个游戏音效优化

**修改时间**: 2026-06-23
**修改目标**: 重做APP设置功能（新增声音/音效/振动开关），优化5个游戏（中国象棋/2048/俄罗斯方块/贪吃蛇/斗地主）+ 五子棋音效开关持久化
**问题编号**: 设置重做+游戏音效优化

### 修改内容

#### 1. 设置功能重做

**SettingsManager 新增方法**：
- `isSfxEnabled()` / `setSfxEnabled(boolean)` - 音效总开关（控制所有音频）
- `shouldPlayGameSound()` - 综合判断是否播放游戏音效（音效总开关 && 游戏音效开关）
- `shouldVibrate()` - 综合判断是否执行振动（音效总开关 && 振动开关）

**设置布局重做**（dialog_settings_modern.xml）：
- 新增"声音与反馈"分组卡片
- 音效总开关（MaterialSwitch，关闭时联动禁用游戏音效和振动开关）
- 游戏音效开关（MaterialSwitch）
- 振动反馈开关（MaterialSwitch）
- 所有硬编码中文文本迁移到 strings.xml

**AppSettingsDialog 重写**：
- 新增 `updateAudioSwitchStates()` 方法实现开关联动
- 主题/语言选择器字符串资源化
- 所有硬编码中文迁移到 strings.xml

#### 2. 游戏音效优化（6个游戏）

| 游戏 | 优化内容 |
|------|---------|
| 五子棋 | 音效/振动开关接入 SettingsManager 持久化 |
| 斗地主 | 音效开关接入 SettingsManager 持久化 |
| 中国象棋 | 新增落子/将军/胜利/失败音效（复用 R.raw.ui_turn） |
| 2048 | 新增滑动/合并音效（复用 R.raw.ui_turn） |
| 俄罗斯方块 | 新增旋转/落地/消行音效（复用 R.raw.ui_turn） |
| 贪吃蛇 | 新增吃食物/死亡音效（复用 R.raw.ui_turn） |

### 修改文件列表

**设置功能**：
1. `core/common/src/main/java/com/gamecenter/app/SettingsManager.java` - 新增音效总开关方法
2. `app/src/main/res/layout/dialog_settings_modern.xml` - 重做布局
3. `app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java` - 重写开关逻辑
4. `app/src/main/res/values/strings.xml` - 新增20条设置字符串
5. `app/src/main/res/values-en/strings.xml` - 新增20条英文翻译

**游戏音效**：
6. `app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java` - 音效开关持久化
7. `app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java` - 音效开关持久化
8. `app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessActivity.java` - 新增音效
9. `app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessView.java` - 新增音效回调
10. `app/src/main/java/com/gamecenter/app/games/game2048/Game2048Activity.java` - 新增音效
11. `app/src/main/java/com/gamecenter/app/games/game2048/Game2048View.java` - 新增音效播放
12. `app/src/main/java/com/gamecenter/app/games/tetris/TetrisActivity.java` - 新增音效
13. `app/src/main/java/com/gamecenter/app/games/tetris/TetrisView.java` - 新增音效回调
14. `app/src/main/java/com/gamecenter/app/games/snake/SnakeActivity.java` - 新增音效

**文档**：
15. `修改记录.md` - 本记录
16. `docs/modules/模块变更日志/游戏模块变更日志.md` - 各游戏变更记录

### 验证结果

- ✅ Debug构建成功（BUILD SUCCESSFUL）
- ✅ 主应用安装到蓝叠模拟器无崩溃
- ✅ 应用启动正常
- ✅ Monkey稳定性测试通过（500事件，无应用崩溃，应用存活）
- ⚠️ Monkey测试在事件779时触发蓝叠系统级崩溃（`android.speech.action.WEB_SEARCH` 无Activity处理），非应用问题

### 回滚方法

```bash
git checkout HEAD -- \
    core/common/src/main/java/com/gamecenter/app/SettingsManager.java \
    app/src/main/res/layout/dialog_settings_modern.xml \
    app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java \
    app/src/main/res/values/strings.xml \
    app/src/main/res/values-en/strings.xml \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java \
    app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java \
    app/src/main/java/com/gamecenter/app/games/chinesechess/ \
    app/src/main/java/com/gamecenter/app/games/game2048/ \
    app/src/main/java/com/gamecenter/app/games/tetris/ \
    app/src/main/java/com/gamecenter/app/games/snake/SnakeActivity.java
```

### 影响范围

- APP设置功能（所有用户）
- 6个游戏的音效播放（五子棋/斗地主/中国象棋/2048/俄罗斯方块/贪吃蛇）

### 风险评估

- **风险等级**: 中
- **影响启动**: 否
- **影响核心功能**: 是（设置存储和游戏音效）
- **替代方案**: 无

---

## 贪吃蛇游戏音效功能

**修改时间**: 2026-06-23
**修改目标**: 为贪吃蛇游戏添加吃食物音效和游戏结束（撞墙/死亡）音效
**问题编号**: 贪吃蛇音效

### 修改文件列表

1. **app/src/main/java/com/gamecenter/app/games/snake/SnakeActivity.java**
   - 新增 import：`android.media.SoundPool`、`com.gamecenter.app.R`、`com.gamecenter.app.SettingsManager`
   - 新增成员变量：`SoundPool soundPool`、`int eatSoundId`、`int deathSoundId`
   - `initGame()` 末尾调用 `initSounds()` 初始化 SoundPool 并加载 `R.raw.ui_turn`（吃食物音与死亡音复用同一资源，不新增资源文件）
   - 新增 `initSounds()`：构建 SoundPool（maxStreams=2），加载 `R.raw.ui_turn` 到 eatSoundId 和 deathSoundId，加载失败静默处理
   - 新增 `playEatSound()`：先检查 `SettingsManager.getInstance(this).shouldPlayGameSound()`，再播放（音量 0.6，原速）
   - 新增 `playDeathSound()`：先检查 `SettingsManager.getInstance(this).shouldPlayGameSound()`，再播放（音量 0.8，速率 0.6 以区分吃食物音）
   - 在已有的 `setOnFoodEatenListener` 回调中调用 `playEatSound()`
   - 在已有的 `setOnGameOverListener` 回调中调用 `playDeathSound()`
   - 重写 `onDestroy()`：释放 SoundPool 资源（`soundPool.release()` 并置 null）

### 设计说明

- **不修改 SnakeView.java**：SnakeView 已定义 `OnFoodEatenListener` 和 `OnGameOverListener` 回调接口，并在 `tick()`（吃食物）和 `onGameOver()`（游戏结束）中正确触发。SoundPool 由 SnakeActivity 持有，在监听器回调实现中播放音效，与同项目 GomokuActivity 模式一致，不改动任何游戏逻辑。
- **音效资源复用**：复用现有 `R.raw.ui_turn`（`app/src/main/res/raw/ui_turn.wav`），不新增资源文件。吃食物音与死亡音通过不同音量/速率区分。
- **设置开关**：播放前检查 `SettingsManager.shouldPlayGameSound()`（综合音效总开关与游戏音效开关），尊重用户偏好。
- **异常安全**：SoundPool 构建失败、播放异常均静默处理，不影响游戏运行。

### 回滚方法

本次仅修改一个文件，回滚命令：
```bash
git checkout -- app/src/main/java/com/gamecenter/app/games/snake/SnakeActivity.java
```

### 验证结果

- `SnakeActivity.java` 编译通过（原先的 `R.raw.ui_turn` 引用错误已通过添加 `import com.gamecenter.app.R;` 修复）
- 注：工作区中 `ChineseChessView.java:119` 存在预先存在的编译错误（`moveSoundListener` 变量未声明），与本次任务无关，未做修改

---

## 循环14：五子棋主页优化 + 闪退修复 + Monkey稳定性测试

**修改时间**: 2026-06-23
**修改目标**: 五子棋主页排版/元素优化（方案B浅色木纹卡片），修复主页闪退和AI下子崩溃
**问题编号**: 五子棋UI优化+崩溃修复

### 问题描述

1. **主页闪退**：布局中 `difficulty_panel` 从 `LinearLayout` 改为 `ScrollView`（支持内容滚动），但 `GomokuActivity` 中仍用 `LinearLayout` 类型转换，导致 `ClassCastException` 闪退
2. **AI下子崩溃**：`GomokuAI.getCandidateMoves` 中 `HashSet<Long>.add(key)` 触发 NPE（`HashMap.hash` key为null），AI计算线程崩溃
3. **测试APK导致蓝叠崩溃**：`BootstrapActivity` 缺少 `action MAIN`，蓝叠找不到主入口activity；修复后蓝叠仍在 `hcallOnAppInstalledClbk` 回调中崩溃（蓝叠版本bug，无法通过APK修复）

### 修改文件列表

1. **app/src/main/res/layout/activity_gomoku.xml**
   - 重写主页布局：浅米色渐变背景 + 中央木纹卡片
   - 标题区：大标题"五子棋" + 副标题"人机对战·经典15×15"
   - 难度区：2×2宫格，每个按钮带星级图标+难度名+描述
   - 先手区：黑白棋子图标+文字卡片式选择器
   - 开始按钮：match_parent宽、深棕渐变
   - 教程按钮：移到卡片外底部
   - ScrollView包裹难度面板（支持小屏幕滚动）

2. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java**
   - `difficultyPanel` 类型从 `LinearLayout` 改为 `ScrollView`（修复 ClassCastException）
   - `selectDifficulty` 方法新增难度按钮 `setSelected` 状态更新
   - `onCreate`/`startGame`/`handleRestart`/`restoreGameState` 中添加棋盘 `setVisibility` 控制

3. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuAI.java**
   - `getCandidateMoves` 方法：`Set<Long> seen = new HashSet<>()` 改为 `boolean[][] seen`（修复 NPE）
   - 移除不再使用的 `import java.util.HashSet` 和 `import java.util.Set`

4. **app/src/main/res/values/colors.xml**
   - 新增17条五子棋主页配色（浅色主题）

5. **app/src/main/res/values-night/colors.xml**
   - 新增17条五子棋主页配色（深色主题）

6. **app/src/main/res/values/strings.xml**
   - 新增6条字符串（标题/副标题/难度描述）

7. **app/src/main/res/values-en/strings.xml**
   - 新增6条英文翻译

8. **app/src/androidTest/AndroidManifest.xml**
   - BootstrapActivity 添加 `action MAIN` + `category LAUNCHER` intent-filter

**新增 drawable 文件（11个）**：
- `gomoku_main_bg.xml` - 浅米色渐变背景
- `gomoku_main_card_bg.xml` - 木纹渐变卡片
- `gomoku_difficulty_selector.xml` - 难度按钮selector
- `gomoku_color_selector.xml` - 先手选择selector
- `gomoku_start_btn_bg.xml` - 开始按钮渐变
- `gomoku_piece_black.xml` - 黑棋子图标
- `gomoku_piece_white.xml` - 白棋子图标
- `gomoku_stars_1.xml` ~ `gomoku_stars_4.xml` - 难度星级图标

### 验证结果

- ✅ Debug构建成功
- ✅ 主应用安装到蓝叠模拟器无崩溃
- ✅ 五子棋主页正常显示（标题/难度/先手/开始按钮齐全）
- ✅ 点击"开始游戏"进入棋盘无闪退
- ✅ 点击棋盘下子后AI计算无崩溃
- ✅ Monkey稳定性测试通过（500个随机触摸事件，无崩溃，应用存活）
- ⚠️ 测试APK（app-debug-androidTest.apk）安装仍导致蓝叠崩溃（蓝叠版本bug，非应用问题）

### 自动化测试说明

由于蓝叠模拟器在安装测试APK时触发 `0xC0000409 STATUS_STACK_BUFFER_OVERRUN` 崩溃（蓝叠 `hcallOnAppInstalledClbk` 回调bug），无法运行 `connectedDebugAndroidTest` 完整测试套件。改用 Monkey 稳定性测试替代：
- **测试命令**: `adb shell monkey -p com.gamecenter.app --pct-touch 80 -v -s 42 500`
- **测试结果**: 500个事件全部注入，无崩溃，应用存活（PID 4546）
- **覆盖范围**: 随机触摸事件覆盖应用各区域

### 回滚方法

```bash
git checkout HEAD -- \
    app/src/main/res/layout/activity_gomoku.xml \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuAI.java \
    app/src/main/res/values/colors.xml \
    app/src/main/res/values-night/colors.xml \
    app/src/main/res/values/strings.xml \
    app/src/main/res/values-en/strings.xml \
    app/src/androidTest/AndroidManifest.xml
Remove-Item app/src/main/res/drawable/gomoku_main_bg.xml, app/src/main/res/drawable/gomoku_main_card_bg.xml, app/src/main/res/drawable/gomoku_difficulty_selector.xml, app/src/main/res/drawable/gomoku_color_selector.xml, app/src/main/res/drawable/gomoku_start_btn_bg.xml, app/src/main/res/drawable/gomoku_piece_black.xml, app/src/main/res/drawable/gomoku_piece_white.xml, app/src/main/res/drawable/gomoku_stars_1.xml, app/src/main/res/drawable/gomoku_stars_2.xml, app/src/main/res/drawable/gomoku_stars_3.xml, app/src/main/res/drawable/gomoku_stars_4.xml
```

### 影响范围

- 五子棋游戏模块（UI + AI + Activity）
- 测试APK manifest

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 是（五子棋游戏流程和AI计算）
- **替代方案**: 无

---

## 循环13：五子棋游戏全面优化（界面/AI/功能体验）

**修改时间**: 2026-06-23
**修改目标**: 优化五子棋游戏的界面、人机逻辑和功能体验，修复未选难度即可下子的bug
**问题编号**: 五子棋优化

### 问题描述

1. **Bug**：进入游戏后未选择难度、未点"开始游戏"就能在棋盘上下子（难度选择面板与棋盘交互未隔离）
2. **界面**：无坐标标识、无胜利五连高亮、无AI思考指示、无落子动画、硬编码中文文本违反本地化规则
3. **AI**：低难度对新手偏强、无随机性、不识别"跳活三"/"跳冲四"、开局固定天元、防守偏置固定
4. **体验**：玩家固定黑方不能选白、无对局计时、无音效/震动、悔棋只能1手、无认输、返回键直接重开无确认、旋转屏幕丢局

### 修改文件列表

#### 1. Bug修复：未选难度可下子
1. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuView.java**
   - 新增 `interactive` 字段和 `setInteractive(boolean)` 方法
   - `onTouchEvent` 开头检查 `if (!interactive) return true`，未开始游戏时不响应触摸
   - `drawPieces` 中悬停预览也受 `interactive` 控制

2. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java**
   - `onCreate` 中 `gomokuView.setInteractive(false)` 初始禁止交互
   - `startGame` 中 `gomokuView.setInteractive(true)` 开启交互
   - `handleRestart` 中 `gomokuView.setInteractive(false)` 恢复禁止

#### 2. 界面优化（GomokuView）
3. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuView.java**（重写）
   - 新增 `drawCoordinates()`：棋盘四周绘制坐标标识（列 A-P 跳过 I，行 1-15）
   - 新增 `drawWinningLine()`：游戏结束时绘制红色粗线贯穿胜利五子
   - 新增 `setAiThinking(boolean)` 和 `aiThinking` 字段：AI思考时顶部显示"AI思考中…"琥珀色指示
   - 新增 `animateLastMove()` 和 `pieceAnimator`：落子缩放渐显动画（OvershootInterpolator 回弹效果，180ms）
   - 颜色全部从资源读取（`R.color.gomoku_bg` 等），支持浅色/深色主题

#### 3. AI逻辑优化（GomokuAI，子代理完成）
4. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuAI.java**
   - 新增 `maybePickRandomFromTop()`：低难度（1档30%、2档15%）从评分前3着法随机选，新手友好
   - 新增 `evaluateGapPatterns()`：识别"跳活三"（评分25000）和"跳冲四"（评分120000）
   - `getCandidateMoves` 棋盘为空时从天元及周围7个位置随机选，避免每局雷同
   - 新增 `defenseBias` 字段按难度递减（1→1.40, 2→1.25, 3→1.18, 4→1.12），替换硬编码
   - 新增 `findVcfMove()`/`vcfSearch()`/`generateFourMoves()`：大师难度VCF浅层算杀（深度6层，超时40%）

#### 4. 功能体验优化（GomokuActivity 重写）
5. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java**（重写）
   - 新增先手选择：`setupColorSelectionButtons()` + `selectPlayerColor()`，可选执黑或执白
   - 新增对局计时：`gameStartElapsedMs` + `timerRunnable`，顶部显示 MM:SS
   - 新增落子音效：`SoundPool` 加载 `R.raw.ui_turn`（复用现有资源）
   - 新增震动反馈：`Vibrator`/`VibratorManager`，落子时20ms轻震动
   - 新增认输功能：`handleResign()` 弹确认对话框，记录AI获胜
   - 返回键改为确认对话框：`OnBackPressedCallback`，游戏中弹"退出/重开/继续"三选
   - 新增状态保存：`onSaveInstanceState`/`restoreGameState`，旋转屏幕不丢局（保存棋盘/历史/难度/先手）
   - `triggerAiMove` 增加 `aiGeneration` 版本号，重开时取消旧AI任务

#### 5. GomokuGame 扩展
6. **app/src/main/java/com/gamecenter/app/games/gomoku/GomokuGame.java**
   - 新增 `winningLine` 字段（`int[4]`: x1,y1,x2,y2）和 `getWinningLine()`
   - `checkWinAt` 记录胜利五连线两端坐标
   - `reset` 清空 `winningLine`

#### 6. 布局优化
7. **app/src/main/res/layout/activity_gomoku.xml**（重写）
   - 新增 `tv_timer` 对局计时显示
   - 新增 `btn_color_black`/`btn_color_white` 先手选择按钮
   - 新增 `btn_resign` 认输按钮
   - 新增 `btn_tutorial_ingame` 游戏中教程按钮
   - 移除原"联机对战"按钮（避免误导，联机已在模块商店）
   - 所有文本改用 `@string/` 资源引用

#### 7. 本地化
8. **app/src/main/res/values/strings.xml**
   - 新增 32 条五子棋专用字符串（难度/先手/认输/退出/回合/胜负/计时等）

9. **app/src/main/res/values-en/strings.xml**
   - 新增对应 32 条英文翻译

#### 8. Feature Flag
10. **app/build.gradle**
    - 新增 `buildConfigField "boolean", "GOMOKU_ENHANCED", "true"` 开关

### 技术说明

- **interactive 开关**：GomokuView 新增 `interactive` 字段，`onTouchEvent` 开头检查，未开始游戏时直接消费事件不响应，从根本上隔离难度选择面板与棋盘交互
- **胜利线记录**：GomokuGame.checkWinAt 在检测到五连时记录两端坐标，GomokuView.drawWinningLine 读取绘制
- **落子动画**：ValueAnimator + OvershootInterpolator，棋子从0缩放到1带轻微回弹
- **AI思考指示**：GomokuView.setAiThinking 控制顶部信息栏颜色和文本（琥珀色"AI思考中…"）
- **VCF算杀**：仅考虑冲四着法的递归搜索，深度6层，超时控制在总时间40%内
- **状态保存**：onSaveInstanceState 保存棋盘一维数组+历史记录+难度+先手+游戏开始标志，restoreGameState 用反射恢复私有字段
- **音效复用**：复用现有 `ui_turn.wav` 避免新增资源文件

### 验证结果

- ✅ Debug 构建成功 (`.\gradlew.bat :app:assembleDebug -PleakCanary=false`)
- ✅ 无编译错误
- ✅ Bug修复：未选难度时棋盘不响应触摸
- ✅ 界面：坐标/胜利线/AI思考指示/落子动画全部实现
- ✅ AI：5项优化全部完成（随机走子/跳棋型/开局变化/防守偏置/VCF）
- ✅ 功能：先手选择/计时/音效震动/认输/状态保存/返回键确认全部实现
- ✅ 本地化：中英文32条字符串全部添加
- ✅ Feature Flag：GOMOKU_ENHANCED 开关已添加

### 回滚方法

```bash
git checkout HEAD -- \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuView.java \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuAI.java \
    app/src/main/java/com/gamecenter/app/games/gomoku/GomokuGame.java \
    app/src/main/res/layout/activity_gomoku.xml \
    app/src/main/res/values/strings.xml \
    app/src/main/res/values-en/strings.xml \
    app/build.gradle
```

### 影响范围

- 五子棋游戏模块（gomoku 包下4个核心文件）
- 布局资源（activity_gomoku.xml）
- 字符串资源（中英文）
- 构建配置（build.gradle feature flag）

### 风险评估

- **风险等级**: 中（涉及核心游戏逻辑和AI算法）
- **影响启动**: 否
- **影响核心功能**: 是（五子棋游戏流程和AI变更）
- **缓解措施**: 通过 `GOMOKU_ENHANCED` feature flag 可一键关闭

---

## 循环12：Android 11-17 全版本适配性修复（子代理批量执行 + 16KB 对齐）

**修改时间**: 2026-06-23
**修改目标**: 彻查并修复 app 对 Android 11-17 的全部适配性问题，确保各版本稳定运行；通过 9 个并行子代理任务批量完成所有 P2/P3 修复
**问题编号**: Android 11-17 适配性彻查

### 问题描述

针对 Android 11 (API 30) 到 Android 17 (API 36) 全版本进行适配性彻查，共识别 11 个适配问题，其中 9 个通过子代理并行修复，2 个在主线程修复。主要问题类型：
- Android 12+ (API 31) SplashScreen 双启动屏闪烁
- Android 13+ (API 33) POST_NOTIFICATIONS 运行时权限
- Android 14+ (API 34) registerReceiver 必须显式 export 标志、前台服务类型、READ_MEDIA_VISUAL_USER_SELECTED 部分照片权限、FileProvider URI、WebView SSL 错误处理
- Android 15+ (API 35) Edge-to-Edge 强制显示、16KB 页面对齐
- Android 16+ (API 36) screenOrientation 被忽略
- 私有 API 反射调用风险

### 修改文件列表（按子代理任务分组）

#### 任务1：SplashScreen API 适配（Android 12+ 双启动屏修复）
1. **app/build.gradle** - 添加 `androidx.core:core-splashscreen:1.0.1` 依赖
2. **app/src/main/res/values/themes.xml** - `Theme.GameMatrixApp.Splash` parent 改为 `Theme.SplashScreen`，添加 `windowSplashScreenBackground`、`windowSplashScreenAnimatedIcon`、`postSplashScreenTheme`
3. **app/src/main/res/values-night/themes.xml** - 新增深色主题 `Theme.GameMatrixApp.Splash`
4. **app/src/main/java/com/gamecenter/app/SplashActivity.java** - 新增 `SplashScreen.installSplashScreen(this)` 调用；修复预存编译错误 `playEnterAnimations()` → `playEnterAnimation()`

#### 任务2：屏幕方向统一适配（Android 16+ 忽略 manifest screenOrientation）
5. **app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java** - onCreate 中添加 `setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_PORTRAIT)` 统一覆盖 25 个竖屏游戏
6. **app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java** - 添加 `setRequestedOrientation(SCREEN_ORIENTATION_LANDSCAPE)`（横屏）

#### 任务3：SystemInfoCollector 安全适配
7. **app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java** - `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志；`getSystemProperty` 反射添加风险注释和日志；`getWidevineLevel` 添加 `Build.VERSION.SDK_INT < Build.VERSION_CODES.P` 版本判断

#### 任务4：ModuleResourceLoader 私有 API 风险注释
8. **app/src/main/kotlin/com/gamecenter/app/modular/ModuleResourceLoader.kt** - 添加 `@Suppress("DiscouragedPrivateApi")`，完善 try-catch 错误处理，添加私有 API 风险注释

#### 任务5：VpnFragment 前台服务适配（Android 14+）
9. **module-store/feature/tools/vpn/src/main/java/com/gamecenter/app/vpn/VpnFragment.kt** - `startService` 改为 `ContextCompat.startForegroundService`，添加 try-catch 处理 IllegalStateException

#### 任务6：Android 14 部分照片访问权限适配
10. **app/src/main/AndroidManifest.xml** - 新增 `READ_MEDIA_VISUAL_USER_SELECTED` 权限声明
11. **app/src/main/java/com/gamecenter/app/PermissionHelper.java** - 新增 Android 14+ (API 34+) 请求 `READ_MEDIA_VISUAL_USER_SELECTED` 权限逻辑

#### 任务7：测试 APK manifest 修复（蓝叠崩溃）
12. **app/src/androidTest/AndroidManifest.xml** (新增) - 声明占位 Activity `BootstrapActivity`，使用 `tools:replace="android:exported"` 解决合并冲突

#### 任务8：UpdateInstaller FileProvider URI 适配（Android 7+）
13. **core/update/src/main/java/com/gamecenter/app/update/UpdateInstaller.java** - `Uri.fromFile()` 替换为 `FileProvider.getUriForFile()`，添加 `FLAG_GRANT_READ_URI_PERMISSION`

#### 任务9：BrowserFragment SSL 错误处理适配
14. **module-store/feature/tools/browser/src/main/java/com/gamecenter/app/fragments/BrowserFragment.java** - `onReceivedSslError` 中 `handler.proceed()` 改为 `handler.cancel()`，添加 Toast 提示和日志

#### 主线程修复：registerReceiver export 标志（Android 14+）
15. **module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/BatteryToolBinder.java** - `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志
16. **module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/AdvancedToolBinders.java** - `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志

#### 主线程修复：VpnService 前台服务（Android 14+）
17. **module-store/feature/tools/vpn/src/main/java/com/gamecenter/app/vpn/service/VpnServiceProxy.kt** - 添加 `startForegroundIfNeeded()` 方法，创建通知渠道和前台通知，在 `onStartCommand` 开头调用

#### 主线程修复：Android 15+ Edge-to-Edge
18. **app/src/main/java/com/gamecenter/app/MainActivity.java** - 添加 `import androidx.activity.EdgeToEdge;`，在 onCreate 中 `super.onCreate()` 后添加 `EdgeToEdge.enable(this);`

#### 主线程修复：Android 15+ 16KB 页面对齐
19. **app/build.gradle** - 添加 `packaging { jniLibs { useLegacyPackaging = false } }` 配置，确保 .so 在 APK 中 16KB 对齐

### 技术说明

- **SplashScreen API**: 使用 AndroidX SplashScreen API 统一处理 Android 12+ 系统启动屏与应用启动屏的衔接，消除双启动屏闪烁
- **屏幕方向**: Android 16+ 忽略 manifest 中的 `screenOrientation`，需通过 `setRequestedOrientation()` 在运行时设置
- **registerReceiver export 标志**: Android 14+ (API 34) 要求显式指定 `RECEIVER_EXPORTED`/`RECEIVER_NOT_EXPORTED`，否则抛出 SecurityException
- **前台服务**: Android 14+ 要求前台服务调用 `startForeground`，VpnService 是系统特殊服务（ServiceInfo 无 FOREGROUND_SERVICE_TYPE_VPN 常量），直接调用 `startForeground(NOTIFICATION_ID, notif)` 不传类型
- **POST_NOTIFICATIONS**: Android 13+ (API 33) 运行时权限，未授权则请求
- **READ_MEDIA_VISUAL_USER_SELECTED**: Android 14+ (API 34) 部分照片访问权限，与 `READ_MEDIA_IMAGES` 一起请求
- **Edge-to-Edge**: Android 15+ (API 35) 强制显示，使用 `EdgeToEdge.enable()` 适配
- **16KB 页面对齐**: AGP 8.5+ 默认启用 16KB 对齐打包，`useLegacyPackaging=false` 确保 .so 在 APK 中 16KB 对齐（注意：MediaPipe `libllm_inference_engine_jni.so` 本身未 16KB 对齐，需等待上游库更新）
- **FileProvider**: Android 7+ (API 24) 禁止 `file://` URI，需使用 FileProvider 并添加 `FLAG_GRANT_READ_URI_PERMISSION`
- **WebView SSL**: `handler.proceed()` 绕过 SSL 错误存在安全风险，改为 `handler.cancel()` 并提示用户

### 验证结果

- ✅ Debug 构建成功 (`.\gradlew.bat :app:assembleDebug -PleakCanary=false`)
- ✅ 无编译错误
- ✅ 版本号自动递增: 477 → 478 → 479
- ✅ 16KB 对齐配置编译通过
- ✅ 主应用已在蓝叠模拟器上稳定运行（安装+启动无崩溃）

### 回滚方法

```bash
# 全部回滚
git checkout HEAD -- app/build.gradle \
    app/src/main/AndroidManifest.xml \
    app/src/main/res/values/themes.xml \
    app/src/main/res/values-night/themes.xml \
    app/src/main/java/com/gamecenter/app/SplashActivity.java \
    app/src/main/java/com/gamecenter/app/MainActivity.java \
    app/src/main/java/com/gamecenter/app/PermissionHelper.java \
    app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java \
    app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java \
    app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java \
    app/src/main/kotlin/com/gamecenter/app/modular/ModuleResourceLoader.kt \
    app/src/androidTest/AndroidManifest.xml \
    core/update/src/main/java/com/gamecenter/app/update/UpdateInstaller.java \
    module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/BatteryToolBinder.java \
    module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/AdvancedToolBinders.java \
    module-store/feature/tools/vpn/src/main/java/com/gamecenter/app/vpn/VpnFragment.kt \
    module-store/feature/tools/vpn/src/main/java/com/gamecenter/app/vpn/service/VpnServiceProxy.kt \
    module-store/feature/tools/browser/src/main/java/com/gamecenter/app/fragments/BrowserFragment.java

# 删除新增的 androidTest manifest 文件
Remove-Item "app\src\androidTest\AndroidManifest.xml"
```

### 影响范围

- 启动屏模块（SplashActivity）
- 主界面（MainActivity）
- 权限管理（PermissionHelper）
- 系统信息收集（SystemInfoCollector）
- 游戏基类与具体游戏 Activity（BaseGameActivity、DouDiZhuActivity）
- 模块资源加载（ModuleResourceLoader）
- 更新安装器（UpdateInstaller）
- 工具模块（BatteryToolBinder、AdvancedToolBinders）
- VPN 模块（VpnFragment、VpnServiceProxy）
- 浏览器模块（BrowserFragment）
- 测试 APK manifest
- 构建配置（build.gradle、themes.xml）

### 风险评估

- **风险等级**: 低
- **影响启动**: 否（SplashScreen API 兼容旧版本）
- **影响核心功能**: 否
- **替代方案**: 无（均为必要的版本适配修复）

### 待办事项

- MediaPipe `libllm_inference_engine_jni.so` 本身未 16KB 对齐，需等待上游库更新后才能真正支持 Android 15+ 16KB 页面大小设备
- 后续可在真机上验证各 Android 版本（11-17）的实际运行稳定性

---

## 修复蓝叠模拟器安装测试 APK 崩溃问题（空 activity 元数据栈溢出）

**修改时间**: 2026-06-23
**修改目标**: 修复安装 app-debug-androidTest.apk 时蓝叠模拟器解析空 activity 元数据触发栈缓冲区溢出崩溃（0xC0000409）的问题
**问题编号**: 蓝叠测试APK崩溃修复

### 问题描述

蓝叠模拟器在 `hcallOnAppInstalledClbk` 回调中解析测试 APK 元数据时，无法处理空 activity 字段：
```
jsonData = {"pkg":"com.gamecenter.app.test","activity":"","label":"","versionCode":0,...}
```
空 `activity` 字段导致蓝叠触发栈缓冲区溢出崩溃 (0xC0000409)。

### 修改文件列表

1. **app/src/androidTest/AndroidManifest.xml** (新增)
   - 声明 `package="com.gamecenter.app.test"` 和 `xmlns:android`、`xmlns:tools` 命名空间
   - 声明 `<application android:label="GameMatrix Tests">` 提供非空 label
   - 声明占位 Activity `androidx.test.core.app.InstrumentationActivityInvoker$BootstrapActivity`（由 androidx.test:core 传递依赖提供，无需实现）
   - 使用 `tools:replace="android:exported"` 覆盖 androidx.test:core 库 manifest 中的默认 exported 值，解决合并冲突

### 验证结果

- ✅ AndroidTest 构建成功 (`.\gradlew.bat :app:assembleDebugAndroidTest -PleakCanary=false`)
- ✅ 无编译错误，manifest 合并无冲突
- ✅ 合并后的测试 APK manifest 包含非空 activity 和 label：
  - `package="com.gamecenter.app.test"`
  - `application android:label="GameMatrix Tests"`
  - `activity android:name="androidx.test.core.app.InstrumentationActivityInvoker$BootstrapActivity"` (exported=false)
- ✅ 测试 APK 已生成: `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk`

### 回滚方法

```bash
# 删除新增的 androidTest manifest 文件
Remove-Item "app\src\androidTest\AndroidManifest.xml"
```

或使用 git 回滚（若已提交）：
```bash
git checkout HEAD -- app/src/androidTest/AndroidManifest.xml
```

### 影响范围

- 仅影响测试 APK (app-debug-androidTest.apk) 的 manifest 声明
- 不影响主应用代码和功能
- 不影响现有测试用例的执行
- BootstrapActivity 类来自 androidx.test:core（androidx.test:runner 的传递依赖），无需额外依赖

---

## Android 14 SelectedPhotoAccess 部分照片访问权限适配

**修改时间**: 2026-06-23
**修改目标**: 适配 Android 14+ 引入的"部分照片访问"权限 READ_MEDIA_VISUAL_USER_SELECTED，使用户可选择只授权部分照片
**问题编号**: Android 14 权限适配

### 修改文件列表

1. **app/src/main/AndroidManifest.xml**
   - 在 `READ_MEDIA_IMAGES` 权限声明后新增 `READ_MEDIA_VISUAL_USER_SELECTED` 权限声明，附中文注释说明

2. **app/src/main/java/com/gamecenter/app/PermissionHelper.java**
   - 在 `requestRuntimePermissions` 方法的 Android 13+ 分支内，新增 Android 14+ (API 34+, `UPSIDE_DOWN_CAKE`) 判断
   - 当运行在 Android 14+ 时，额外请求 `READ_MEDIA_VISUAL_USER_SELECTED` 权限，与 `READ_MEDIA_IMAGES` 一起请求
   - 添加中文注释说明部分照片访问权限的用途

### 验证结果

- ✅ Debug 构建成功 (`.\gradlew.bat :app:assembleDebug -PleakCanary=false`)
- ✅ 无编译错误
- ✅ 版本号自动递增: 476 → 477

### 回滚方法

```bash
git checkout HEAD -- app/src/main/AndroidManifest.xml app/src/main/java/com/gamecenter/app/PermissionHelper.java
```

### 影响范围

- 权限申请流程（PermissionHelper）
- 应用权限清单（AndroidManifest.xml）

---

## SplashScreen API 适配（Android 12+ 双启动屏修复）

**修改时间**: 2026-06-23
**修改目标**: 接入 AndroidX SplashScreen API，消除 Android 12+ 设备上的双启动屏闪烁问题
**问题编号**: 启动屏适配

### 修改文件列表

1. **app/build.gradle**
   - 在 dependencies 中添加 `androidx.core:core-splashscreen:1.0.1` 依赖

2. **app/src/main/res/values/themes.xml**
   - 修改 `Theme.GameMatrixApp.Splash` 主题：parent 从 `Theme.Material3.DayNight.NoActionBar` 改为 `Theme.SplashScreen`
   - 新增 `windowSplashScreenBackground`（`#2D2A26`，匹配现有启动屏渐变起始色）
   - 新增 `windowSplashScreenAnimatedIcon`（`@drawable/ic_launcher_foreground`）
   - 新增 `postSplashScreenTheme`（`@style/Theme.GameMatrixApp`）
   - 保留原有全屏/透明系统栏配置，移除不再需要的 colorPrimary 等颜色项

3. **app/src/main/res/values-night/themes.xml**
   - 新增 `Theme.GameMatrixApp.Splash` 主题（深色主题显式支持，配置与浅色一致）

4. **app/src/main/java/com/gamecenter/app/SplashActivity.java**
   - 新增 import `androidx.core.splashscreen.SplashScreen`
   - 在 `super.onCreate` 之前调用 `SplashScreen.installSplashScreen(this)`
   - 保持现有跳转逻辑和动画流程不变

### 验证结果

- ✅ Debug 构建成功 (`.\gradlew.bat :app:assembleDebug -PleakCanary=false`)
- ✅ 无编译错误
- ✅ 版本号自动递增: 474 → 475

### 回滚方法

```bash
git checkout HEAD -- app/build.gradle app/src/main/res/values/themes.xml app/src/main/res/values-night/themes.xml app/src/main/java/com/gamecenter/app/SplashActivity.java
```

### 影响范围

- 启动屏模块（SplashActivity）
- 主题资源（themes.xml）

---

## 循环1：清理美国VPS残留配置

**修改时间**: 2026-06-19  
**修改目标**: 完全移除已下线的美国VPS相关配置,简化代码结构  
**问题编号**: P0-1

### 修改文件列表

1. **app/build.gradle**
   - 移除 `SERVER_URL_FALLBACK` BuildConfig字段
   - 移除 `MODULE_FALLBACK_HOST` BuildConfig字段
   - 移除 `configuredServerUrlFallback` 变量定义和读取逻辑

2. **core/update/build.gradle**
   - 移除 `SERVER_URL_FALLBACK` BuildConfig字段
   - 移除 `configuredServerUrlFallback` 变量定义和读取逻辑

3. **core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java**
   - 移除 `US_BASE_URL` 常量
   - 简化 `buildUpdateUrls()` 方法,移除US VPS分支逻辑
   - 更新注释说明

4. **app/src/main/java/com/gamecenter/app/App.java**
   - 修改 `SecureOkHttpFactory.setHosts()` 调用,移除fallbackHost参数

5. **core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt**
   - 修改 `setHosts()` 方法签名,移除fallbackHost参数
   - 移除 `FALLBACK_HOST` 变量

### 保留的向后兼容代码

以下代码保留用于向后兼容,处理旧用户的历史选择:

1. **SettingsManager.java**
   - 保留 `UPDATE_SOURCE_VPS_US` 常量(标记为@Deprecated)
   - `getUpdateSource()` 方法中自动回退到 `UPDATE_SOURCE_AUTO`

2. **AppSettingsDialog.java**
   - 保留对 `UPDATE_SOURCE_VPS_US` 的检查逻辑
   - 旧用户历史选择了美国VPS时,显示为"自动"

### 验证结果

- ✅ Debug构建成功 (55s)
- ✅ 版本号自动递增: 452 → 453
- ✅ 无编译错误
- ✅ 无编译警告(除deprecation提示)

### 回滚方法

```bash
git checkout HEAD -- app/build.gradle core/update/build.gradle core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java app/src/main/java/com/gamecenter/app/App.java core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt
```

### 影响范围

- 更新分发模块
- 设置模块
- 安全模块

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

---

## 下一步计划

- 循环2: 修复P0-2证书绑定问题
- 循环3: 修复P0-3 R8混淆问题
- 循环4: 修复P1-1 ABI Splits问题
- 循环5: 修复P1-3设置界面美国VPS选项

---

## 循环2：修复P0-2证书绑定问题

**修改时间**: 2026-06-19  
**修改目标**: 在Release构建中启用证书绑定,提升网络安全性  
**问题编号**: P0-2

### 修改文件列表

1. **core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt**
   - 添加 `enableCertificatePinning` 变量控制证书绑定启用状态
   - 修改 `setHosts()` 方法增加 `enablePinning` 参数
   - 修改 `buildModuleClient()` 方法,根据 `enableCertificatePinning` 动态启用证书绑定
   - 移除注释掉的证书绑定代码,改为条件判断

2. **app/src/main/java/com/gamecenter/app/App.java**
   - 修改 `SecureOkHttpFactory.setHosts()` 调用,传入 `!BuildConfig.DEBUG` 参数
   - Release构建启用证书绑定,Debug构建禁用(兼容模拟器)

### 技术说明

- **Release构建**: `BuildConfig.DEBUG=false`, 因此 `!BuildConfig.DEBUG=true`, 启用证书绑定
- **Debug构建**: `BuildConfig.DEBUG=true`, 因此 `!BuildConfig.DEBUG=false`, 禁用证书绑定
- **兼容性**: 解决Android模拟器TLS CertificatePinner的SIGSEGV问题

### 验证结果

- ✅ Debug构建成功 (32s)
- ✅ 版本号自动递增: 453 → 454
- ✅ 无编译错误
- ✅ 证书绑定逻辑正确实现

### 回滚方法

```bash
git checkout HEAD -- core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt app/src/main/java/com/gamecenter/app/App.java
```

### 影响范围

- 安全模块
- 网络请求模块

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

---

## 循环3：修复P0-3 R8混淆问题

**修改时间**: 2026-06-19  
**修改目标**: 启用R8混淆和资源收缩,减小APK体积  
**问题编号**: P0-3

### 修改文件列表

1. **app/build.gradle**
   - 将 `minifyEnabled` 从 `false` 改为 `true`
   - 将 `shrinkResources` 从 `false` 改为 `true`

2. **app/proguard-rules.pro**
   - 添加游戏模块匿名内部类保留规则
   - 添加房间模块、网络模块、更新模块保留规则
   - 避免R8混淆导致的重复类问题

3. **app/src/main/res/values-en/strings.xml**
   - 添加XML声明和`<resources>`根元素
   - 添加`xliff`命名空间声明
   - 修复未转义的`&`字符(`Terms & privacy` → `Terms &amp; privacy`)

### 技术说明

- **R8混淆**: 启用代码混淆和资源收缩,减小APK体积
- **ProGuard规则**: 保留游戏模块匿名内部类,避免BreakoutActivity$1重复类问题
- **资源文件修复**: 英文字符串文件缺少XML根元素导致构建失败

### 验证结果

- ✅ Debug构建成功 (55s)
- ✅ 版本号自动递增: 454 → 455
- ✅ R8混淆启用成功
- ✅ 资源收缩启用成功
- ✅ 无编译错误

### 回滚方法

```bash
git checkout HEAD -- app/build.gradle app/proguard-rules.pro app/src/main/res/values-en/strings.xml
```

### 影响范围

- 构建配置
- 代码混淆
- 资源处理

### 风险评估

- **风险等级**: 中
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 可禁用R8混淆

---

## 循环4：修复P1-1 ABI Splits问题

**修改时间**: 2026-06-19  
**修改目标**: 启用ABI拆分,减小APK体积  
**问题编号**: P1-1

### 修改文件列表

1. **app/build.gradle**
   - 将 `splits.ab.enable` 从 `false` 改为 `true`
   - 更新注释说明,标注模拟器测试注意事项

### 技术说明

- **ABI拆分**: Release构建只生成arm64-v8a APK,减小约75%的Native库体积
- **模拟器兼容**: 如需x86_64模拟器测试,临时改回`enable false`
- **设备覆盖**: arm64-v8a占现代设备95%以上

### 验证结果

- ✅ Debug构建成功 (51s)
- ✅ 版本号自动递增: 455 → 456
- ✅ ABI splits启用成功
- ✅ 无编译错误

### 回滚方法

```bash
git checkout HEAD -- app/build.gradle
```

### 影响范围

- 构建配置
- APK生成

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 可临时禁用ABI splits

---

## 循环5：修复P1-3设置界面美国VPS选项

**修改时间**: 2026-06-19  
**修改目标**: 验证设置界面已正确移除美国VPS选项  
**问题编号**: P1-3

### 验证内容

1. **AppSettingsDialog.java** 检查：
   - `getUpdateSourceNames()` 方法将美国VPS选项设置为空字符串，从UI中隐藏
   - `getUpdateSourceName()` 方法处理旧用户回退逻辑（选择美国VPS的用户显示为"自动"）

2. **SettingsManager.java** 检查：
   - 保留 `UPDATE_SOURCE_VPS_US` 常量（标记为@Deprecated）
   - `getUpdateSource()` 方法自动回退已废弃的值

### 技术说明

- **向后兼容**: 旧用户历史选择了美国VPS时，自动回退到"自动"选项
- **UI隐藏**: 美国VPS选项在界面中不再显示
- **常量保留**: 保留废弃常量用于向后兼容处理

### 验证结果

- ✅ Debug构建成功 (53s)
- ✅ 版本号自动递增: 456 → 457
- ✅ 设置界面美国VPS选项已隐藏
- ✅ 旧用户回退逻辑正常工作
- ✅ 无编译错误

### 回滚方法

无需回滚（仅验证，未修改代码）

### 影响范围

- 设置界面
- 用户偏好处理

### 风险评估

- **风险等级**: 无
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

---

## 循环6：创建9个休闲类游戏自动化测试文件

**修改时间**: 2026-06-22  
**修改目标**: 为9个休闲类游戏创建 UiAutomator 自动化测试文件，覆盖启动、按钮遍历、游戏交互、退出四个测试场景  
**问题编号**: 无（新增功能）

### 修改文件列表

**新增文件（9个）**，位于 `app/src/androidTest/java/com/gamecenter/app/tests/games/casual/`：

1. **TetrisTest.kt** - 俄罗斯方块测试，滑动控制方块移动
2. **SnakeTest.kt** - 贪吃蛇测试，滑动控制方向
3. **FlappyTest.kt** - Flappy Bird 测试，点击屏幕跳跃
4. **BrotatoTest.kt** - Brotato 测试，点击屏幕射击/移动
5. **PlaneTest.kt** - 飞机大战测试，滑动控制飞机
6. **ReactionTest.kt** - 反应测试，点击出现的目标
7. **GuessTest.kt** - 猜数字测试，点击数字按钮
8. **TicTacToeTest.kt** - 井字棋测试，点击格子下棋
9. **WhackTest.kt** - 打地鼠测试，点击地鼠出现的位置

### 技术说明

- **测试基类**: 所有测试类继承 `com.gamecenter.app.EmulatorTestBase`，复用 `device`、`safeSleep()` 等通用方法
- **测试辅助**: 使用 `com.gamecenter.app.tests.GameTestHelper` 提供的 `launchGameActivity`、`clickAllVisibleButtons`、`assertAppAlive`、`exitGameToHall` 方法
- **测试注解**: 使用 `@RunWith(AndroidJUnit4::class)` 和 `@FixMethodOrder(MethodSorters.NAME_ASCENDING)` 保证测试按序执行
- **测试用例**: 每个测试类包含4个测试用例：
  - `test_001_launchGame`: 启动游戏 Activity，验证不崩溃
  - `test_002_clickAllButtons`: 遍历点击页面上所有可见可点击元素
  - `test_003_gameInteraction`: 根据游戏特点模拟基本游戏交互
  - `test_004_exitGame`: 退出游戏返回大厅
- **编译修复**: 用户模板中 `Log.d(TAG, ...)` 未导入 `android.util.Log` 且 `TAG` 在基类中为 private 不可访问，已在每个测试类的 companion object 中补充 `TAG` 常量定义并添加 `import android.util.Log`
- **包名统一**: 所有测试文件包名为 `com.gamecenter.app.tests.games.casual`

### 验证结果

- ✅ 9个测试文件全部创建成功
- ✅ 所有游戏 Activity 类已验证存在（Java 源文件）
- ✅ 包名与 Activity 全限定名匹配
- ✅ 每个文件包含4个测试用例
- ✅ 注释全部使用中文

### 回滚方法

删除新增的9个测试文件即可：

```bash
# Windows PowerShell
Remove-Item -Recurse "app\src\androidTest\java\com\gamecenter\app\tests\games\casual\"
```

或使用 git 回滚（若已提交）：

```bash
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/tests/games/casual/
```

### 影响范围

- 自动化测试模块（androidTest）
- 不影响主应用代码
- 不影响现有功能

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无（纯新增测试文件）

---

## 循环8：自动化测试体系建设（基础设施+架构+单元测试修复+文档）

**修改时间**: 2026-06-22  
**修改目标**: 建设完整的模块级自动化测试体系，修复权限弹窗阻塞、单元测试失败、预存编译错误，更新测试文档  
**问题编号**: 无（系统性测试建设）

### 修改文件列表

#### 1. 基础设施修复（权限弹窗+TEST_MODE）

1. **app/build.gradle**
   - 新增 `TEST_MODE` BuildConfig 字段（Debug 包默认 true）
   - 新增 `debug` buildType，关闭 ABI 拆分
   - 新增 `androidTestImplementation 'androidx.test.uiautomator:uiautomator:2.3.0'`
   - 新增 `androidTestImplementation 'androidx.room:room-testing:2.7.1'`
   - 新增 `implementation project(':core:online')` 依赖

2. **app/src/main/java/com/gamecenter/app/PermissionHelper.java**
   - `isFirstLaunch()` 方法增加 `BuildConfig.TEST_MODE` 检查，测试模式自动跳过权限弹窗

3. **app/src/androidTest/java/com/gamecenter/app/EmulatorTestBase.kt**
   - 新增 `grantAppPermissions()` 方法：通过 ADB 预授权运行时权限
   - 新增 `dismissPermissionDialogIfShown()` 方法：兜底处理权限弹窗
   - 新增 `allowSystemPermissionIfRequested()` 方法：处理系统权限弹窗
   - 新增 `launchAppAndHandlePermissionDialog()` 方法：启动应用并处理弹窗
   - 新增 `safeSleep()` 方法：安全等待

#### 2. 测试架构与通用辅助类

4. **app/src/androidTest/java/com/gamecenter/app/tests/GameTestHelper.kt** (新增)
   - 游戏测试通用辅助类，提供 `launchGameActivity`、`clickAllVisibleButtons`、`clickButtonByText`、`assertAppAlive`、`exitGameToHall` 等方法

5. **app/src/androidTest/java/com/gamecenter/app/tests/home/HomeNavigationTest.kt** (新增)
   - 主页导航测试，10 个用例：应用启动、底部导航、分类 Tab、搜索、设置、模块商店、导航切换、游戏卡片、版本号

#### 3. 益智类游戏测试（10个文件）

6. **app/src/androidTest/java/com/gamecenter/app/tests/games/puzzle/** (新增 10 个文件)
   - Game2048Test.kt、SudokuTest.kt、KlotskiTest.kt、SokobanTest.kt、PipelineTest.kt
   - MinesweeperTest.kt、MatchTest.kt、MemoryTest.kt、BreakoutTest.kt、TilesTest.kt

#### 4. 功能模块测试（5个文件）

7. **app/src/androidTest/java/com/gamecenter/app/tests/features/** (新增 5 个文件)
   - SettingsTest.kt（10 个用例）、ToolBoxTest.kt、AiAssistantTest.kt、BrowserTest.kt、ModuleStoreTest.kt（8 个用例）

#### 5. 预存编译错误修复

8. **app/src/androidTest/java/com/gamecenter/app/T02ModuleStoreTest.kt**
   - 移除未使用的 mockito 导入
   - 修复 4 处 `ModuleManifest().apply { }` 为构造参数方式

9. **app/src/androidTest/java/com/gamecenter/app/T04BuiltInGameUpdateTest.kt**
   - 修复 `ModuleVersionChecker` 导入路径（`modulestore` → `modules`）
   - 新增 `ModuleManifest` 导入
   - 修复 2 处 `ModuleManifest().apply { }` 为构造参数方式

10. **app/src/androidTest/java/com/gamecenter/app/RoomDatabaseIntegrationTest.kt**
    - 移除不存在的 `AiMessageEntity` 相关测试（2 个测试方法）
    - 修复 `getByGameType` → `getByGameTypeSync`

11. **app/src/androidTest/java/com/gamecenter/app/games/GameRegistryTest.java**
    - 移除引用不存在方法 `isPluginGame()` 的测试方法

#### 6. 单元测试失败修复

12. **app/src/main/java/com/gamecenter/app/games/GameRegistry.java**
    - 新增 `categoryName()` 方法，统一分类名称获取
    - `categoryName()` 添加 `Resources.NotFoundException` 捕获和回退逻辑，修复 Robolectric 环境下资源找不到问题

13. **core/moduleloader/src/main/java/com/gamecenter/app/moduleloader/ModuleLoaderV2.java**
    - `unloadModule(null)` 增加 null 安全检查
    - `getFrameworkVersionCodeFromPackage()` 返回值确保 >= 1

14. **app/src/test/java/com/gamecenter/app/modules/ModuleLoaderV2Test.java**
    - `setUp()` 增加单例重置逻辑，确保测试独立性

#### 7. 文档更新

15. **测试方案.md**
    - 新增第 10 章「自动化 UI 测试架构」（架构概述、文件结构、用例说明、运行命令、权限处理、新增规范、模块对应关系）
    - 更新第 9 章新增 9.5 节「UI 自动化测试框架」
    - 更新第 11 章注意事项（新增权限弹窗、APK 安装提示）
    - 版本号升级至 2.0

### 验证结果

- ✅ `:app:compileDebugAndroidTestKotlin` 编译通过
- ✅ `:app:compileDebugUnitTestKotlin` 编译通过
- ✅ `:app:assembleDebug :app:assembleDebugAndroidTest` 构建成功
- ✅ GameRegistryTest 单元测试全部通过
- ✅ ModuleLoaderV2Test 单元测试全部通过
- ✅ 34 个自动化测试文件（151 个用例）全部创建

### 回滚方法

```bash
# 1. 回滚 build.gradle
git checkout HEAD -- app/build.gradle

# 2. 回滚主代码修改
git checkout HEAD -- app/src/main/java/com/gamecenter/app/PermissionHelper.java
git checkout HEAD -- app/src/main/java/com/gamecenter/app/games/GameRegistry.java
git checkout HEAD -- core/moduleloader/src/main/java/com/gamecenter/app/moduleloader/ModuleLoaderV2.java

# 3. 回滚测试基类
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/EmulatorTestBase.kt

# 4. 删除新增测试目录
Remove-Item -Recurse "app\src\androidTest\java\com\gamecenter\app\tests\"

# 5. 回滚预存测试修复
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/T02ModuleStoreTest.kt
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/T04BuiltInGameUpdateTest.kt
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/RoomDatabaseIntegrationTest.kt
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/games/GameRegistryTest.java

# 6. 回滚单元测试修复
git checkout HEAD -- app/src/test/java/com/gamecenter/app/modules/ModuleLoaderV2Test.java

# 7. 回滚文档
git checkout HEAD -- 测试方案.md
```

### 影响范围

- 自动化测试模块（androidTest）：新增 34 个测试文件
- 单元测试模块（test）：修复 2 个测试文件
- 主代码：PermissionHelper（TEST_MODE）、GameRegistry（资源回退）、ModuleLoaderV2（null 安全+版本号）
- 构建配置：build.gradle（依赖+TEST_MODE）
- 文档：测试方案.md

### 风险评估

- **风险等级**: 低
- **影响启动**: 否（TEST_MODE 仅 Debug 包生效）
- **影响核心功能**: 否（GameRegistry 回退逻辑仅在资源加载失败时触发）
- **替代方案**: 无

---

## 循环7：创建8个经典类游戏自动化测试文件

**修改时间**: 2026-06-22  
**修改目标**: 为8个经典类游戏创建 UiAutomator 自动化测试文件，覆盖启动、按钮遍历、游戏交互、退出四个测试场景  
**问题编号**: 无（新增功能）

### 修改文件列表

**新增文件（8个）**，位于 `app/src/androidTest/java/com/gamecenter/app/tests/games/classics/`：

1. **GomokuGameTest.kt** - 五子棋测试，点击棋盘落子、切换难度
2. **ChineseChessGameTest.kt** - 中国象棋测试，点击棋盘选择并移动棋子
3. **GoGameTest.kt** - 围棋测试，开始对弈、落子、虚着
4. **DouDiZhuGameTest.kt** - 斗地主测试，点击单机模式进入游戏
5. **BlackjackGameTest.kt** - 21点测试，新游戏、要牌、停牌
6. **CheckersGameTest.kt** - 跳棋测试，选择难度、点击棋盘移动
7. **DiceGameTest.kt** - 骰子测试，掷骰子、加倍、下一局
8. **RockGameTest.kt** - 石头剪刀布测试，选择石头/剪刀/布

### 技术说明

- **测试基类**: 所有测试类继承 `com.gamecenter.app.EmulatorTestBase`，复用 `device`、`safeSleep()` 等通用方法
- **测试辅助**: 使用 `com.gamecenter.app.tests.GameTestHelper` 提供的 `launchGameActivity`、`clickAllVisibleButtons`、`clickButtonByText`、`assertAppAlive`、`exitGameToHall` 方法
- **测试注解**: 使用 `@RunWith(AndroidJUnit4::class)` 和 `@FixMethodOrder(MethodSorters.NAME_ASCENDING)` 保证测试按序执行
- **测试用例**: 每个测试类包含4个测试用例：
  - `test_001_launchGame`: 启动游戏 Activity，验证不崩溃
  - `test_002_clickAllButtons`: 遍历点击页面上所有可见可点击元素
  - `test_003_gameInteraction`: 根据游戏特点模拟基本游戏交互
  - `test_004_exitGame`: 退出游戏返回大厅
- **编译修复**: 用户模板中 `Log.d(TAG, ...)` 未导入 `android.util.Log` 且 `TAG` 在基类中为 private 不可访问，已在每个测试类的 companion object 中补充 `TAG` 常量定义并添加 `import android.util.Log`
- **包名统一**: 所有测试文件包名为 `com.gamecenter.app.tests.games.classics`
- **交互适配**: 每个游戏的 `test_003_gameInteraction` 根据实际 Activity 的按钮文本定制（已核对源码确认按钮文本）

### 验证结果

- ✅ 8个测试文件全部创建成功
- ✅ 所有游戏 Activity 类已验证存在（Java 源文件位于 `app/src/main/java/com/gamecenter/app/games/`）
- ✅ 包名与 Activity 全限定名匹配
- ✅ 每个文件包含4个测试用例
- ✅ 注释全部使用中文

### 回滚方法

删除新增的8个测试文件即可：

```bash
# Windows PowerShell
Remove-Item -Recurse "app\src\androidTest\java\com\gamecenter\app\tests\games\classics\"
```

或使用 git 回滚（若已提交）：

```bash
git checkout HEAD -- app/src/androidTest/java/com/gamecenter/app/tests/games/classics/
```

### 影响范围

- 自动化测试模块（androidTest）
- 不影响主应用代码
- 不影响现有功能

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无（纯新增测试文件）

---

## 循环8：执行全部自动化UI测试并验证

**修改时间**: 2026-06-22  
**修改目标**: 在模拟器上执行全部自动化UI测试，验证145个测试用例全部通过  
**问题编号**: 无（测试执行）

### 测试执行概述

在模拟器（emulator-5556）上成功执行了全部自动化UI测试，测试版本为 versionCode=466, versionName=1.4.1。

### 测试执行详情

#### 1. 主页导航测试
- **测试文件**: `HomeNavigationTest.kt`
- **用例数**: 10个
- **执行结果**: ✅ 全部通过
- **测试内容**: 应用启动、底部导航栏可见性、Tab切换、游戏大厅、工具箱、AI助手、设置、模块商店、返回键处理、深度链接

#### 2. 经典类游戏测试（8个）
- **测试文件**: 8个独立测试文件
- **用例数**: 32个（每个游戏4个用例）
- **执行结果**: ✅ 全部通过
- **测试游戏**: 五子棋、中国象棋、围棋、斗地主、21点、跳棋、骰子、石头剪刀布
- **测试内容**: 游戏启动、按钮遍历、游戏交互、退出游戏

#### 3. 益智类游戏测试（10个）
- **测试文件**: 10个独立测试文件
- **用例数**: 40个（每个游戏4个用例）
- **执行结果**: ✅ 全部通过
- **执行耗时**: 426.9秒
- **测试游戏**: 2048、数独、华容道、推箱子、管道、扫雷、消消乐、记忆翻牌、打砖块、拼图
- **测试内容**: 游戏启动、按钮遍历、游戏交互、退出游戏

#### 4. 休闲类游戏测试（9个）
- **测试文件**: 9个独立测试文件
- **用例数**: 36个（每个游戏4个用例）
- **执行结果**: ✅ 全部通过
- **执行耗时**: 315.8秒
- **测试游戏**: 俄罗斯方块、贪吃蛇、Flappy Bird、Brotato、飞机大战、反应测试、猜数字、井字棋、打地鼠
- **测试内容**: 游戏启动、按钮遍历、游戏交互、退出游戏

#### 5. 功能模块测试（5个）
- **测试文件**: 5个独立测试文件
- **用例数**: 27个
- **执行结果**: ✅ 全部通过
- **执行耗时**: 572.6秒
- **测试模块**:
  - 设置模块（10个用例）：打开设置、返回按钮、模块商店、管理模块、清除缓存、主题切换、语言切换、统计信息、关于页面、按钮遍历
  - 工具箱模块（3个用例）：打开工具箱、按钮遍历、退出
  - AI助手模块（3个用例）：打开AI助手、按钮遍历、退出
  - 浏览器模块（3个用例）：打开浏览器、按钮遍历、退出
  - 模块商店模块（8个用例）：打开商店、分类标签、子分类标签、搜索功能、刷新模块、已安装模块、按钮遍历、退出商店

### 测试执行命令

```powershell
# 主页导航测试
adb -s emulator-5556 shell am instrument -w -r -e class com.gamecenter.app.tests.home.HomeNavigationTest com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 经典类游戏测试
adb -s emulator-5556 shell am instrument -w -r -e package com.gamecenter.app.tests.games.classics com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 益智类游戏测试
adb -s emulator-5556 shell am instrument -w -r -e package com.gamecenter.app.tests.games.puzzle com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 休闲类游戏测试
adb -s emulator-5556 shell am instrument -w -r -e package com.gamecenter.app.tests.games.casual com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner

# 功能模块测试
adb -s emulator-5556 shell am instrument -w -r -e package com.gamecenter.app.tests.features com.gamecenter.app.test/androidx.test.runner.AndroidJUnitRunner
```

### 测试结果汇总

| 测试模块 | 用例数 | 结果 | 耗时 |
|---------|--------|------|------|
| 主页导航测试 | 10 | ✅ 全部通过 | - |
| 经典类游戏测试 | 32 | ✅ 全部通过 | - |
| 益智类游戏测试 | 40 | ✅ 全部通过 | 426.9s |
| 休闲类游戏测试 | 36 | ✅ 全部通过 | 315.8s |
| 功能模块测试 | 27 | ✅ 全部通过 | 572.6s |
| **总计** | **145** | **✅ 全部通过** | **~22分钟** |

### 关键技术修复

在执行测试过程中，发现并修复了以下问题：

1. **BaseGameActivity视图重复添加崩溃**
   - **问题**: 游戏Activity启动时报错"The specified child already has a parent"
   - **修复**: 在添加视图前检查并移除已有的父视图
   - **影响文件**: `app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java`

2. **权限弹窗阻塞测试**
   - **问题**: 首次安装后权限使用说明弹窗无法自动关闭
   - **修复**: 在PermissionHelper中添加TEST_MODE检查，EmulatorTestBase中实现ADB预授权和弹窗点击处理
   - **影响文件**: 
     - `app/src/main/java/com/gamecenter/app/PermissionHelper.java`
     - `app/src/androidTest/java/com/gamecenter/app/EmulatorTestBase.kt`

3. **HomeNavigationTest底部导航栏检测失败**
   - **问题**: 底部导航栏可见性检测失败
   - **修复**: 增加多方式查找和等待逻辑（resourceId、className、resourceIdMatches）
   - **影响文件**: `app/src/androidTest/java/com/gamecenter/app/tests/home/HomeNavigationTest.kt`

### 验证结果

- ✅ 145个自动化UI测试用例全部通过
- ✅ 无崩溃、无ANR
- ✅ 所有游戏Activity正常启动和退出
- ✅ 所有功能模块正常访问
- ✅ 测试总耗时约22分钟

### 回滚方法

本次为测试执行，无需回滚。如需回滚之前的测试文件创建，请参考循环6和循环7的回滚方法。

### 影响范围

- 测试执行结果记录
- 测试方案.md文档更新
- 修改记录.md文档更新

### 风险评估

- **风险等级**: 无
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

---

## 循环9：修复蓝叠模拟器闪退 + 安卓11-17适配性修复

**修改时间**: 2026-06-23
**修改目标**: 修复蓝叠模拟器安装应用后崩溃问题，彻查并修复Android 11-17适配性问题
**问题编号**: P0-2

### 问题分析

#### 1. 蓝叠模拟器闪退（根本原因）
- **现象**: 安装应用后蓝叠模拟器进程崩溃，异常代码 `0xC0000409`（STATUS_STACK_BUFFER_OVERRUN）
- **根因**: LeakCanary 2.14 通过 ContentProvider 自动注册 `LeakLauncherActivity`，蓝叠模拟器的 `ucrtbase.dll` 在解析该 Activity 时触发栈缓冲区溢出崩溃
- **日志证据**: `hcallOnAppInstalledClbk` 回调后立即 `UnhandledException`

#### 2. Android 11-17 适配性问题（彻查发现）
- **P0**: `registerReceiver` 未指定 export 标志（Android 14+ 崩溃）
- **P0**: `VpnServiceProxy` 未调用 `startForeground`（Android 14+ 服务被杀）
- **P1**: 未声明 `POST_NOTIFICATIONS` 权限（Android 13+ 通知不显示）
- **P1**: 未处理 Edge-to-Edge 显示（Android 15+ UI遮挡）

### 修改文件列表

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `app/build.gradle` | 修改 | LeakCanary 改为通过 `-PleakCanary=true` 开关启用，默认关闭 |
| `app/src/main/AndroidManifest.xml` | 修改 | 添加 `POST_NOTIFICATIONS` 权限声明 |
| `app/src/main/java/com/gamecenter/app/PermissionHelper.java` | 修改 | 添加 Android 13+ `POST_NOTIFICATIONS` 运行时权限请求 |
| `app/src/main/java/com/gamecenter/app/MainActivity.java` | 修改 | 添加 `EdgeToEdge.enable()` 适配 Android 15+ |
| `app/src/main/java/com/gamecenter/app/vpn/service/VpnServiceProxy.kt` | 修改 | 添加 `startForeground` 调用，适配 Android 14+ 前台服务要求 |
| `module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/BatteryToolBinder.java` | 修改 | `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志 |
| `app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java` | 修改 | `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志 |
| `module-store/feature/tools/tools/src/main/java/com/gamecenter/app/tools/AdvancedToolBinders.java` | 修改 | `registerReceiver` 添加 `RECEIVER_NOT_EXPORTED` 标志 |

### 关键代码变更

#### 1. LeakCanary 开关化（app/build.gradle）
```gradle
// 通过 -PleakCanary=true 启用，默认关闭以避免蓝叠模拟器崩溃
if (project.hasProperty('leakCanary') && project.property('leakCanary').toBoolean()) {
    debugImplementation 'com.squareup.leakcanary:leakcanary-android:2.14'
}
```

#### 2. registerReceiver 适配 Android 14+
```java
// Android 14+ (API 34) 要求动态注册广播接收器必须显式指定 export 标志
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    context.registerReceiver(batteryReceiver, filter, Context.RECEIVER_NOT_EXPORTED);
} else {
    context.registerReceiver(batteryReceiver, filter);
}
```

#### 3. VpnServiceProxy 前台服务适配
```kotlin
// Android 14+ 适配：VpnService 作为前台服务运行，避免被系统杀死
startForeground(NOTIFICATION_ID, notif)
```

#### 4. POST_NOTIFICATIONS 权限请求
```java
// Android 13+ 通知权限：未授权则请求
if (ContextCompat.checkSelfPermission(activity, Manifest.permission.POST_NOTIFICATIONS)
        != PackageManager.PERMISSION_GRANTED) {
    permissionsToRequest.add(Manifest.permission.POST_NOTIFICATIONS);
}
```

#### 5. Edge-to-Edge 适配
```java
// Android 15+ 适配：启用 Edge-to-Edge 显示
EdgeToEdge.enable(this);
```

### 验证结果

- ✅ 蓝叠模拟器安装主应用后不再崩溃
- ✅ 应用启动正常，模拟器保持稳定在线
- ✅ 编译成功（versionCode=470）
- ✅ 所有修改通过编译验证

### 回滚方法

1. **LeakCanary 恢复**: 将 `app/build.gradle` 中的条件判断改回 `debugImplementation 'com.squareup.leakcanary:leakcanary-android:2.14'`
2. **registerReceiver 回滚**: 移除 `Build.VERSION.SDK_INT` 判断，直接使用 `context.registerReceiver(receiver, filter)`
3. **VpnServiceProxy 回滚**: 移除 `startForegroundIfNeeded()` 方法和调用
4. **POST_NOTIFICATIONS 回滚**: 移除 AndroidManifest 中的权限声明和 PermissionHelper 中的请求逻辑
5. **Edge-to-Edge 回滚**: 移除 `EdgeToEdge.enable(this)` 调用和 import

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否
- **替代方案**: 无

### 仍需关注的适配问题（P2/P3，暂未修复）

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P2 | SplashScreen API 未接入 | Android 12+ 双启动屏体验问题，非崩溃问题 |
| P2 | 固定屏幕方向将被忽略 | Android 16+ 布局可能错乱，需后续适配 |
| P2 | 16KB 页面对齐未验证 | MediaPipe .so 库需在 Android 15+ 设备实测 |
| P3 | 反射私有 API（部分修复） | `addAssetPath` 已加固错误处理（见循环10）；`SystemProperties` 未来版本可能失效 |

---

## 循环10：加固 ModuleResourceLoader 反射私有 API 错误处理

**修改时间**: 2026-06-23  
**修改目标**: 为 `AssetManager.addAssetPath` 私有 API 反射调用添加完善的错误处理，避免未来 Android 版本封禁该 API 时导致应用崩溃  
**问题编号**: P3（对应循环9遗留问题）

### 问题分析

`ModuleResourceLoader.loadResources()` 通过反射调用 `AssetManager.addAssetPath` 私有 API 加载外部 APK 资源。原实现仅有一个外层 `catch (Exception)`，存在以下隐患：

1. 反射调用失败时日志信息不够详细，难以定位是哪个环节失败（方法找不到/非法访问/目标异常/实例化失败）
2. 未检查 `addAssetPath` 的返回值（返回 -1 或 0 表示路径无效/加载失败）
3. 未添加 `@Suppress("DiscouragedPrivateApi")` 注解，Lint 会持续告警
4. 未来 Android 版本若通过非 SDK 接口黑名单封禁该 API，应用可能崩溃

### 修改文件列表

1. **app/src/main/kotlin/com/gamecenter/app/modular/ModuleResourceLoader.kt**
   - 在反射调用处添加 `@Suppress("DiscouragedPrivateApi")` 注解抑制 Lint 警告
   - 将反射调用独立包裹在 try-catch 中，分别捕获 `NoSuchMethodException`、`IllegalAccessException`、`InvocationTargetException`、`InstantiationException` 四类具体异常
   - 每类异常记录详细的失败原因日志（包括模块 ID、APK 路径、异常类型）
   - 检查 `addAssetPath` 返回值，<= 0 时记录日志并返回 null
   - 添加详细注释说明私有 API 现状、未来风险、备选方案（PathClassLoader）
   - 保留外层 try-catch 作为兜底，确保不会崩溃
   - 方法签名和返回值类型保持不变（`ModuleResources?`）

### 技术说明

- **私有 API 现状**: `AssetManager.addAssetPath` 是 `@hide` 标注的私有 API，目前在所有 Android 版本（含 14/15）均可用，是加载外部 APK 资源的事实标准方案
- **未来风险**: 未来 Android 版本可能通过非 SDK 接口黑名单（darkgreylist/maxtargetapi）封禁该 API
- **备选方案**: 使用 `dalvik.system.PathClassLoader` 加载 APK 再通过 `PackageManager` 获取资源，缺点是效率较低（需解析整个 APK 包信息），且对未安装 APK 支持有限
- **返回值约定**: `addAssetPath` 返回 cookie（正整数）表示成功，返回 -1 或 0 表示路径无效/加载失败
- **错误处理策略**: 反射失败时返回 null（保持原方法签名），调用方应处理资源加载失败的情况

### 验证结果

- ✅ `:app:assembleDebug -PleakCanary=false` 编译成功（11s）
- ✅ 版本号自动递增: 472 → 473
- ✅ 无编译错误
- ✅ 无新增编译警告
- ✅ 方法签名和返回值类型未改变

### 回滚方法

```bash
git checkout HEAD -- app/src/main/kotlin/com/gamecenter/app/modular/ModuleResourceLoader.kt
```

### 影响范围

- 模块化资源加载模块（modular）
- 不影响现有功能（仅加固错误处理，正常路径行为不变）
- 不影响方法签名和返回值

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否（仅加固错误处理，正常路径行为不变）
- **替代方案**: 可切换为 PathClassLoader 加载资源，但效率较低

---

## 循环10：修复 SystemInfoCollector 私有API反射与版本兼容问题

**修改时间**: 2026-06-23  
**修改目标**: 修复 SystemInfoCollector 中两处兼容性隐患：SystemProperties 私有API反射未来可能被封禁、MediaDrm#getSecurityLevel 缺少 API 28+ 版本判断  
**问题编号**: #7（SystemProperties 反射）、#8（MediaDrm#getSecurityLevel 版本判断）

### 问题分析

#### 问题1（#7）：SystemProperties 反射访问私有 API
- **位置**: `SystemInfoCollector.java` 第906行附近 `getSystemProperty` 方法
- **现象**: 通过反射访问 `android.os.SystemProperties` 私有 API，未来 Android 版本可能封禁此类反射访问
- **风险**: Android P (API 28) 起对非 SDK 接口反射做了限制，目前 SystemProperties 仍在灰名单中可用，但更新版本可能被完全屏蔽

#### 问题2（#8）：MediaDrm#getSecurityLevel 缺少版本判断
- **位置**: `SystemInfoCollector.java` 第614行附近 `getWidevineLevel` 方法
- **现象**: 调用 `MediaDrm#getSecurityLevel` 需要 API 28+ (Build.VERSION_CODES.P)，但项目 minSdk 为 24
- **风险**: 低于 API 28 的设备调用会抛出 `NoSuchMethodError`，导致 Widevine 安全级别获取失败

### 修改文件列表

1. **app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java**
   - **getSystemProperty 方法**（问题1）：
     - 在 Javadoc 中添加注释说明这是私有 @hide API，未来 Android 版本可能封禁反射访问
     - 在方法内部注释中说明 Android P+ 对非 SDK 接口的限制情况
     - 在 catch 块中添加 `Log.w` 日志记录（与项目其他方法风格一致），记录失败的属性名和异常信息
     - 保留失败时返回空字符串的优雅降级逻辑
   - **getWidevineLevel 方法**（问题2）：
     - 在调用 `drm.getSecurityLevel()` 前添加 `if (Build.VERSION.SDK_INT < Build.VERSION_CODES.P)` 版本判断
     - 低于 API 28 的设备返回 -1（表示不支持）
     - 更新 Javadoc 说明版本要求和返回值含义
   - **getDrmInfo 方法**（问题2配套显示优化）：
     - 调用 `getWidevineLevel` 处增加对返回值 -1 的处理，显示"不支持"而非"L-1"
     - 添加注释说明 `getWidevineLevel` 在低于 API 28 的设备上返回 -1

### 技术说明

- **私有 API 反射降级**: SystemProperties 反射调用保留，但通过 try-catch 优雅降级。未来 Android 版本若封禁反射，将自动返回空字符串并记录日志，不影响其他采集项
- **版本判断**: `MediaDrm#getSecurityLevel` 是 API 28 新增方法，必须做版本判断。低于 API 28 的设备返回 -1，调用方显示"不支持"
- **import 复用**: `android.os.Build` 已在文件第19行 import，无需新增
- **代码风格**: 保持与项目现有风格一致（中文注释、Log.w 日志、try-catch 异常处理）

### 验证结果

- ✅ `.\gradlew.bat :app:assembleDebug -PleakCanary=false` 编译成功（BUILD SUCCESSFUL in 11s）
- ✅ 版本号自动递增: 471 → 472
- ✅ 无编译错误
- ✅ 无编译警告（除 Gradle deprecation 提示）

### 回滚方法

```bash
git checkout HEAD -- app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java
```

### 影响范围

- 系统信息采集模块（SystemInfoCollector）
- 不影响其他模块
- 不影响现有功能（仅增强兼容性和健壮性）

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否（系统信息采集为辅助功能）
- **替代方案**: 无（保留功能并优雅降级）

---

## 循环11：适配 Android 16+ 屏幕方向锁定（游戏 Activity）

**修改时间**: 2026-06-23  
**修改目标**: 适配 Android 16+ (API 36) 将忽略 manifest 中 `android:screenOrientation` 的行为，改为在运行时通过 `setRequestedOrientation()` 强制锁定游戏屏幕方向  
**问题编号**: P2（对应循环9遗留问题：固定屏幕方向将被忽略）

### 问题分析

Android 16+ (API 36) 起系统将忽略 `android:screenOrientation="portrait"` 等 manifest 静态声明，导致游戏在 Android 16+ 设备上方向不再被锁定，可能造成布局错乱。`AndroidManifest.xml` 中有 30+ 处 Activity 声明了固定屏幕方向。

适配方案：保留 manifest 中的 `android:screenOrientation` 属性（旧版本仍需），同时在游戏 Activity 的 `onCreate` 中调用 `setRequestedOrientation()`（运行时调用在 Android 16+ 仍然有效）。

### 修改文件列表

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java` | 修改 | 新增 `import android.content.pm.ActivityInfo`；onCreate 中 super 后调用 `setRequestedOrientation(SCREEN_ORIENTATION_PORTRAIT)`，统一覆盖 25 个继承该基类的竖屏游戏 |
| `app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java` | 修改 | 新增 import；onCreate 中调用 `setRequestedOrientation(SCREEN_ORIENTATION_LANDSCAPE)`（横屏） |
| `app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuMenuActivity.java` | 修改 | 新增 import；onCreate 中调用 `setRequestedOrientation(SCREEN_ORIENTATION_PORTRAIT)`（竖屏） |
| `app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java` | 修改 | 新增 import；onCreate 中调用 `setRequestedOrientation(SCREEN_ORIENTATION_PORTRAIT)`（竖屏） |
| `app/src/main/java/com/gamecenter/app/SplashActivity.java` | 修改（修复预存编译错误） | 第39行 `playEnterAnimations()` 改为 `playEnterAnimation()`，与方法定义（单数）一致 |

### 覆盖范围说明

- **BaseGameActivity 统一处理（25 个竖屏游戏）**：Flappy、ChineseChess、Blackjack、Checkers、Dice、Brotato、Go、Game2048、Breakout、Guess、Memory、Match、Klotski、Minesweeper、Pipeline、Rock、Sokoban、Plane、Tetris、Reaction、Sudoku、Whack、TicTacToe、Tiles、Snake
- **单独处理（直接继承 AppCompatActivity 的游戏）**：DouDiZhuActivity（横屏）、DouDiZhuMenuActivity（竖屏）、GomokuActivity（竖屏）
- **未修改（非游戏 Activity，按需求范围）**：MainActivity、SettingsActivity、RecoveryActivity、AchievementCenterActivity、StatsActivity、DynamicGameActivity（根包，非 games 包，且为转发器）、ModuleStoreActivity、InstalledModulesActivity

### 技术说明

- **为何不引入 feature flag**：此为 Android 16+ 兼容性修复，`setRequestedOrientation` 在所有 Android 版本行为一致且与 manifest 声明等价，引入开关反而会导致 Android 16+ 游戏方向失效，故不引入 flag。
- **为何不移除 manifest 属性**：旧版本（API < 36）仍依赖 manifest 中的 `android:screenOrientation` 在 Activity 创建前锁定方向，避免启动瞬间方向闪烁，故保留。
- **调用时机**：在 `super.onCreate()` 之后、`setContentView()` 之前调用，确保方向在布局渲染前生效。

### 验证结果

- ✅ `.\gradlew.bat :app:assembleDebug -PleakCanary=false` 编译成功（BUILD SUCCESSFUL in 10s）
- ✅ 版本号自动递增: 473 → 474
- ✅ 无编译错误
- ✅ 无编译警告（除 Gradle deprecation 提示）

### 回滚方法

```bash
git checkout HEAD -- app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuActivity.java app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuMenuActivity.java app/src/main/java/com/gamecenter/app/games/gomoku/GomokuActivity.java app/src/main/java/com/gamecenter/app/SplashActivity.java
```

### 影响范围

- 游戏模块（games 包下所有游戏 Activity）
- 启动屏（SplashActivity，仅修复预存编译错误）
- 不影响非游戏模块

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否（运行时锁定方向与原 manifest 声明行为一致）
- **替代方案**: 无

---

## 循环14：俄罗斯方块添加音效功能

**修改时间**: 2026-06-23
**修改目标**: 为俄罗斯方块游戏添加方块旋转、落地、消行三种音效，复用现有 `R.raw.ui_turn` 资源，不新增资源文件
**问题编号**: 无（功能增强）

### 问题描述

俄罗斯方块游戏此前无任何音效反馈，用户体验单调。需在方块旋转、落地、消行三个关键节点播放音效，且播放前需检查用户的游戏音效开关设置。

### 修改文件列表

1. **app/src/main/java/com/gamecenter/app/games/tetris/TetrisView.java**
   - 新增 `OnPieceRotateListener` 接口（方块旋转成功回调）
   - 新增 `OnPieceLandListener` 接口（方块落地/锁定回调）
   - 新增对应监听器字段 `pieceRotateListener`、`pieceLandListener` 及 setter 方法
   - 在 `rotate()` 方法成功旋转后触发 `pieceRotateListener.onPieceRotated()`
   - 在 `lockPiece()` 方法末尾触发 `pieceLandListener.onPieceLanded()`
   - 未改动任何游戏逻辑（旋转判定、锁定逻辑、消行算法均保持原样）

2. **app/src/main/java/com/gamecenter/app/games/tetris/TetrisActivity.java**
   - 新增 import：`android.media.SoundPool`、`com.gamecenter.app.R`、`com.gamecenter.app.SettingsManager`
   - 新增成员变量：`SoundPool soundPool` 及 `soundIdRotate`/`soundIdLand`/`soundIdClear` 三个音效 id
   - 在 `initGame()` 中初始化 SoundPool（`setMaxStreams(4)`）并加载 `R.raw.ui_turn` 三次（旋转/落地/消行各一份）
   - 注册 `setOnPieceRotateListener` 和 `setOnPieceLandListener` 回调，分别播放旋转音和落地音
   - 在已有的 `setOnLinesClearedListener` 回调中追加 `playSound(soundIdClear)` 播放消行音
   - 新增 `playSound(int soundId)` 方法，播放前检查 `SettingsManager.getInstance(this).shouldPlayGameSound()`
   - 新增 `onDestroy()` 释放 SoundPool 资源（`soundPool.release()` 并置 null）

### 技术说明

- **音效资源复用**：三种音效均复用现有 `R.raw.ui_turn`（`app/src/main/res/raw/ui_turn.wav`），不新增任何资源文件，符合资源构建链路要求
- **音效开关**：通过 `SettingsManager.getInstance(this).shouldPlayGameSound()` 统一判断（综合音效总开关与游戏音效开关），与五子棋、斗地主等游戏一致
- **实现模式**：参照 `GomokuActivity` 的 SoundPool 成熟实现模式（`SoundPool.Builder` + `load` + `play` + `release`）
- **回调解耦**：音效播放逻辑全部在 TetrisActivity 中，TetrisView 仅暴露回调接口，不直接持有 SoundPool，保持视图与音频解耦
- **音量参数**：左右声道各 0.6f，优先级 1，速率 1.0f（与 GomokuActivity 落子音效参数一致）

### 验证结果

- ✅ Tetris 代码无编译错误（编译器未报告任何 Tetris 文件错误）
- ✅ 改动遵循 GomokuActivity 成熟模式
- ⚠️ 项目整体编译存在预存错误（ChineseChessView.java、Game2048Activity.java 的未提交改动导致，与本次 Tetris 改动无关，超出任务范围）

### 回滚方法

```bash
git checkout HEAD -- app/src/main/java/com/gamecenter/app/games/tetris/TetrisActivity.java app/src/main/java/com/gamecenter/app/games/tetris/TetrisView.java
```

### 影响范围

- 仅俄罗斯方块游戏（tetris 包）
- 不影响其他游戏模块
- 不影响游戏逻辑（仅新增音效播放）
- 未新增资源文件

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否（仅新增音效反馈，游戏逻辑未变）
- **替代方案**: 无

---

## 循环 16：中国象棋 UI 优化 + 难度选择功能（2026-06-23）

### 修改目标

- 优化中国象棋画面 UI（棋盘绘制升级、棋子立体感、坐标标注、移动提示等）
- 新增难度选择功能（进入游戏先选难度，再开始对弈）
- 新增悔棋、重新开始、返回键确认等功能
- AI 思考状态可视化（棋盘顶部显示"AI 思考中…"指示）

### 实际修改文件列表

#### 1. Java/Kotlin 源文件（3 个）

1. **app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessView.java**
   - 颜色全部资源化（`color(R.color.chinese_chess_*)`，支持深浅主题）
   - 棋子绘制升级：RadialGradient 径向渐变背景 + ShadowLayer 阴影 + 双圈边框
   - 新增"楚河汉界"文字绘制
   - 新增列号坐标（顶部黑方视角 1-9，底部红方视角 9-1）
   - 选中棋子高亮 + `computeValidMoves()` 计算可移动位置提示（绿点/空心圆吃子提示）
   - 最后一手标记（`lastMoveToRow/Col` 红色圆点）
   - AI 思考指示（`setAiThinking(boolean)` 方法，顶部绘制"AI 思考中…"圆角背景）
   - AI 思考时禁止触摸操作
   - 新增走法历史栈 `moveHistory`，记录每步走法（fromRow, fromCol, toRow, toCol, capturedPiece）
   - 新增 `undoMove()` 方法：撤销最近 1-2 步（玩家+AI），还原棋盘状态和最后一手标记
   - 硬编码"AI 思考中…"改为 `R.string.chinese_chess_ai_thinking` 字符串资源

2. **app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessActivity.java**（完全重写）
   - 使用 `activity_chinesechess.xml` 自定义布局替代 BaseGameActivity 默认 FrameLayout 容器
   - 新增难度选择 UI 逻辑：4 个难度按钮（简单/普通/困难/大师），点击选中高亮
   - 进入游戏先显示难度选择面板，隐藏棋盘；点击"开始对弈"后切换到棋盘视图
   - 未选难度时点击"开始对弈"提示"请先选择难度"
   - 新增悔棋按钮：调用 `chessView.undoMove()`，AI 思考时禁止悔棋
   - 新增重新开始按钮：重置棋盘，取消 AI 计算
   - 新增返回键确认对话框（"确定要退出当前对局吗？"）
   - AI 思考状态联动：`handlePlayerMove` 时 `chessView.setAiThinking(true)`，AI 落子后 `setAiThinking(false)`
   - 游戏结束 Toast 提示（获胜/失败）
   - 难度切换 Toast 提示（使用字符串资源格式化）

3. **app/src/main/java/com/gamecenter/app/games/base/BaseGameActivity.java**
   - 无修改（仅参考难度选择框架）

#### 2. 布局文件（1 个新增）

4. **app/src/main/res/layout/activity_chinesechess.xml**（新增）
   - 浅色木纹卡片风格（与五子棋主页风格一致）
   - 顶部：棋盘视图（初始 `visibility="gone"`）
   - 中部：难度选择面板（ScrollView，初始可见）
     - 中央卡片：标题"中国象棋" + 副标题"楚河汉界，将帅对弈"
     - 2×2 难度宫格：简单/普通/困难/大师，每格含星级图标+名称+描述
     - "开始对弈"按钮（深棕渐变背景）
   - 底部：控制面板（初始 `visibility="gone"`）
     - 悔棋按钮 + 重新开始按钮（横向排列）

#### 3. 资源文件（4 类，共 15 个）

5. **app/src/main/res/values/colors.xml**
   - 新增 12 条象棋棋盘配色（浅色主题）：`chinese_chess_board_bg` 等
   - 新增 17 条象棋主页配色（浅色主题）：`chess_main_bg`、`chess_card_bg` 等

6. **app/src/main/res/values-night/colors.xml**
   - 新增 12 条象棋棋盘配色（深色主题）
   - 新增 17 条象棋主页配色（深色主题）

7. **app/src/main/res/values/strings.xml**
   - 新增 22 条象棋相关字符串：`chinese_chess_title`、`chinese_chess_difficulty_*` 等

8. **app/src/main/res/values-en/strings.xml**
   - 新增 22 条象棋相关英文字符串

9. **app/src/main/res/drawable/chess_main_bg.xml**（新增）
   - 浅米色渐变背景

10. **app/src/main/res/drawable/chess_main_card_bg.xml**（新增）
    - 木纹渐变 + 圆角 + 边框卡片背景

11. **app/src/main/res/drawable/chess_difficulty_selector.xml**（新增）
    - 难度按钮 selector：选中深棕填充，未选中浅棕填充

12. **app/src/main/res/drawable/chess_start_btn_bg.xml**（新增）
    - 深棕渐变开始按钮背景

13. **app/src/main/res/drawable/chess_control_btn_bg.xml**（新增）
    - 深棕填充控制按钮背景

14. **app/src/main/res/drawable/chess_stars_1.xml** ~ **chess_stars_4.xml**（4 个新增）
    - 难度星级图标（1-4 星，黄色填充+灰色空星）

#### 4. 配置文件（1 个）

15. **version.properties**
    - `versionCode` 486 → 487（编译后自动 bump 到 488）

### 技术说明

- **UI 风格统一**：象棋主页采用与五子棋一致的浅色木纹卡片风格，保持应用内游戏主页视觉一致性
- **颜色资源化**：所有颜色通过 `getContext().getColor(resId)` 加载，自动适配深浅主题
- **字符串本地化**：所有新增文本已迁移到 `strings.xml`，并提供 `values-en/strings.xml` 英文翻译
- **悔棋实现**：使用 `List<int[]>` 走法历史栈，每步记录 `{fromRow, fromCol, toRow, toCol, capturedPiece}`，撤销时还原棋子位置和被吃棋子
- **AI 思考状态联动**：Activity 设置 `chessView.setAiThinking(true/false)`，View 在 `onDraw` 中绘制思考指示，并在 `onTouchEvent` 中阻止 AI 思考时的触摸操作
- **难度选择框架**：复用 BaseGameActivity 的 `currentDifficultyIndex` 和 `getDifficultyLevels()`，但使用自定义 UI 替代默认对话框
- **布局切换**：通过 `setVisibility(View.GONE/VISIBLE)` 切换难度面板和棋盘视图，避免 Activity 跳转

### 验证结果

- ✅ 编译成功（`assembleDebug` BUILD SUCCESSFUL，版本号 487→488）
- ✅ 安装到蓝叠模拟器成功（Tiramisu64 实例，ADB 端口 5555）
- ✅ 主页 UI 正确显示：标题/副标题/4 个难度卡片/开始按钮
- ✅ 点击"开始对弈"后棋盘正确显示，控制面板（悔棋/重新开始）可见
- ✅ 悔棋按钮点击无崩溃
- ✅ 重新开始按钮点击无崩溃
- ✅ 返回键确认对话框正确显示（"确定要退出当前对局吗？" + "继续对局"/"退出"）
- ✅ Monkey 测试通过（500 + 300 事件，无崩溃/ANR）
- ✅ 应用进程稳定（PID 3268 全程未变）

### 回滚方法

```bash
# 回滚所有修改
git checkout HEAD -- app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessView.java
git checkout HEAD -- app/src/main/java/com/gamecenter/app/games/chinesechess/ChineseChessActivity.java
git checkout HEAD -- app/src/main/res/values/colors.xml
git checkout HEAD -- app/src/main/res/values-night/colors.xml
git checkout HEAD -- app/src/main/res/values/strings.xml
git checkout HEAD -- app/src/main/res/values-en/strings.xml
git checkout HEAD -- version.properties

# 删除新增文件
rm app/src/main/res/layout/activity_chinesechess.xml
rm app/src/main/res/drawable/chess_main_bg.xml
rm app/src/main/res/drawable/chess_main_card_bg.xml
rm app/src/main/res/drawable/chess_difficulty_selector.xml
rm app/src/main/res/drawable/chess_start_btn_bg.xml
rm app/src/main/res/drawable/chess_control_btn_bg.xml
rm app/src/main/res/drawable/chess_stars_1.xml
rm app/src/main/res/drawable/chess_stars_2.xml
rm app/src/main/res/drawable/chess_stars_3.xml
rm app/src/main/res/drawable/chess_stars_4.xml
```

### 影响范围

- 仅中国象棋游戏（chinesechess 包）
- 不影响其他游戏模块
- 新增布局文件和 drawable 资源进入正确资源构建链路
- 新增颜色和字符串资源不影响现有资源

### 风险评估

- **风险等级**: 低
- **影响启动**: 否
- **影响核心功能**: 否（仅 UI 升级和新增难度选择功能，AI 逻辑未变）
- **替代方案**: 无

---

## 下一步计划

- 生成最终报告
- 生成待删除清单
- 生成待确认操作清单


---
[🔙 返回文档索引](/docs/DOCUMENTATION_INDEX.md)
