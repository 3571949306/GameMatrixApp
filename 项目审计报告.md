# 项目审计报告 - GameMatrix App

## 1. 项目结构说明

### 1.1 项目类型
Android 游戏中心应用，采用模块化架构，支持动态加载游戏模块和功能模块。

### 1.2 目录结构
```
GameMatrixApp/
├── app/                          # 主应用壳层
│   ├── src/main/
│   │   ├── java/                 # Java 源码
│   │   ├── kotlin/               # Kotlin 源码
│   │   ├── res/                  # 资源文件
│   │   ├── assets/               # 资产文件（modules.json, game_configs.json）
│   │   └── AndroidManifest.xml   # 应用清单
│   ├── build.gradle              # 应用构建配置
│   ├── version.gradle            # 版本配置
│   └── dependencies.gradle       # 依赖配置
├── core/                         # 核心模块
│   ├── common/                   # 通用基础模块
│   ├── network/                  # 网络模块
│   ├── update/                   # 更新模块
│   ├── security/                 # 安全模块
│   ├── module-host/              # 模块宿主框架
│   ├── moduleloader/             # 模块加载引擎
│   ├── modulestore/              # 模块商店
│   └── online/                   # 联机公共模块
├── module-store/                 # 模块商店功能模块
│   └── feature/
│       ├── games/                # 游戏模块
│       │   ├── games/            # 游戏集合
│       │   ├── chinesechess/     # 中国象棋
│       │   ├── klotski/          # 华容道
│       │   └── game2048/         # 2048
│       └── tools/                # 工具模块
│           ├── browser/          # 浏览器
│           ├── tools/            # 工具箱
│           ├── ai/               # AI 助手
│           └── vpn/              # VPN
├── docs/                         # 文档
├── tools/                        # 工具脚本
└── vps/                          # VPS 部署模板
```

### 1.3 模块数量
- **核心模块**: 9 个（common, network, update, security, module-host, moduleloader, modulestore, online, app）
- **功能模块**: 7 个（browser, tools, ai, vpn, chinesechess, klotski, game2048）
- **游戏模块**: 29+ 款（内置 + 动态下载）

## 2. 技术栈分析

### 2.1 开发语言
- **Java 17**: 主要业务逻辑（60%）
- **Kotlin 2.0.21**: 新增模块和核心组件（40%）

### 2.2 框架与库
| 类别 | 技术 | 版本 |
|------|------|------|
| 构建工具 | Gradle | 8.13 |
| Android 插件 | AGP | 8.13.2 |
| 依赖注入 | Hilt | 2.57.2 |
| 数据库 | Room | 2.7.1 |
| 网络 | OkHttp | 4.12.0 |
| 图片加载 | Glide | 4.16.0 |
| JSON 解析 | Gson | 2.11.0 |
| 二维码 | ZXing | 3.5.3 |
| 静态分析 | Detekt | 1.23.7 |
| 代码风格 | Ktlint | 12.1.2 |
| 测试 | JUnit | 4.13.2 |
| 内存泄漏检测 | LeakCanary | 2.14 |

### 2.3 Android SDK
- **minSdk**: 24 (Android 7.0)
- **targetSdk**: 35 (Android 15)
- **compileSdk**: 35

## 3. 启动流程分析

### 3.1 启动链路
```
SplashActivity (启动页)
    ↓
App.onCreate() (全局初始化)
    ├─ CrashDetector.markAppStart() (崩溃检测)
    ├─ applyLanguage() (语言设置)
    ├─ applyTheme() (主题设置)
    └─ registerActivityLifecycleCallbacks() (生命周期监听)
    ↓
MainActivity (主界面)
    ├─ 权限检查与申请
    ├─ 底部导航初始化
    ├─ 自动更新检查
    └─ Fragment 加载（GamesFragment, ToolsFragment, BrowserFragment）
```

### 3.2 关键初始化点
1. **Hilt 依赖注入**: `@HiltAndroidApp` 注解启动
2. **App Startup**: `NetworkInitializer` 延迟初始化 OkHttpClient
3. **模块加载**: `ModuleLoaderV2` 动态加载已安装模块
4. **更新检查**: `UpdateViewModel` 异步检查更新

## 4. 核心模块分析

### 4.1 更新分发模块 (`:core:update`)
**职责**: 应用更新检查、下载、安装

**关键类**:
- `UpdateChecker`: 更新检查器，支持多源自动切换
- `UpdateDownloader`: APK 下载器，支持速度检测和自动换源
- `UpdateInfo`: 更新信息数据模型
- `UpdateViewModel`: 生命周期安全的更新 ViewModel

**分发架构** (2026-06-19 更新):
```
优先级 1: 香港 VPS (hk-update.<DOMAIN>)
    ↓ (失败或速度慢时)
优先级 2: GitHub Releases (全球 CDN)
```

**已移除**: 美国 VPS 备用源（2026-06-19 下线）

### 4.2 模块加载模块 (`:core:moduleloader`)
**职责**: 动态加载 APK 模块

**关键类**:
- `ModuleLoaderV2`: 模块加载引擎（DexClassLoader）
- `ModuleVerifier`: 模块 SHA-256 校验
- `ModuleDownloader`: 模块下载器

**加载流程**:
1. 下载模块 APK/ZIP
2. SHA-256 校验
3. DexClassLoader 动态加载
4. 反射或 Hook 重载资源

### 4.3 模块商店模块 (`:core:modulestore`)
**职责**: 模块浏览、下载、安装、卸载

**关键类**:
- `ModuleManager`: 模块管理器
- `ModuleStoreActivity`: 模块商店界面
- `InstalledModulesActivity`: 已安装模块管理

**模块清单**: `assets/modules.json` (version 11, 33 个模块)

### 4.4 网络模块 (`:core:network`)
**职责**: HTTP/WebSocket 连接管理

**关键类**:
- `OkHttpClientProvider`: HTTP 客户端单例
- `RelayHttpClient`: Relay HTTP + WebSocket URL
- `RemoteP2PUtil`: 房间码工具类

**特性**:
- 50MB 磁盘缓存
- 自动重试 3 次（指数退避）
- 连接复用

### 4.5 安全模块 (`:core:security`)
**职责**: 证书固定、SHA-256 校验

**关键类**:
- `SecureOkHttpFactory`: 安全 OkHttp 工厂
- `SSLHelper`: SSL 信任管理

**注意**: 证书绑定已临时关闭以兼容模拟器 SIGSEGV 问题

## 5. 风险点分析

### 5.1 高风险
1. **证书绑定关闭**: `SecureOkHttpFactory` 中证书固定被禁用，存在中间人攻击风险
2. **R8 混淆关闭**: Release 构建未启用 R8，APK 体积大且易被反编译
3. **美国 VPS 残留配置**: `server.url.fallback` 配置项已废弃但保留，可能引起混淆

### 5.2 中风险
1. **ABI Splits 关闭**: 测试阶段关闭了 ABI 拆分，导致 APK 包含 arm64-v8a + x86_64，体积增大 75%
2. **Lint 宽松模式**: `abortOnError false` 和 `warningsAsErrors false` 可能导致问题积累
3. **模块商店动态加载**: DexClassLoader 动态加载存在兼容性和安全风险

### 5.3 低风险
1. **Java/Kotlin 混合**: 项目 60% Java + 40% Kotlin，边界需要规范
2. **测试覆盖不足**: 437+ 测试用例，但部分核心模块缺少测试
3. **文档分散**: 文档分布在多个 MD 文件，缺少统一索引

## 6. 构建与测试命令

### 6.1 构建命令
```bash
# Debug 构建
.\gradlew.bat :app:assembleDebug

# Release 构建（需要 keystore）
.\gradlew.bat :app:assembleRelease

# 完整发布（VPS + GitHub）
.\gradlew.bat :app:buildAndUploadToVpsAndGitHub -PupdateChannel=stable
```

### 6.2 测试命令
```bash
# 运行所有单元测试
.\gradlew.bat :app:test

# 运行特定测试
.\gradlew.bat :app:testDebugUnitTest --tests "com.gamecenter.app.games.gomoku.GomokuGameTest"

# Lint 检查
.\gradlew.bat :app:lintDebug
```

### 6.3 当前构建状态
- **构建结果**: ✅ BUILD SUCCESSFUL (1m 14s)
- **版本**: versionCode=451, versionName=1.4.0
- **测试**: 15+ 个测试文件，437+ 个测试用例

## 7. 配置文件说明

### 7.1 local.properties
```properties
# 主更新源（必填）
server.url=https://hk-update.<YOUR_DOMAIN>

# WebSocket Relay（云联机）
ws.url=wss://hk-ws.<YOUR_DOMAIN>/ddz-ws

# HTTP Relay（云联机）
relay.url=https://hk-relay.<YOUR_DOMAIN>/api/ddz-relay

# 反馈服务（可选）
feedback.url=https://<YOUR_DOMAIN>/api/feedback

# MiMo TTS API Key（可选）
mimo.api.key=你的key
```

### 7.2 version.properties
```properties
versionCode=451
versionName=1.4.0
lastStableVersionCode=267
lastStableVersionName=1.3.29
betaNoticeVersionGap=3
```

### 7.3 BuildConfig 字段
- `SERVER_URL`: 主更新源 URL
- `SERVER_URL_FALLBACK`: 备用更新源（已废弃，保留空值）
- `MODULES_URL`: 模块清单 URL
- `DOWNLOAD_BASE_URL`: 模块下载基础 URL
- `MODULE_HOST`: 模块主机名
- `MODULE_FALLBACK_HOST`: 模块备用主机名（已废弃）

## 8. 项目优势

1. **模块化架构**: 清晰的模块划分，支持动态加载
2. **多级分发**: 香港 VPS + GitHub Releases 双源保障
3. **自动换源**: 下载速度慢时自动切换源
4. **依赖注入**: Hilt 管理依赖，代码可测试性好
5. **协程支持**: UpdateViewModel 使用协程，生命周期安全
6. **安全加固**: allowBackup=false，备份规则配置
7. **CI/CD**: GitHub Actions 自动构建和测试

## 9. 项目劣势

1. **证书绑定关闭**: 安全风险
2. **R8 混淆关闭**: APK 体积大，易被反编译
3. **Java/Kotlin 混合**: 代码风格不统一
4. **文档分散**: 缺少统一文档索引
5. **测试覆盖不足**: 部分核心模块缺少测试
6. **Lint 宽松模式**: 问题可能积累

## 10. 审计结论

项目整体架构清晰，模块化设计合理，分发机制完善。主要问题集中在安全配置（证书绑定、R8 混淆）和代码质量（Lint 宽松、测试覆盖不足）。建议优先修复安全相关配置，逐步提升代码质量和测试覆盖率。

**审计时间**: 2026-06-19
**审计版本**: versionCode=451, versionName=1.4.0
