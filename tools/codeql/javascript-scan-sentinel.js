/*
 * This file deliberately has no runtime role.
 *
 * CodeQL treats a JavaScript/TypeScript analysis with no valid source files
 * as a configuration error. Keeping one harmless, valid source file lets the
 * repository continue to scan this language after the retired browser HTML
 * asset was removed, so stale alerts are closed and future JS remains covered.
 */
void 0;
