# GameMatrixApp 代码质量提升计划

> 创建：2026-08-30 · v2 修订：19:55（四路子代理交叉验证后重写）
> v3 修订（2026-08-30）：用户拍板三项决策——优先级由执行 agent 自主选择、CI 时长预算通过、覆盖率锁 70%（核心域口径，排除硬 UI）
> **v4 修订（2026-08-30 晚）**：独立健康审查后增补——§〇.3 前提再被推翻（插队逻辑已激活）、新增 §〇-A 四条未覆盖问题、§二两项完成状态更新、chess-ai-hint 归档决策。详见 §〇-B。
> **v5 修订（2026-08-30 晚）**：新一轮子代理交叉验证——规范执行 ✅、测试 ✅ 两路完成并回写 §一/§三；安全、CI/整体两路因子代理并发限额未派出（仅做轻量抽查）。事实修正与机制升级详见 §〇-C。
> 基线评分：架构 85 / 文档 88 / 代码 60 / 安全 45 / 测试 20
> 目标不是满分，是**改任何一行代码都不怕炸**。
> 用法：每完成一项，把 `- [ ]` 改成 `- [x]`，并补上完成日期。
> **执行顺序由执行 agent 自主选择**，下文「依赖」仅标注技术上的客观先后。
> 当前工作分支：`feature/dl-i18n-preinstall`（数据层英文化 + 出厂预装首启安装已在此分支，PR #41 已合入 main；评审六缺口修复续后）

---

## 〇、交叉验证结论：原计划被推翻的 5 个前提

四路子代理独立核查后，v1 计划有 5 处事实错误。修订如下，避免照 v1 执行踩坑。

| # | v1 的错误前提 | 实测真相 |
|---|---|---|
| 1 | 公钥配在 `local.properties` 的 `catalog.public.keys` | **该键不存在**。`build.gradle:41-61` 只认 server/relay/ws/feedback/mimo/dl.mirrors 六个键。真实入口是 `-PcatalogEd25519PublicKeys=` 或环境变量 `GAME_MATRIX_CATALOG_ED25519_PUBLIC_KEYS` |
| 2 | 生产验签在降级运行 | **并非如此**。`signed-release.yml:114` 已注入 Secret，`assembleRelease` 现在就在失败，靠 `ciUnsignedRelease` 绕过。真因是 **GitHub Secret 是否真填了值**，不是代码没做 |
| 3 | 镜像插队 bug 影响用户 | ~~当前是死代码~~ **v4 推翻：插队逻辑已激活**。实测当前 assets 双清单 34 条 downloadUrl 主机全部是 `hk-update.tcp888.uk:2083`（2026-08-30 晚 catalog 对齐生产 v16 操作的结果），与 `DOWNLOAD_BASE_URL` 完全同源，`startsWith=true`。且 v2 实现已是"测速胜者首→JP→HK→US"正确顺序，**§二"修插队顺序"项关闭**。tcp0053.shop 的 31 处残留分布在 iconUrl/fallbackUrl 等非 downloadUrl 字段 |
| 4 | 需要新增 `catalogVersion` 字段 | **字段已存在**（值 16），客户端已在解析。无需改生成脚本和签名脚本 |
| 5 | javac 绕行测试能拉高覆盖率 | **不能。贡献为 0**。它不挂 JaCoCo agent，也不在 Gradle classDirectories 内。测试质量提升 ✅、覆盖率数字 +0 ❌ |

**关键耦合（v4 修订）**：~~"清死域会立刻激活插队 bug"~~ **作废**——插队逻辑已激活且顺序正确（胜者首）。清死域（tcp0053 残留字段清理）可独立执行，无同批耦合约束。

---

## 〇-B、独立健康审查增补（2026-08-30 晚，四项未覆盖问题 + 两项已确认）

以下问题在本对话的健康审查/分发实施中发现，v3 未覆盖：

- [x] **P1 预装安装与核心预加载并发竞态**（✅ 2026-08-30）：Splash 同一线程池串行——install 先跑完、preload 排其后，消除同目录并发窗口
- [x] **P1 `parseModulesArray` 对 Catalog V2 对象格式返回 null**（✅ 2026-08-30）：V2 分支（JSONObject→getJSONArray("modules")）已上提为正式实现，`bundledVersionCodeOf`/`loadModuleList`/SP 缓存分支全部调用点生效
- [x] **P2 `/admin/feedback` nginx 公网暴露**（✅ 2026-08-30：HK 07-hk-update-uk.conf 该 location 已加 allow 127.0.0.1/::1/100.64.0.0/10 + deny all（tailnet 实测存在），nginx -t 通过并 reload；公网 curl 实测 403。附带发现：feedback 后端 9011 未监听（502 为既有故障）且 local.properties feedback.url 仍指死域 tcp0053.shop，已单独上报）：token 已轮换 ✓ 但入口仍公网。nginx 加 `allow tailnet/127.0.0.1; deny all`
- [x] **P2 `SourceTestStore.append` 非原子写**（✅ 2026-08-30）：已实现 tmp+rename，含 rename 失败兜底
- [ ] **P2 `keystore.properties` 明文密码**（旧待修复清单 P1#8）：gitignore ✓ 但磁盘明文，迁环境变量
- [x] **chess-ai-hint 分支处置已定**：基于 2026-06 旧代码（gradle.properties 含本地路径硬编码，CI 必挂），合并会回退 main 几十项修复 → **已关闭 PR #42、删远端分支、归档不合并**。其"AI 提示系统"演进已在 main chinesechess 模块 v300 完成

**已确认无恙**：GitHub Contributors API 复核 = **仅 3571949306 一个贡献者**（无 Claude）；提交作者/拖尾无 claude 归属；全仓 "claude" 字样仅 2 处惰性文本（.gitignore 忽略规则 + 旧提交信息里的已删除文件路径）。

---

## 〇-C、v5 交叉验证修正（规范执行 ✅ / 测试 ✅ 两路完成；安全、CI 两路待补）

> 验证方式：两个独立子代理只读核查；安全、CI 两路因子代理并发限额未派出，仅主 agent 轻量抽查。以下修正已回写 §一 / §三 原文。

| # | 原断言 | v5 实测 |
|---|---|---|
| 1 | `verify_isolation.py` 其余 8 条 SOFT | 实为 **7 条 SOFT**（:114,125,144,165,190,208,231）；2 条 HARD（:83,:99）属实；:268 无条件 `sys.exit(0)` 确认 WARN 不阻断 |
| 2 | docs/ 11 份文档 | **10 份 md（2,969 行精确）** + 1 份 `pending_delete_game_apks.txt` |
| 3 | androidTest 17 个 | **19 个文件** = 16 个测试类 + 3 个辅助（BaseAndroidTest/EmulatorTestBase/GameTestHelper），仍全部不进 CI |
| 4 | core 1.9 万行 | 实测 **20,646 行**（28 个游戏模块 46,877 行属实） |
| 5 | 96.4% 的行 import android | **不可复现**：实测含 android import 的行 80.0%、文件级 87.8%；"71.1% 硬 UI" 是口径依赖（严格目录排除 42.8% ~ 连平台粘合全排 ~86%）——**核心域分母必须由真实 excludes 反推，16,594 只是估算** |
| 6 | ci.yml:96 死路径 → artifact 必空 | :96 死 glob 属实，但 **:95 的 `coverage/**` 已覆盖 AGP 真实输出**；真问题是 **:87 的 `|| true` 掩盖失败** |
| 7 | JaCoCo "只统计 :app" 的归因 | `jacoco.gradle` 给所有子项目开了 `enableUnitTestCoverage`，并未限制 :app；"只有 :app"来自 CI 只跑 `:app:createDebugUnitTestCoverageReport`（ci.yml:87）+ AGP 报告默认只含本模块类。**AGP 已产出 exec，无需自配 agent** |
| 8 | （新发现） | **`verify_ai_parity.py` 前提已失效**：宿主副本已删（commit 0c52cb9）、配对缺失仅 WARN——直接接 CI = 接入新假绿灯，必须先修 |
| 9 | （新发现） | **`/.claude/` 被 `.gitignore:244` 忽略**（连同 `.agents/`、`.codex/`）：settings.json 根本提交不了，hook 方案必须先改 .gitignore |
| 10 | 25/28 build.gradle 字节级相同 | 归一化（剥 namespace/applicationId/versionCode/versionName 4 行 + 行尾差异）后恰为 **25/28 完全相同**，变体仅 doudizhu/td/klotski；"仅 td 配 testImplementation"在 28 个游戏模块内属实（全仓另有 14 处有） |
| 11 | 核心域 25–40 人天 | **乐观下界**：0.46 测试/代码比偏乐观；核心域能裸 JVM 跑的仅约 3,021 行，其余需 Robolectric/mock → **现实 40–60 人天** |
| 12 | （抽查补充） | 全仓 grep `tcp0053.shop` 共 **79 处**——口径大于 〇.3 的 assets 31 处，多出部分在脚本/文档/本计划自身；清死域任务仍未勾选 |

---

## 一、规范执行专项

### 为什么值得做

用户提出的核心问题：**本地规范很多，agent 开发时几乎不遵守**。交叉验证确认这不是感觉，是事实：

- 规范总量 **AGENTS.md 99 行 + docs/ 2,969 行（10 份 md）**，全部靠 agent 自觉
- `.claude/` 和 `.agents/` 是**空目录**——零 hook 配置；且 `.gitignore:16,19,244` 把 `/.claude/`、`.agents/`、`.codex/` 全部 ignore，**想配 hook 连配置文件都提交不了**
- 无 pre-commit hook（`.git/hooks/` 全是 .sample）
- 14 个 verify 脚本全在 `scripts/`，**CI 只接了 4 个**（protected_assets×2、agent_contract、chinese_chess、go）
- AGENTS.md §5 第 3 条（:59）要求"修 bug 必须加回归测试"，实测 28 个游戏模块仅 td 有 `src/test`，**25 个零测试**（chinesechess/go 只有 javac 绕行 `tests/`，不计覆盖率）

### 根因（按重要性排序）

1. **脚手架与规范冲突**（最根本）。25/28 个模块 build.gradle **归一化后完全相同**（仅差 namespace/applicationId/versionCode/versionName 四行），说明 agent 在照抄既有模块。而既有模块**只有 td 一个配了 testImplementation**，其余没有 tests/ 目录——**照抄必然产出无测试模块**。agent 不是不读规范，是脚手架引导它违反规范。
2. **规范自身失效**。`AGENTS.md:16` 和 `:76` 指向的宿主镜像路径（chinesechess / go 的 app 侧副本）**已不存在**。agent 照做会直接踩空。
3. **假绿灯**。`verify_agent_contract.py:99-102,146-147` 有"自指断言"——检查的是「AGENTS.md 文本里有没有写着 `undoCount=0` 这句话」，**检查不了任何代码**。
4. **门禁强度虚高**。`verify_isolation.py` 名义是护栏，实际只有 **2 条 HARD**（:83、:99），其余 **7 条 SOFT** WARN 不阻断（:268 还无条件 `sys.exit(0)`）。
5. **无强制机制**。没有 hook、没有 pre-commit、CI 覆盖不全。

### 任务清单

- [x] **修规范自身的失效路径**（✅ 2026-08-30：随 L95 拆分同步处理，宿主镜像/副本要求已删除）
- [x] **删自指断言**（✅ 2026-08-30：先前批次已移除全部文档文本断言，仅残留 1 个未使用变量已清理；拆分 AGENTS.md 不会再红 CI）
- [x] **先修 `verify_ai_parity.py` 的失效前提**（✅ 2026-08-30：已反转为单一真相源守卫——宿主侧副本出现即 FAIL，6 守卫类全部核实，已接 CI）
- [x] **把已写好却没接的脚本接进 CI**（✅ 2026-08-30：本地实跑 9/9 全 PASS，连同修复后的 ai_parity、新增 security_clauses、ratchet 共 12 个脚本已直挂 ci.yml）
- [x] **补齐安全条款的机器检查**（✅ 2026-08-30：新增 scripts/verify_security_clauses.py，11 项检查全 PASS，已接 CI）
- [x] **脚手架对齐规范**（✅ 2026-08-30：scripts/new_game_module.py 已建——归一化 build.gradle+testImplementation 注入+src/test 占位+tests/stubs/+统一 LF+include/注册提示，冒烟通过）——实际流程 = 复制既有模块改 4 行身份 + `settings.gradle` 手工 include + `catalog.json` 注册（受保护文件）。新建 `scripts/new_game_module.py`：以归一化基线复制，注入 `testImplementation`、生成 `src/test/<pkg>/` 占位 + `tests/stubs/`、统一 LF，并输出 include/注册提示。**这一步做完，agent 照抄出来的模块天然合规**
- [x] **拆分 AGENTS.md**（✅ 2026-08-30：根文件压成「五条铁律+路由表+verify 速查」；§2/§3/§6→chinesechess/AGENTS.md、§7→go/AGENTS.md、§8→core/module-host/AGENTS.md；失效镜像同步要求已删；verify_agent_contract 仍 PASS）：99 行压成「5 条铁律 + 路由表」；详细条款按域下沉子目录 AGENTS.md（agent 必读 = 根 + 所改目录向上的子目录 AGENTS.md）：§3/§6→`chinesechess/AGENTS.md`、§7→`go/AGENTS.md`、§8→`core/module-host/AGENTS.md`
- [x] **建立 hook 机制**（✅ 2026-08-30：gitignore 放行 settings.json（local 仍忽略）；scripts/hooks/guard.py 版本化（受保护阻断+域路由，三态实测）；.claude/settings.json 仅接线。只覆盖 Claude 系 agent，其他靠 pre-commit+CI 兜底）。hook 脚本实体放 `scripts/hooks/guard.py`（版本化、CI 可复用），`.claude/settings.json` 只做接线（PreToolUse matcher `Edit|Write|MultiEdit`，stdin JSON 取 `file_path` 路由对应 verify 脚本）。注意只覆盖 Claude 系 agent，其他 agent 靠 pre-commit + CI 兜底
- [x] **加"只降不增"计数器门禁**（✅ 2026-08-30：verify_ratchet.py + quality_baseline.json（空 catch 98 / boolean flag 84 / SOFT 7，实测口径），已接 CI）
- [x] **PR 模板加规范检查单**（✅ 2026-08-30：PR 模板已加必选检查单；required checks 属仓库设置，待管理员在 Settings→Branches 配置）

**效果**：这是**唯一能让其他所有改进不白做**的工作包。规范从"文档"变成"代码"后，后续所有阶段才有持久性。CI 时长影响：正则型脚本 0.14–0.47 秒，javac 型 1.2–2.8 秒，**可忽略**。

---

## 二、安全封堵

> v1 的三条里有两条前提错误，已按实测重写。v4：插队顺序项**已由 W2 实现关闭**（胜者首+级联，无需删 asReversed 的 cosmetics）；同批耦合警告作废。

- [x] **先查 GitHub Secret**（✅ 2026-08-30：5 个 Secret 全部存在（今日 12:19 设置）；Secret 值 GitHub 设计上不可读，是否正确由下次 signed release 运行实证）（零成本，可能直接消掉一半问题）
- [ ] 密钥链已存在（`tools/Protect-CatalogProductionKey.ps1` + `Sign-ProductionCatalog.ps1`），**不要重新造**。公钥格式是 base64(raw 32 字节)，注意 Tink `NO_WRAP` 与 `java.util.Base64` 的 padding 容忍度不同，直接原样粘贴脚本输出
- [x] ~~修 `ModuleDownloader.kt:127-141` 插队顺序~~ ✅ W2 已实现（胜者首+级联快速失败，v4 关闭此项）
- [x] **清死域 `tcp0053.shop` 31 处**（✅ 2026-08-30：catalog.json + modules.json 各 31 处全部替换为 hk-update.tcp888.uk:2083，JSON 校验通过；根因核实——`build_production_catalog.py:137` 默认值已是 hk-update:2083，产物遗留来自更早的手工生成批次，无回潮风险）
- [x] ~~两项必须同批发布~~ **作废**（v4：插队逻辑已激活且顺序正确，无"激活 bug"风险）
- [x] catalog 版本单调校验（✅ 2026-08-30：`StoreCatalogRepository.kt` 已实现——`in 1 until lastVersion` 即仅严格更小才拒、相等放行，schemaVersion≥2 门控）
- [ ] 签名内嵌 JSON（消除 nginx header 单点）——可行为但代价中高，**降为本轮可选**

**效果**：安全 **45 → 85**。依赖：修插队顺序与清死域必须同批。

---

## 三、测试

> 已拍板：**锁 70%，口径 = 排除硬 UI 的核心域**。全量口径需 250–400 人天，不现实，弃用。

### 先认清现状

- `core` 实测 **20,646 行** + 28 个游戏模块 46,877 行**都不在分母里**——`jacoco.gradle` 给所有子项目开了 `enableUnitTestCoverage`，但 CI 只跑 `:app:createDebugUnitTestCoverageReport`（ci.yml:87），且 AGP 报告默认只含本模块类
- `app` 57,473 行（精确）；硬 UI 占比**口径依赖**（严格目录排除 42.8% ~ 连平台粘合全排 ~86%，71.1% 是其中一种）；含 android import 的行 80.0%、文件级 87.8%
- 无历史覆盖率数据（`app/build/reports/` 下没有 coverage 目录，该任务本地从未成功产出）
- CI 里 `ci.yml:96` 的 `app/build/reports/jacoco/**` 是死 glob，但 **`:95` 的 `coverage/**` 已覆盖 AGP 真实输出**，artifact 并非必空——**真问题是 `:87` 的 `|| true` 掩盖覆盖率任务失败**
- 测试实际是 `app/src/test` **36 个有效文件 / 5,284 行（两者精确）**；androidTest **19 个文件 = 16 测试类 + 3 辅助**
- **16 个 androidTest 测试类从不在 CI 跑**（workflows 无任何 emulator/connectedCheck 痕迹）——这是比覆盖率更真的漏洞

### 70% 锁在哪个分母上（已定：核心域）

| 口径 | 分母 | 需新增测试 | 工作量 | 判断 |
|---|---|---|---|---|
| 全量 app | 57,473 行 | 约 3.5 万行（含 2.9 万 UI） | **250–400 人天** | ❌ 弃用（不现实） |
| **排除硬 UI 后的核心域** | 16,594 行（**估算值**：纯 JVM 下限 3,021 ~ 宽口径上限 33,010，最终由真实 excludes 反推） | 约 7,600 行 | **40–60 人天（v5 修正；25–40 是乐观下界）** | ✅ **已选定** |

**口径正当性**：排除 UI 是正当工程实践，不是作弊——但必须满足三个前提，否则就是自欺：
1. excludes 收敛进独立文件 `config/jacoco-excludes.txt` 并冻结；`.github/CODEOWNERS` 对该文件指定 owner + 分支保护勾选 **Require review from Code Owners**（原生不可绕），CI diff 脚本作辅助——excludes 散在 build.gradle 里时 CODEOWNERS 不可靠，因为触碰面太大
2. 被排除的 40,879 行必须有替代保障
3. 先做增量门禁（patch 覆盖），别一上来锁全量

### 任务清单

- [x] **先跑一次真实覆盖率，测出基线**（✅ 2026-08-30：实测 LINE 12.03%，排除后分母 8,887 行；远低于 v1 估算，起步阈值定为 10%）
- [x] **摘掉覆盖率步骤的 `|| true`**（✅ 2026-08-30：根因核实——createDebugUnitTestCoverageReport 因未开启 enableUnitTestCoverage 根本不存在；已替换为真实存在的 jacocoTestReport + jacocoTestCoverageVerification，无 || true）
- [x] 写 `jacocoTestReport` + `jacocoTestCoverageVerification`（✅ 2026-08-30：已按下方思路落地 app/build.gradle，并实测基线校准阈值为 0.10 而非 0.40）。**AGP `enableUnitTestCoverage` 已产出 exec（`build/outputs/unit_test_code_coverage/`），无需自配 agent**，直接消费：

```groovy
// app/build.gradle；uiExcludes 逐行读自 config/jacoco-excludes.txt
def uiExcludes = ['**/*Activity*','**/*Fragment*','**/*Adapter*','**/*Dialog*',
    '**/*Binding*','**/R$*','**/BuildConfig*','**/*Application*',
    'com/gamecenter/app/browser/**','com/gamecenter/app/ui/**','com/gamecenter/app/views/**']
tasks.register('jacocoTestReport', JacocoReport) {
    dependsOn 'testDebugUnitTest'
    classDirectories.setFrom files(
        fileTree("${buildDir}/intermediates/javac/debug/classes", excludes: uiExcludes),
        fileTree("${buildDir}/tmp/kotlin-classes/debug", excludes: uiExcludes))
    executionData.setFrom fileTree(buildDir, includes: ['**/*.exec'])
}
tasks.register('jacocoTestCoverageVerification', JacocoCoverageVerification) {
    classDirectories = tasks.jacocoTestReport.classDirectories
    executionData = tasks.jacocoTestReport.executionData
    violationRules { rule { limit { counter = 'LINE'; value = 'COVEREDRATIO'; minimum = 0.40 } } } // 起步值待基线，爬坡至 0.70
}
```

- [x] 设 excludes 掉硬 UI，对核心域设阈值（✅ 2026-08-30：excludes 覆盖 UI/生成代码；实测基线 12.03% 后起步阈值定 0.10（防抖红），季度爬坡至 70%，-PcoverageMinRatio 可覆盖）
- [x] 把 **`./gradlew testDebugUnitTest`** 接进 CI（✅ 2026-08-30：CI 已跑 :app + :core:adb/common/network/update 四个有测试的模块，debug 变体口径；本地全绿）
- [x] **把 androidTest 接进 CI**（✅ 2026-08-30：androidtest job 已加，API 34 + KVM + emulator-runner（SHA 固定），continue-on-error 软门禁）
- [ ] javac 绕行模式补模块回归测试（**注意：提升质量，不计入覆盖率**）：gomoku 1–1.5 人天、doudizhu 3–5 人天
  - 坑 1：`GomokuAI.java:112` 用 `new Random()` 无种子，`:815` 从 top-N 随机选——断言落子坐标必然 flaky，只能测 master 难度或断言不变量
  - 坑 2：难度预算最高 5000ms，30 个用例 = 150 秒纯搜索
  - 坑 3：抄 `chinesechess/tests/stubs/GameAI.java` 会编译失败——它只声明了 `isThinking()`，而 GomokuAI 有两个 `@Override` 方法
  - 坑 4：doudizhu 另有 ~1,491 行关键代码不在"纯逻辑"统计内（`DouDiZhuProtocol` 需 stub `org.json.*`、`DouDiZhuSeatManager` 需 stub Kotlin object `ModuleScopedPreferences`）

**效果**：测试 **20 → 65**。覆盖率按已定口径（核心域，终值 70%）达成。

---

## 四、代码减债

> 交叉验证结论：**v1 里两项是纯代码洁癖，不建议做**。

- [x] **删 2 个零引用 feature flag**（✅ 2026-08-30：`GAME_REVAMP_2026`、`ENABLE_P4_DYNAMIC_NAVIGATION` 已从 app/build.gradle 删除，全仓零代码引用已核实）
- [x] **加 CI 检查禁止新增 `buildConfigField boolean`**（✅ 2026-08-30：并入 verify_ratchet.py 的 buildconfig_boolean_flag_count 指标，超基线即红）
- [ ] **❌ 不要清理 60+ 个 flag**——实测 81 个字面量 flag 中 **79 个仍被活跃读取，共 190 处**。删除是 190 次带语义判断的手工编辑，落在零测试代码上，收益只是"gradle 少 81 行"。**判定：不值得**
- [ ] **❌ 不要全修 151 处空 catch**——实测 144 处，关键路径（模块加载/SHA/签名/事务安装）**零空 catch**；命中的 5 处都在测试辅助函数或 UI 容错。全修收益近零，反而在零覆盖文件里引入 144 个新行为。**只做"只降不增"计数器门禁**
- [ ] 人工过一遍 `BrowserWebViewPool`(6 处) 和 `AdbEngine`(10 处)——吞的是资源池与设备连接异常，排查线上问题时最缺日志
- [x] **删 `core/moduleloader/ModuleVerifier.java`（340 行死代码）**（✅ 2026-08-30：已删除；`:core:moduleloader` 模块保留——ModuleContextHelper 与依赖方仍在）
- [x] 修 `ModuleSignatureVerifier.kt:14/27/54` 三处过时注释（✅ 2026-08-30：随 ModuleVerifier.java 删除同步重写，指向 app 层 ModuleVerifier.verifySha256）
- [ ] **拆 `BrowserFragment`（2614 行）**——更稳妥的路径：① 先做方法区物理分段 + 分区注释（零行为变更）② 填实 `BrowserFragmentDelegates.java:18-22` 那个空占位（按播放器桥接 / 查找-书签-阅读列表 / 权限与地理定位三个簇逐个来）③ 大拆推迟到有测试兜底后

**效果**：代码 60 → 70（去掉洁癖项后收益变小，但风险也小）。

---

## 五、设计决策

- [x] 证书 pin 远程轮换 + kill switch 方案设计（✅ 2026-08-30：docs/证书钉扎轮换与熔断方案设计_2026-08-30.md——三级 pin 保留+双 Leaf 轮换流程+分级降级熔断（PIN_FAILURE→滑源→GitHub 兜底仍验签），远程 pin 配置通道判定本轮不做）
- [ ] 方案拍板后实施

**效果**：消除「LE 换中级 → 全量 Release 用户砖化，而修复升级本身走被 pin 的链路」的死锁。

---

## 决策点（已拍板 / 待拍板）

| # | 事项 | 状态 |
|---|---|---|
| 1 | CI 时长预算 | ✅ 可接受。补充：新增 verify 脚本仅增加 0.14–2.8 秒，瓶颈在 Gradle 侧，与本项目无关 |
| 2 | 覆盖率阈值 | ✅ 锁 70%，口径 = 排除硬 UI 的核心域（分母由真实 excludes 反推；**现实 40–60 人天**，v5 修正）。全量口径不现实，弃用 |
| 3 | 优先级 | ✅ 无需理会，执行 agent 自主选择 |
| 4 | 起步阈值与爬坡节奏（终值 70% 已锁） | ⏳ 待拍板。v5 结论：**必须先跑基线**——分母由 excludes 政策决定，没有真实数字定不了起步值 |
| 5 | 签名内嵌 JSON 做不做 | ⏳ 建议本轮不做（代价中高，收益是消除 nginx 单点） |
| 6 | v5 交叉验证完成度 | ⚠️ 规范执行、测试两路完成并已回写；**安全、CI/整体两路因子代理并发限额未跑**（其事实层 v2 已验过一轮），并发空闲后补验 |

---

## 开工前置

> **v5 快照（2026-08-30 晚，只读抽查）**：当前分支已是 `main`（PR #41 已合入），未提交文件 **39 个**（catalog.json、modules.json + 一批重签名模块 APK）——下条第 1 行的分支名与 37 已过时，以实际为准。另有并行执行 agent 在改代码，复选框勾选权归执行方。

- [x] 理清 37 个未提交文件（✅ 已由 PR #41 流程消化：该分支已合并、远端已删；当前 main 工作区的 APK/清单哈希更新属**发布批次在途改动**，按 L227 与质量提交隔离，不混入本轮）
- [x] 发布批次隔离（✅ 2026-08-30：本轮质量提交不包含 catalog.json/modules.json/APK/version.properties；死域清理已落在工作区产物中，随下次发布批次一并入库）

## 风险登记

- ~~**清死域与修插队顺序必须同批**~~ **作废（v4）**：插队逻辑已激活且顺序正确（胜者首），清死域可独立执行
- **配错公钥会 100% 挂掉所有构建**（含 debug）。`build.gradle:201-216` 的格式校验是**无条件 throw**，不区分变体
- **配了公钥后现有未签名 catalog 不会崩溃，而是静默回落缓存/assets**——远程目录永久失效。必须配套把"生成→签名→部署→公网回读"串成发布门禁
- **gomoku 测试 flaky**：AI 用无种子 `Random`，需改断言策略
- **拆 AGENTS.md 前必须先删 `verify_agent_contract.py` 三处自指断言**（v5），否则文本一变 CI 必红
- **hook 方案前置**（v5）：`/.claude/` 被 `.gitignore:244` 忽略，不改 ignore 则 settings.json 落地即失效
