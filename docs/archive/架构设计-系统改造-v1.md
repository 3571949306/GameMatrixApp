# 架构设计 - GameCenterApp 系统改�?v1.0

> **文档类型**: 系统架构设计  
> **创建日期**: 2026-05-25  
> **架构�?*: 高见�?(Gao)  
> **对应 PRD**: `文档/PRD-系统改�?v1.md`  
> **技术栈**: Android (Java + Kotlin), RePlugin, Hilt, OkHttp, WebSocket

---

## 一、实现方�?+ 框架选型

### 1.1 核心架构决策

#### 决策 1：模块格式选择 �?**RePlugin 插件化框�?*

**选项对比**�?
| 选项 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **A: Dynamic Feature Modules** | 官方支持、资源自动管�?| 依赖 Play Store、中国用户无法访�?| Google Play 市场应用 |
| **B: 独立 APK** | 简单直接、完全隔�?| 用户体验差（跳转 APP）、无法共享数�?| 完全独立应用 |
| **C: RePlugin** �?| 完整 APK 加载、资�?so 支持、成熟稳�?| 需要适配 Android 新版�?| 国内 Android 应用 |
| **D: VirtualAPK** | 轻量级、携程开�?| 已停止维护、兼容性差 | 不推�?|

**选择理由**�?1. **完整 APK 支持**：RePlugin 支持加载包含 Activity、Service、Receiver、Provider 的完�?APK，满�?下载完整游戏"需�?2. **资源加载能力**：当�?dex 方案无法加载资源，RePlugin 支持 R 资源访问
3. **成熟稳定**�?60 公司开源，�?50+ 款应用、数亿设备上验证
4. **不依�?Play Store**：适合国内应用市场分发
5. **向后兼容**：支�?Android 4.0 - 14.0

**技术风�?*�?- Android 15+ 适配可能需要等待官方更�?- 插件化框架的学习成本和调试复杂度

---

#### 决策 2：内置游戏更新策�?�?**版本号判�?+ ClassLoader 优先�?*

**方案对比**�?
| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **A: 版本号判�?* | 比较内置版本 vs 商店版本，加载更高版�?| 逻辑清晰、易调试 | 需要维护版本号 |
| **B: ClassLoader 优先�?* | 优先加载外置插件，失败回退内置 | 自动化、无需版本判断 | 调试困难、类冲突风险 |

**选择：组合方�?A + B**
- **主逻辑**：版本号判断（明确、可控）
- **兜底机制**：ClassLoader 优先级（防止版本判断失效�?
**伪代码实�?*�?```kotlin
// ModuleManager.kt
fun loadGameModule(gameId: String): Boolean {
    val builtInVersion = getBuiltInVersion(gameId)  // �?assets/modules.json 读取
    val storeModule = getInstalledModuleFromStore(gameId)  // 从模块商店读�?    
    return if (storeModule != null && storeModule.versionCode > builtInVersion) {
        // 优先加载商店下载的版�?        loadExternalModule(storeModule)
    } else {
        // 回退到内置版�?        loadBuiltInModule(gameId)
    }
}

// ClassLoader 优先级（兜底�?class GameModuleClassLoader(
    externalPath: String,
    parent: ClassLoader
) : DexClassLoader(externalPath, optimizedDir, libraryPath, parent) {
    
    override fun loadClass(name: String): Class<*> {
        // 1. 优先从外置插件加�?        return try {
            super.loadClass(name)
        } catch (e: ClassNotFoundException) {
            // 2. 失败回退到内置类
            parent.loadClass(name)
        }
    }
}
```

---

#### 决策 3：联机功能模块化 �?**独立公共 AAR 库（可被其他 App 依赖调用�?*

**用户需�?*：模块商店下载的其他游戏联机功能，可以调用同一个公共联机模块；同时其他应用也能共同调用这个公共联机模块�?
**方案对比**�?
| 方案 | 格式 | 优点 | 缺点 |
|------|------|------|------|
| **A: AAR 公共�?* �?| `:core:online` (Android Library) | 可被任何 App 通过 `implementation` 依赖；支持跨 App 共享 | 需要作�?AAR 分发 |
| **B: RePlugin 插件** | APK 插件 | 支持动态下载安�?| 其他 App 无法直接依赖调用 |
| **C: AIDL 跨进�?* | Service + AIDL | 真正�?App 进程 | 复杂度高、性能开销 |

**选择理由**�?1. **AAR 可被任何 App 依赖**：其�?App 只需 `implementation project(":core:online")` 即可调用
2. **游戏模块共用同一套联机逻辑**：斗地主、五子棋、未来其他游戏都依赖 `:core:online`
3. **支持作为 RePlugin 插件分发**：`feature/online-plugin` 只是 `:core:online` 的包装层，真正的联机逻辑�?AAR �?4. **�?App 调用**：其�?App 可以依赖同一�?AAR，连接到同一�?Relay 服务�?
**模块边界**�?```
:core:online (AAR 公共�?
├── OnlineManager.java          # 联机管理器（公共接口�?├── RelayClient.java           # WebSocket Relay 客户�?├── RoomManager.java          # 房间管理
└── 可被以下模块/App 依赖�?    ├── :app (主框�? �?implementation project(":core:online")
    ├── game_doudizhu (斗地主模�? �?implementation project(":core:online")
    ├── game_gomoku (五子棋模�? �?implementation project(":core:online")
    └── 其他 App �?implementation project(":core:online") 或下�?AAR

可选：feature/online-plugin (RePlugin 包装�?
└── 只是 :core:online �?RePlugin 封装，方便模块商店分�?```

---

#### 决策 4：对标大型团队的具体措施

| 措施 | 实施方案 | 优先�?| 验收标准 |
|------|----------|--------|----------|
| **代码规范** | ktlint + detekt 静态检�?| P0 | CI 构建 0 规范警告 |
| **CI/CD 完善** | GitHub Actions 增加单元测试 + UI 测试 | P0 | 测试覆盖�?> 40% |
| **性能监控** | 自研性能监控模块 | P1 | 启动时间 < 2s、内存占�?< 200MB |
| **错误上报** | Firebase Crashlytics + 自研 ErrorReporter | P0 | 崩溃�?< 0.1% |
| **模块化架�?* | 按功能拆分为独立 Gradle Module | P0 | 模块依赖关系清晰、可独立编译 |
| **依赖注入** | Hilt 全面替换手动 DI | P1 | 所�?Manager 类使�?@Inject |
| **文档自动�?* | Dokka 生成 API 文档 + 架构图自动生�?| P2 | 每次构建自动更新文档 |

---

### 1.2 总体架构�?
```mermaid
graph TB
    subgraph "主框�?APK (<15MB)"
        App[App.java<br/>Application 入口]
        MainActivity[MainActivity<br/>底部导航]
        ModuleStore[ModuleStoreActivity<br/>模块商店]
        ModuleManager[ModuleManager<br/>模块管理引擎]
        BuiltInGames[内置游戏<br/>斗地�?+ 五子棋]
        LAN[局域网 P2P<br/>基础联机能力]
    end
    
    subgraph "可下载模�?(RePlugin)"
        GameModules[游戏模块<br/>APK 格式]
        OnlineRelay[云联机模�?br/>WebSocket Relay]
        ToolsModule[工具箱模块]
        BrowserModule[浏览器模块]
        AIModule[AI 助手模块]
        VPNModule[VPN 模块]
    end
    
    subgraph "VPS 服务�?
        ModuleRepo[模块仓库<br/>/var/www/modules/]
        RelayServer[WebSocket Relay<br/>Node.js]
        UpdateServer[更新服务�?br/>Python]
    end
    
    App --> MainActivity
    MainActivity --> ModuleStore
    ModuleStore --> ModuleManager
    ModuleManager -->|下载| ModuleRepo
    ModuleManager -->|加载| GameModules
    ModuleManager -->|加载| OnlineRelay
    BuiltInGames -->|优先加载商店版本| GameModules
    OnlineRelay -->|WebSocket| RelayServer
    LAN -->|HTTP Relay| RelayServer
    
    style App fill:#e1f5fe
    style ModuleStore fill:#e1f5fe
    style ModuleManager fill:#e1f5fe
    style GameModules fill:#fff3e0
    style OnlineRelay fill:#fff3e0
```

---

## 二、文件列表及相对路径

### 2.1 新增文件

#### 2.1.1 插件化框架集�?
| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `app/libs/replugin-host-lib.jar` | RePlugin Host �?| 放在 app/libs/ 目录 |
| `buildsrc/src/main/kotlin/RePluginExtension.kt` | Gradle 插件配置扩展 | 配置 RePlugin 参数 |
| `core/common/src/main/java/com/gamecenter/core/common/RePluginHolder.java` | RePlugin 初始化管理器 | Application 中初始化 |

#### 2.1.2 模块商店增强

| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `app/src/main/kotlin/com/gamecenter/app/modules/RePluginModuleLoader.kt` | RePlugin 模块加载�?| 替代现有 ModuleLoader |
| `app/src/main/kotlin/com/gamecenter/app/modules/ModuleDownloadManager.kt` | 模块下载管理�?| 支持 APK 格式下载 |
| `app/src/main/kotlin/com/gamecenter/app/modules/ModuleInstaller.kt` | 模块安装�?| APK 插件安装逻辑 |
| `app/src/main/kotlin/com/gamecenter/app/modules/ModuleVersionChecker.kt` | 版本检查器 | 内置 vs 商店版本比较 |

#### 2.1.3 联机公共模块（AAR 库）

| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `core/online/build.gradle.kts` | 联机公共库构建脚�?| Android Library (AAR) |
| `core/online/src/main/java/com/gamecenter/core/online/OnlineManager.java` | 联机管理器（公共接口�?| 所有游戏模块调用此接口 |
| `core/online/src/main/java/com/gamecenter/core/online/RelayClient.java` | WebSocket Relay 客户�?| 连接 Relay 服务�?|
| `core/online/src/main/java/com/gamecenter/core/online/RoomManager.java` | 房间管理�?| 创建/加入房间 |
| `core/online/src/main/java/com/gamecenter/core/online/MessageProtocol.java` | 消息协议 | �?App 通用协议定义 |

> **设计说明**：`:core:online` 产出 AAR 文件，任�?App 均可通过 `implementation project(":core:online")` 依赖调用�?> 可选包装层 `feature/online-plugin/` 作为 RePlugin 插件分发（AAR 是实质，插件只是分发形式）�?
#### 2.1.4 开发环境管�?
| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `tools/env_manager.py` | 开发环境管理器 | 检�?JDK、SDK、NDK |
| `tools/download_jdk.py` | JDK 自动下载脚本 | 缺失时自动下�?|
| `config/environment.properties` | 环境配置文件 | 配置本地环境路径 |

#### 2.1.5 模拟器自动化测试

| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `tools/emulator_control.py` | 模拟器控制脚�?| ADB 命令封装 |
| `tools/emulator_test_runner.py` | 测试运行�?| 自动化测试流�?|
| `app/src/androidTest/java/com/gamecenter/app/test/EmulatorTestBase.kt` | 测试基类 | 模拟器测试基础�?|

#### 2.1.6 文档自动�?
| 文件路径 | 用�?| 说明 |
|---------|------|------|
| `tools/docs_generator.py` | 文档自动生成脚本 | 基于代码注释生成 MD |
| `buildsrc/src/main/kotlin/DocsGeneratorTask.kt` | Gradle 文档生成任务 | 集成到构建流�?|

---

### 2.2 修改文件

| 文件路径 | 修改内容 | 说明 |
|---------|----------|------|
| `app/build.gradle.kts` | 新增 RePlugin 依赖、移除旧模块加载逻辑 | 构建脚本升级 |
| `app/src/main/java/com/gamecenter/app/App.java` | 初始�?RePlugin、模块管理器 | Application 改�?|
| `app/src/main/kotlin/com/gamecenter/app/modules/ModuleManager.kt` | 重写�?RePlugin 版本 | 支持 APK 插件加载 |
| `app/src/main/kotlin/com/gamecenter/app/modules/ModuleAdapter.kt` | 支持内置游戏更新 UI | 显示"检查更�?按钮 |
| `app/src/main/java/com/gamecenter/app/games/GameRegistry.java` | 支持动态注�?RePlugin 插件 | 游戏注册中心改�?|
| `core/network/src/main/java/com/gamecenter/core/network/GameSocketClient.java` | 支持可选加载云联机模块 | 联机功能可插�?|
| `deploy/modules.json` | 更新模块清单格式（APK 格式�?| 模块清单升级 |
| `AI_CONTEXT.md` | 同步架构改造内�?| 文档更新 |
| `项目AI接手说明.md` | 同步架构改造内�?| 文档更新 |

---

## 三、数据结构和接口（类图）

```mermaid
classDiagram
    class ModuleManager {
        -context: Context
        -rePluginLoader: RePluginModuleLoader
        -downloadManager: ModuleDownloadManager
        -installer: ModuleInstaller
        -versionChecker: ModuleVersionChecker
        +installModule(module: ModuleManifest): Boolean
        +uninstallModule(moduleId: String): Boolean
        +updateModule(module: ModuleManifest): Boolean
        +loadGameModule(gameId: String): Boolean
        +registerInstalledGameModules(): Unit
        -getBuiltInVersion(gameId: String): Int
        -getInstalledModuleFromStore(gameId: String): ModuleManifest?
    }
    
    class RePluginModuleLoader {
        -pluginManager: PluginManager
        +loadPlugin(apkPath: String): Boolean
        +unloadPlugin(pluginName: String): Boolean
        +getPluginClassLoader(pluginName: String): ClassLoader?
        +callPluginActivity(activityName: String, intent: Intent): Unit
    }
    
    class ModuleDownloadManager {
        -okHttpClient: OkHttpClient
        -downloadQueue: PriorityQueue<DownloadTask>
        +downloadModule(module: ModuleManifest, callback: DownloadCallback): Unit
        +pauseDownload(moduleId: String): Unit
        +resumeDownload(moduleId: String): Unit
        +cancelDownload(moduleId: String): Unit
        -getDownloadUrl(module: ModuleManifest): String
        -verifyChecksum(file: File, expectedSha256: String): Boolean
    }
    
    class ModuleInstaller {
        +installApkPlugin(apkFile: File): Boolean
        +uninstallApkPlugin(pluginName: String): Boolean
        +verifyPluginApk(apkFile: File): Boolean
        -extractNativeLibs(apkFile: File): Unit
        -optimizeDex(apkFile: File): Unit
    }
    
    class ModuleVersionChecker {
        +compareVersions(builtIn: Int, store: Int): Int
        +checkForUpdates(module: ModuleManifest): ModuleManifest?
        +shouldLoadExternal(builtInVersion: Int, externalModule: ModuleManifest): Boolean
    }
    
    class ModuleManifest {
        +id: String
        +name: String
        +versionCode: Int
        +versionName: String
        +type: String
        +builtIn: Boolean
        +storeCategory: String
        +downloadUrl: String
        +fileSize: Long
        +sha256: String
        +dependencies: List<String>
        +activityClass: String
        +gameId: String?
        +gameCategory: String?
    }
    
    class GameRegistry {
        -staticEntries: List<GameEntry>
        -annotationEntries: List<GameEntry>
        -dynamicEntries: MutableList<GameEntry>
        +registerGame(entry: GameEntry): Unit
        +unregisterGame(gameId: String): Unit
        +getGames(): List<GameEntry>
        +getGameById(gameId: String): GameEntry?
        +registerInstalledGameModules(): Unit
        -loadBuiltInGames(): Unit
        -loadExternalGames(): Unit
    }
    
    class GameEntry {
        +id: String
        +name: String
        +iconRes: Int
        +description: String
        +category: String
        +activityClass: String
        +isExternal: Boolean
        +pluginName: String?
    }
    
    class OnlineManager {
        +isModuleLoaded: Boolean
        +initialize(context: Context): Unit
        +createRoom(gameType: String): String
        +joinRoom(roomCode: String): Unit
        +sendMessage(message: JSONObject): Unit
        +disconnect(): Unit
        -webSocket: WebSocket
        -roomCode: String
    }
    
    OnlineManager --> ModuleManager : 可选依�?    note right of OnlineManager : ⚠️ 位于 :core:online AAR 库\n可被任何 App 通过 implementation 依赖
```

> **⚠️ 类图更正说明（v1.1�?*�?> - `OnlineRelayManager` 已重命名�?`OnlineManager`，位�?`:core:online` AAR �?> - 所有游戏模块（斗地主、五子棋等）通过 `implementation project(":core:online")` 依赖此库
> - 其他 App 也可依赖同一 AAR，实现跨 App 调用公共联机模块
> - 完整的更新后类图将在编码阶段同步修正

### 4.1 模块下载和安装流�?
```mermaid
sequenceDiagram
    actor User as 用户
    participant MS as ModuleStoreActivity
    participant MM as ModuleManager
    participant MDM as ModuleDownloadManager
    participant MI as ModuleInstaller
    participant RPL as RePluginModuleLoader
    participant VPS as VPS 服务�?    
    User->>MS: 点击"下载"按钮
    MS->>MM: installModule(module)
    MM->>MDM: downloadModule(module, callback)
    MDM->>VPS: HTTP GET /modules/game_doudizhu_v201.apk
    loop 下载进度
        VPS-->>MDM: 返回数据�?        MDM-->>MS: onProgress(progress: Int)
        MS-->>User: 更新进度�?    end
    VPS-->>MDM: 下载完成
    MDM->>MDM: verifyChecksum(file, sha256)
    alt 校验失败
        MDM-->>MS: onError("SHA-256 校验失败")
        MS-->>User: 显示错误提示
    else 校验成功
        MDM-->>MM: onSuccess(file)
        MM->>MI: installApkPlugin(apkFile)
        MI->>MI: verifyPluginApk(apkFile)
        MI->>MI: extractNativeLibs(apkFile)
        MI->>RPL: loadPlugin(apkPath)
        RPL->>RPL: 优化 DEX (optDex)
        RPL-->>MI: 加载成功
        MI-->>MM: installSuccess(pluginName)
        MM->>MM: markModuleInstalled(module)
        MM->>GameRegistry: registerGame(entry)
        MM-->>MS: onInstallSuccess(module)
        MS->>MS: 更新 UI (显示"打开"按钮)
        MS-->>User: 显示"安装成功"
    end
```

---

### 4.2 内置游戏更新检查流�?
```mermaid
sequenceDiagram
    actor User as 用户
    participant GR as GamesFragment
    participant MM as ModuleManager
    participant MVC as ModuleVersionChecker
    participant MS as ModuleStoreActivity
    participant VPS as VPS 服务�?    
    User->>GR: 打开游戏大厅
    GR->>MM: registerInstalledGameModules()
    MM->>MM: 扫描内置游戏 (斗地主、五子棋)
    loop 每个内置游戏
        MM->>MVC: checkForUpdates(builtInModule)
        MVC->>VPS: HTTP GET /modules.json
        VPS-->>MVC: 返回模块清单
        MVC->>MVC: compareVersions(builtInVersion, storeVersion)
        alt 商店版本更高
            MVC-->>MM: UpdateAvailable(storeModule)
            MM->>MM: 标记"检查更�?按钮
        else 已是最�?            MVC-->>MM: NoUpdate
        end
    end
    MM-->>GR: 更新游戏列表
    GR-->>User: 显示游戏卡片（内�?+ 可更新标识）
    
    User->>GR: 点击"检查更�?按钮
    GR->>MS: 打开模块商店
    MS->>MM: installModule(storeModule)
    MM->>MM: 下载并安装商店版�?    MM->>GR: onModuleUpdated(module)
    GR->>GR: 重新加载游戏模块
    GR-->>User: 显示"打开"按钮（使用新版本�?```

---

### 4.3 联机功能调用流程（`:core:online` AAR 公共库）

> **⚠️ 更正说明（v1.1�?*�?> `:core:online` �?Android Library (AAR)，编译后作为 AAR 文件被游戏模块依赖调用�?> **不需�?RePlugin 加载**——AAR 在编译时已打包进游戏模块 APK�?> 其他 App 只需�?`build.gradle.kts` 中添�?`implementation project(":core:online")` 即可调用同一套联机逻辑�?
```mermaid
sequenceDiagram
    actor User as 用户
    participant DA as DouDiZhuOnlineActivity
    participant OM as OnlineManager
    participant WS as WebSocket Relay
    
    User->>DA: 选择"远程联机"
    DA->>OM: createRoom("DDZ")
    Note right of OM: OnlineManager 位于<br/>:core:online AAR �?br/>（编译时已打包进 APK�?    OM->>WS: WebSocket 连接 Relay 服务�?    WS-->>OM: onConnected()
    OM->>WS: createRoom(gameType: "DDZ")
    WS-->>OM: roomCode = "ABC123"
    OM-->>DA: onRoomCreated(roomCode)
    DA-->>User: 显示房间�?"ABC123"
    
    Note over DA,OM: 其他 App（如五子棋）调用同一 AAR �?br/>只需 implementation project(":core:online")
```

---

### 4.4 模拟器自动化测试流程

```mermaid
sequenceDiagram
    actor Dev as 开发�?    participant ET as EmulatorTestRunner
    participant EC as EmulatorControl
    participant ADB as ADB 命令
    participant Emu1 as 模拟�? (5554)
    participant Emu2 as 模拟�? (5556)
    
    Dev->>ET: 运行自动化测�?    ET->>EC: checkEmulatorsRunning()
    EC->>ADB: adb devices
    ADB-->>EC: 返回设备列表
    alt 模拟器未启动
        EC->>EC: startEmulator("Pixel_5_API_30")
        EC->>ADB: emulator -avd Pixel_5_API_30 &
        EC->>EC: waitForDevice(120000)
    end
    ET->>EC: installApp(emulator1, apkPath)
    EC->>ADB: adb -s emulator-5554 install -r app-debug.apk
    ET->>EC: installApp(emulator2, apkPath)
    EC->>ADB: adb -s emulator-5556 install -r app-debug.apk
    
    loop 测试步骤
        ET->>EC: startGame(emulator1, "DouDiZhu")
        EC->>ADB: adb -s emulator-5554 shell am start -n com.gamecenter.app/.games.doudizhu.DouDiZhuMenuActivity
        ET->>EC: tapScreen(emulator1, x, y)
        EC->>ADB: adb -s emulator-5554 shell input tap x y
        ET->>EC: inputText(emulator1, "ABC123")
        EC->>ADB: adb -s emulator-5554 shell input text "ABC123"
        ET->>EC: takeScreenshot(emulator1, "test_step_1.png")
        EC->>ADB: adb -s emulator-5554 exec-out screencap -p > test_step_1.png
    end
    
    ET->>ET: 验证测试结果
    ET-->>Dev: 测试报告 (成功/失败)
```

---

## 五、任务列表（有序、含依赖关系�?
### 任务分解原则
1. **最大任务数**�? 个任务（硬性上限）
2. **最小粒�?*：每个任务至少包�?3 个相关文�?3. **分组原则**：按功能模块/层次分组
4. **依赖关系**：尽量减少线性依赖，让任务可并行

---

### 任务列表

| 任务 ID | 任务名称 | 源文�?| 依赖 | 优先�?|
|---------|---------|--------|------|--------|
| **T01** | **项目基础设施 + RePlugin 集成** | `app/build.gradle.kts`<br/>`buildsrc/src/main/kotlin/RePluginExtension.kt`<br/>`core/common/src/main/java/com/gamecenter/core/common/RePluginHolder.java`<br/>`app/libs/replugin-host-lib.jar`<br/>`local.properties`<br/>`gradle.properties` | �?| P0 |
| **T02** | **模块加载引擎重构（RePlugin 版）** | `app/src/main/kotlin/com/gamecenter/app/modules/RePluginModuleLoader.kt`<br/>`app/src/main/kotlin/com/gamecenter/app/modules/ModuleInstaller.kt`<br/>`app/src/main/kotlin/com/gamecenter/app/modules/ModuleManager.kt`<br/>`app/src/main/java/com/gamecenter/app/App.java` | T01 | P0 |
| **T03** | **模块商店增强（APK 下载 + 内置游戏更新�?* | `app/src/main/kotlin/com/gamecenter/app/modules/ModuleDownloadManager.kt`<br/>`app/src/main/kotlin/com/gamecenter/app/modules/ModuleVersionChecker.kt`<br/>`app/src/main/kotlin/com/gamecenter/app/modules/ModuleAdapter.kt`<br/>`app/src/main/java/com/gamecenter/app/modules/ModuleStoreActivity.kt`<br/>`deploy/modules.json` | T02 | P0 |
| **T04** | **联机公共模块（AAR 库）+ 游戏注册中心改�?* | `core/online/build.gradle.kts`<br/>`core/online/src/main/java/com/gamecenter/core/online/OnlineManager.java`<br/>`core/network/src/main/java/com/gamecenter/core/network/GameSocketClient.java`<br/>`app/src/main/java/com/gamecenter/app/games/GameRegistry.java`<br/>`app/src/main/java/com/gamecenter/app/fragments/GamesFragment.java` | T02 | P1 |
| **T05** | **开发环境管�?+ 模拟器自动化测试** | `tools/env_manager.py`<br/>`tools/emulator_control.py`<br/>`tools/emulator_test_runner.py`<br/>`app/src/androidTest/java/com/gamecenter/app/test/EmulatorTestBase.kt`<br/>`config/environment.properties` | T03, T04 | P1 |

---

### 任务依赖�?
```mermaid
graph TD
    T01[T01: 项目基础设施 + RePlugin 集成] --> T02[T02: 模块加载引擎重构]
    T02 --> T03[T03: 模块商店增强]
    T02 --> T04[T04: 联机模块拆分]
    T03 --> T05[T05: 开发环�?+ 模拟器测试]
    T04 --> T05
    
    style T01 fill:#c8e6c9
    style T02 fill:#c8e6c9
    style T03 fill:#fff9c4
    style T04 fill:#fff9c4
    style T05 fill:#ffccbc
```

---

## 六、依赖包列表

### 6.1 新增依赖

```
# RePlugin 插件化框�?implementation 'com.qihoo360.replugin:replugin-host-lib:2.3.4'

# 联机公共模块（AAR 库，所有游戏模块依赖此库）
implementation project(":core:online")

# APK 解析和安�?implementation 'androidx.core:core-ktx:1.13.1'
implementation 'androidx.fragment:fragment-ktx:1.7.1'

# 下载管理（增强版�?implementation 'com.squareup.okhttp3:okhttp:4.12.0'
implementation 'androidx.work:work-runtime-ktx:2.9.1'  # 后台下载任务

# 测试依赖
androidTestImplementation 'androidx.test.espresso:espresso-core:3.6.1'
androidTestImplementation 'androidx.test:runner:1.6.1'
androidTestImplementation 'androidx.test:rules:1.6.1'
androidTestImplementation 'com.linkedin.dexmaker:dexmaker-mockito-inline:2.28.6'

# 文档生成
classpath 'org.jetbrains.dokka:dokka-gradle-plugin:1.9.20'
```

### 6.2 移除依赖

```
# 移除旧版 DexClassLoader 相关（如果有�?# 无特定依赖需要移除，现有实现是手写的
```

---

## 七、共享知识（跨文件约定）

### 7.1 模块 APK 格式规范

**文件命名规则**�?```
{moduleId}_v{versionCode}_{architecture}.apk

示例�?- game_doudizhu_v201_arm64-v8a.apk
- feature_online_relay_v100_universal.apk
- tool_tools_v150_armeabi-v7a.apk
```

**APK 打包规范**�?1. 使用 `gradlew :feature:assembleRelease` 构建
2. 必须包含 `META-INF/MANIFEST.MF` 签名文件
3. 支持 `arm64-v8a`、`armeabi-v7a`、`x86_64`、`universal` 架构
4. 最�?SDK 版本必须 �?�?APP �?minSdkVersion

---

### 7.2 RePlugin 插件通信机制

**跨插件通信**�?```kotlin
// �?APP 调用插件 Activity
RePlugin.startActivity(context, Intent().apply {
    component = ComponentName("plugin_name", "com.gamecenter.plugin.game.ActivityName")
})

// 插件调用�?APP 功能
RePlugin.getHostContext().let { hostContext ->
    // 使用�?APP �?Context
}
```

**数据共享**�?- 使用 `ContentProvider` 共享数据�?- 使用 `SharedPreferences` 共享配置（指�?MODE_MULTI_PROCESS�?- 使用文件路径共享（指定公共存储目录）

---

### 7.3 模块版本号管�?
**版本号规�?*�?```
versionCode: Integer (递增，不可回退)
versionName: "x.y.z" (语义化版�?

示例�?- 内置版本: versionCode=100, versionName="1.0.0"
- 商店版本: versionCode=201, versionName="2.0.1"
```

**版本比较逻辑**�?```kotlin
fun compareVersions(builtIn: Int, store: Int): Int {
    return when {
        store > builtIn -> 1  // 商店版本更高
        store == builtIn -> 0  // 版本相同
        else -> -1  // 内置版本更高（异常场景）
    }
}
```

---

### 7.4 模拟�?ADB 端口分配

**默认端口**�?- 模拟�?1: `emulator-5554`
- 模拟�?2: `emulator-5556`
- 模拟�?3: `emulator-5558`

**ADB 命令模板**�?```bash
# 安装 APK 到指定模拟器
adb -s emulator-5554 install -r app-debug.apk

# 启动 Activity
adb -s emulator-5554 shell am start -n com.gamecenter.app/.MainActivity

# 发送按键事�?adb -s emulator-5554 shell input keyevent KEYCODE_BACK

# 点击屏幕坐标
adb -s emulator-5554 shell input tap 500 1000

# 输入文本
adb -s emulator-5554 shell input text "hello"

# 截屏
adb -s emulator-5554 exec-out screencap -p > screenshot.png
```

---

## 八、待明确事项

### 8.1 架构层面

| 问题 | 现状 | 需要确�?|
|------|------|----------|
| **RePlugin 是否支持 Android 15+?** | RePlugin 最新版 2.3.4 支持�?Android 14 | 需要测�?Android 15 兼容性，或寻找替代方�?|
| **模块 APK 的签名要�?** | �?APP 和插�?APK 可以使用不同签名 | 确认是否需要相同签名（某些权限需要） |
| **插件化对 Android 12+ 的限�?** | Android 12 引入更严格的反射限制 | 需要测�?`android.os.Build` 等反射调�?|

### 8.2 产品层面

| 问题 | 现状 | 需要确�?|
|------|------|----------|
| **初始 APK < 15MB 是否包含内置游戏?** | 需�?P0-3 要求保留内置 | 确认内置游戏（斗地主、五子棋）是否计�?15MB |
| **模块下载是否支持断点续传?** | 当前 ModuleDownloader 支持 | 确认是否需要多源下载（香港 VPS + 美国 VPS�?|
| **联机模块是否免费?** | 当前所有功能免�?| 确认云联机模块是否需要付费或会员权限 |

### 8.3 测试层面

| 问题 | 现状 | 需要确�?|
|------|------|----------|
| **模拟器类型（AS AVD / 第三方）?** | 用户提到"本地已启动两个模拟器" | 确认模拟器类型，以便编写对应控制脚本 |
| **是否需要真机测�?** | 当前只有模拟器测�?| 确认是否需要覆盖真机测试场�?|
| **测试覆盖率目�?** | 当前无明确要�?| 确认是否需要对标大型团队（�?70% 覆盖率） |

---

## 九、风险评估和缓解措施

| 风险 | 严重程度 | 缓解措施 | 负责�?|
|------|---------|----------|--------|
| **RePlugin 兼容性问�?* | �?| 提前进行 Android 15 兼容性测试；准备 Plan B（DFM�?| 高见�?|
| **模块下载失败率高** | �?| 多源下载、断点续传、SHA-256 校验、重试机�?| 高见�?|
| **内置游戏更新冲突** | �?| 版本号管理、降级保护、数据迁移脚�?| 高见�?|
| **联机模块版本不兼�?* | �?| 模块版本依赖声明、强制更新机制、API 版本检�?| 高见�?|
| **初始框架功能过于简�?* | �?| 保留最基本的用户体验（设置、帮助、反馈） | 许清�?|
| **模拟器自动化测试不稳�?* | �?| 增加重试机制、超时处理、错误日志、截图证�?| 高见�?|

---

## 十、附�?
### 10.1 RePlugin 快速上�?
**Host APP 配置**�?```kotlin
// App.java
class App : Application() {
    override fun onCreate() {
        super.onCreate()
        RePlugin.App.attachBaseContext(this)
        RePlugin.App.onCreate(this)
    }
}
```

**Plugin APK 配置**�?```gradle
// feature/game_doudizhu/build.gradle.kts
plugins {
    id("com.android.application")
    id("replugin-plugin")  // 需要添�?RePlugin Plugin 插件
}

android {
    defaultConfig {
        applicationId = "com.gamecenter.plugin.game.doudizhu"
    }
}
```

---

### 10.2 模块商店 API 设计

**获取模块清单**�?```
GET https://your-server.example.com/modules.json

Response:
{
  "modules": [
    {
      "id": "game_doudizhu",
      "name": "斗地�?,
      "versionCode": 201,
      "versionName": "2.0.1",
      "type": "game",
      "builtIn": true,
      "storeCategory": "game",
      "downloadUrl": "https://your-server.example.com/modules/game_doudizhu_v201.apk",
      "fileSize": 15728640,
      "sha256": "abc123...",
      "dependencies": []
    }
  ]
}
```

**下载模块 APK**�?```
GET https://your-server.example.com/modules/game_doudizhu_v201.apk

Headers:
  Range: bytes=0-  # 支持断点续传
```

---

### 10.3 性能基准测试目标

| 指标 | 当前�?| 目标�?| 测试工具 |
|------|--------|--------|----------|
| **冷启动时�?* | ~3.5s | < 2.0s | `adb shell am start -W` |
| **热启动时�?* | ~1.2s | < 0.5s | `adb shell am start -W` |
| **内存占用（首页）** | ~180MB | < 100MB | Android Studio Profiler |
| **APK 大小（框架）** | ~45MB | < 15MB | `du -sh app/build/outputs/apk/release/` |
| **帧率（游戏界面）** | ~55 FPS | > 60 FPS | `adb shell dumpsys gfxinfo` |

---

**文档版本**: v1.2
**创建时间**: 2026-05-25
**最后更�?*: 2026-05-27
**创建�?*: 高见�?(Gao) - GameCenterApp 架构�?**修正�?*: 齐活�?(Qi) - 主理人（决策3改为 AAR 公共库）
**审核状�?*: 已审核（实施完成�?
---

## 十一、改造实施状态（2026-05-27 更新�?
### 11.1 任务完成度总览

| 任务 ID | 任务名称 | 完成状�?| 实际交付�?|
|---------|---------|----------|----------|
| T01 | 项目基础设施 + RePlugin 集成 | �?已完�?| `App.java` RePlugin桥接层、`config/environment.properties` |
| T02 | 模块加载引擎重构 | �?已基本完�?| `ModuleDownloadManager.kt`、`ModuleVersionChecker.kt` |
| T03 | 模块商店增强 | �?已完�?| 模块商店UI更新、下载管理逻辑 |
| T04 | 联机公共模块（AAR 库） | �?已完�?| `core/online/` AAR库（4个Java类） |
| T05 | 开发环境管�?| �?已完�?| `config/environment.properties` |
| T06 | 代码规范 + CI/CD 基础 | �?已完�?| 代码规范框架建立 |
| T07 | QA 测试验证 | �?已完成（已知问题�?| 22/22单元测试通过�?3个仪表化测试待环境配�?|

### 11.2 实际实现与架构决策的差异

#### 决策1：RePlugin JAR 不可�?�?桥接层方�?
**原计�?*：使�?`com.qihoo360.replugin:replugin-host-lib:2.3.4`

**实际问题**：jcenter 已关闭，Maven Central 无此 JAR

**实际方案**�?- 创建桥接�?`com.qihoo360.replugin.RePlugin.java`
- 静态方�?`RePlugin.App.attachBaseContext()` �?`RePlugin.App.onCreate()`
- 底层委托�?`ModuleLoaderV2`（DexClassLoader 实现�?- **风险**：非官方 RePlugin 实现，Android 15+ 兼容性未验证

#### 决策2：内置游戏更新策�?�?仅实现版本号判断

**原计�?*：版本号判断 + ClassLoader 优先级兜�?
**实际实现**：仅实现版本号判断逻辑

**待补�?*：ClassLoader 优先级兜底机制（v1.3�?
#### 决策3：联机功能模块化 �?按原计划实现 �?
**原计�?*：`:core:online` AAR 公共�?
**实际实现**：✅ 完全按原计划实现
- `OnlineManager.java` - 联机管理器（公共接口�?- `RelayClient.java` - WebSocket Relay 客户�?- `RoomManager.java` - 房间管理�?- `MessageProtocol.java` - 消息协议

#### 决策4：对标大型团�?�?基础框架已建�?
**原计�?*：ktlint + detekt + GitHub Actions + 测试覆盖�?> 40%

**实际实现**�?- �?代码规范框架已建�?- ⚠️ CI/CD 流水线待配置
- ⚠️ 测试覆盖率当�?29.3%（目�?> 40%�?
### 11.3 已知技术债务

| 编号 | 技术债务描述 | 影响范围 | 严重�?| 计划修复版本 |
|----------|----------------|----------|---------|----------------|
| TD-01 | `ModuleLifecycleManagerTest.java` �?`ModuleDependencyGraphTest.java` �?`@Ignore` 跳过 | 无法验证模块生命周期管理核心逻辑 | �?| v1.2（Robolectric 重构�?|
| TD-02 | `connectedAndroidTest` 因网络代理问题无法执�?| T01-T04 仪表化测试（53个用例）无法运行 | �?| v1.2（配置离线依赖） |
| TD-03 | RePlugin 桥接层非官方 JAR，Android 15+ 兼容性未验证 | 未来 Android 版本可能不可�?| �?| v1.3（兼容性测试） |
| TD-04 | `CircularDependencyException` 内部类未实现 | 循环依赖检测功能不完整 | �?| v1.2（补充实现） |
| TD-05 | 模块依赖自动下载功能未实�?| 多模块依赖场景需手动管理 | �?| v1.4（P2 需求） |

### 11.4 文件清单（实际实�?vs. 原计划）

#### 已创建文件（新增�?
| 文件路径 | 状�?| 说明 |
|---------|--------|------|
| `app/src/main/java/com/qihoo360/replugin/RePlugin.java` | �?已创�?| RePlugin 桥接类（原计划用 JAR，实际创建桥接类�?|
| `app/src/main/java/com/gamecenter/app/modules/ModuleDownloadManager.kt` | �?已创�?| 模块下载管理�?|
| `app/src/main/java/com/gamecenter/app/modules/ModuleVersionChecker.kt` | �?已创�?| 版本检查器 |
| `core/online/build.gradle` | �?已创�?| 联机公共库构建脚�?|
| `core/online/src/main/java/com/gamecenter/core/online/OnlineManager.java` | �?已创�?| 联机管理�?|
| `core/online/src/main/java/com/gamecenter/core/online/RelayClient.java` | �?已创�?| WebSocket Relay 客户�?|
| `core/online/src/main/java/com/gamecenter/core/online/RoomManager.java` | �?已创�?| 房间管理�?|
| `core/online/src/main/java/com/gamecenter/core/online/MessageProtocol.java` | �?已创�?| 消息协议 |
| `config/environment.properties` | �?已创�?| 开发环境配置文�?|
| `app/src/androidTest/java/com/gamecenter/app/T01RePluginInitTest.kt` | �?已创�?| T01 测试用例�?个） |
| `app/src/androidTest/java/com/gamecenter/app/T02ModuleStoreTest.kt` | �?已创�?| T02 测试用例�?1个） |
| `app/src/androidTest/java/com/gamecenter/app/T03OnlineModuleTest.kt` | �?已创�?| T03 测试用例�?8个） |
| `app/src/androidTest/java/com/gamecenter/app/T04BuiltInGameUpdateTest.kt` | �?已创�?| T04 测试用例�?6个） |
| `app/src/androidTest/java/com/gamecenter/app/EmulatorTestBase.kt` | �?已创�?| 测试基类 |

#### 已修改文�?
| 文件路径 | 修改内容 | 状�?|
|---------|----------|--------|
| `app/src/main/java/com/gamecenter/app/App.java` | 添加 RePlugin 初始化调用（桥接层） | �?已完�?|
| `app/build.gradle` | 添加 `fileTree(dir: 'libs', include: ['*.jar'])` | �?已完�?|
| `app/src/main/res/values/styles.xml` | 添加 Material3 兼容样式 | �?已完�?|
| `app/src/main/res/layout/item_module.xml` | 修复 Material3 样式引用 | �?已完�?|

#### 未创建文件（原计划有，实际未创建�?
| 原计划文件路�?| 未创建原�?| 处理方式 |
|----------|----------|----------|
| `app/libs/replugin-host-lib.jar` | jcenter 关闭，无法下�?| 创建桥接类替�?|
| `buildsrc/src/main/kotlin/RePluginExtension.kt` | 使用桥接层方案，无需 Gradle 插件 | 取消 |
| `tools/env_manager.py` | 环境管理�?`config/environment.properties` 替代 | 取消 |
| `tools/emulator_control.py` | 测试基类直接�?ADB 命令 | 取消 |
| `tools/docs_generator.py` | 文档手动更新 | 取消 |

### 11.5 测试覆盖率现�?
| 测试类型 | 用例�?| 执行�?| 通过�?| 通过�?| 备注 |
|----------|--------|--------|--------|--------|------|
| 单元测试（src/test/�?| 22 | 22 | 22 | 100% | `UpdateInfoTest` �?|
| 仪表化测试（src/androidTest/�?| 53 | 0 | 0 | - | 网络问题待修�?|
| 总计 | 75 | 22 | 22 | 29.3% | 目标 > 40% |

**待修�?*�?1. 配置 Gradle 离线模式或内�?Maven 仓库（修�?TD-02�?2. 使用 Robolectric 重构 `ModuleLifecycleManagerTest` �?`ModuleDependencyGraphTest`（修�?TD-01�?3. 补充 T01-T04 仪表化测试的 Mock 环境

### 11.6 下一步行动计�?
#### 立即执行（v1.2�?
1. **修复网络配置**：使 `connectedAndroidTest` 可以执行
   - 配置 Gradle 离线模式
   - 或搭建内�?Maven 仓库
   - 责任人：软件工程师（寇豆码）

2. **Robolectric 重构**：修�?TD-01
   - �?`ModuleLifecycleManagerTest` �?`ModuleDependencyGraphTest` 添加 Robolectric 环境
   - 移除 `@Ignore` 注解
   - 责任人：软件工程师（寇豆码）

3. **补充 `CircularDependencyException`**：修�?TD-04
   - �?`ModuleLifecycleManager.java` 中添加内部类
   - 更新测试用例
   - 责任人：软件工程师（寇豆码）

#### 后续优化（v1.3�?
1. **Android 15+ 兼容性测�?*：修�?TD-03
   - �?Android 15+ 设备上测�?RePlugin 桥接�?   - 必要时寻找替代方案（�?Dynamic Feature Modules�?   - 责任人：架构师（高见远）

2. **性能基准测试**
   - 冷启动时�?< 2s
   - 内存占用 < 200MB
   - 帧率 > 60 FPS
   - 责任人：QA 工程师（严过关）

3. **模块依赖自动下载功能**：修�?TD-05
   - 实现 `ModuleManifest.dependencies` 解析
   - 安装模块时自动检测并下载依赖
   - 责任人：软件工程师（寇豆码）

---

## 变更日志

### v1.0 (2026-05-25)
- 初始版本创建
- 覆盖 PRD 的所�?8 条需�?- 完成 4 个关键架构决�?- 输出完整的类图和时序�?- 任务分解按功能模块分组（5 个任务）
- 明确待确认问题和风险缓解措施
