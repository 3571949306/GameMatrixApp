# 执行规划 - GameMatrix App

## 总体规划

基于项目审计和问题清单，制定以下分阶段执行计划。每个阶段包含明确的目标、修改范围、验证方式和风险控制。

---

## 阶段一：P0 严重问题修复（立即执行）

### 1.1 清理美国 VPS 残留配置

**阶段目标**: 完全移除已下线的美国 VPS 相关配置，简化代码结构

**修改范围**:
- `app/build.gradle`: 移除 `SERVER_URL_FALLBACK` 和 `MODULE_FALLBACK_HOST` 配置
- `core/update/build.gradle`: 移除 `SERVER_URL_FALLBACK` 配置
- `core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java`: 
  - 移除 `US_BASE_URL` 常量
  - 简化 `buildUpdateUrls()` 方法，移除 US VPS 分支
  - 更新注释说明
- `core/common/src/main/java/com/gamecenter/app/SettingsManager.java`:
  - 移除 `UPDATE_SOURCE_VPS_US` 常量（或保留但标记为内部使用）
  - 更新 `getUpdateSource()` 方法，自动回退已废弃的值
- `local.properties.template`: 移除美国 VPS 相关注释

**修改原因**: 
美国 VPS 已于 2026-06-19 下线，保留残留配置增加代码复杂度，可能误导开发者和用户

**验证方式**:
1. 编译通过：`.\gradlew.bat :app:assembleDebug`
2. 单元测试通过：`.\gradlew.bat :app:test`
3. 检查 BuildConfig 中不再包含 `SERVER_URL_FALLBACK`
4. 检查设置界面不显示美国 VPS 选项

**风险点**: 
- 旧版本用户已存储的 `UPDATE_SOURCE_VPS_US` 值需要自动回退
- 部分代码可能依赖 `US_BASE_URL` 常量

**回退方式**: 
Git 回退到修改前的 commit

**预计时间**: 2-3 小时

---

### 1.2 启用证书绑定（Release 构建）

**阶段目标**: 在 Release 构建中启用证书绑定，提升网络安全性

**修改范围**:
- `core/security/src/main/kotlin/com/gamecenter/app/core/security/SecureOkHttpFactory.kt`:
  - 添加构建类型判断（Debug/Release）
  - Debug 构建禁用证书绑定（兼容模拟器）
  - Release 构建启用证书绑定
  - 添加证书绑定配置开关

**修改原因**: 
证书绑定被完全禁用存在中间人攻击风险，不符合安全最佳实践

**验证方式**:
1. Debug 构建：模拟器测试正常
2. Release 构建：真机测试网络请求正常
3. 检查证书绑定配置生效

**风险点**: 
- 模拟器环境可能仍然存在问题
- 需要确保证书配置正确

**回退方式**: 
Git 回退或临时关闭证书绑定开关

**预计时间**: 2-3 小时

---

### 1.3 启用 R8 代码混淆

**阶段目标**: 启用 R8 混淆和资源收缩，减小 APK 体积，提升代码安全性

**修改范围**:
- `app/build.gradle`:
  - 设置 `minifyEnabled true`
  - 设置 `shrinkResources true`
  - 修复 ProGuard rules 解决 BreakoutActivity$1 问题
- `app/proguard-rules.pro`:
  - 添加游戏模块保留规则
  - 添加反射类保留规则
  - 添加 Hilt/Room 保留规则

**修改原因**: 
R8 未启用导致 APK 体积大、代码易被反编译

**验证方式**:
1. Release 构建成功
2. APK 体积减小 20-30%
3. 功能测试正常（特别是游戏模块）
4. 反编译测试确认代码已混淆

**风险点**: 
- BreakoutActivity$1 重复类问题可能导致 R8 崩溃
- 部分反射调用可能被混淆导致运行时错误
- 需要充分测试所有游戏模块

**回退方式**: 
Git 回退或临时关闭 R8

**预计时间**: 3-4 小时

---

## 阶段二：P1 重要问题修复（优先执行）

### 2.1 启用 ABI Splits（Release 构建）

**阶段目标**: 启用 ABI 拆分，减小 APK 体积

**修改范围**:
- `app/build.gradle`:
  - Release 构建启用 ABI splits（`enable true`）
  - 仅包含 arm64-v8a
  - Debug 构建保持 universal APK（兼容模拟器）

**修改原因**: 
ABI splits 关闭导致 APK 体积增大约 75%

**验证方式**:
1. Release 构建生成 arm64-v8a APK
2. APK 体积减小 60-70%
3. 真机测试正常

**风险点**: 
- x86_64 设备无法安装（占比 <5%）
- 模拟器测试需要 universal APK

**回退方式**: 
Git 回退或临时关闭 ABI splits

**预计时间**: 1-2 小时

---

### 2.2 严格 Lint 模式

**阶段目标**: 逐步启用严格 Lint 模式，提升代码质量

**修改范围**:
- `app/build.gradle`:
  - 设置 `abortOnError true`（或保持 false 但记录问题）
  - 设置 `warningsAsErrors true`（针对新代码）
  - 更新 `lint-baseline.xml` 锁定现有问题
- 修复关键 Lint 问题（Top 20）

**修改原因**: 
Lint 宽松模式导致问题积累，代码质量下降

**验证方式**:
1. Lint 检查通过
2. 新增代码无 Lint 错误
3. CI 构建正常

**风险点**: 
- 现有 Lint 问题可能阻塞构建
- 需要逐步修复

**回退方式**: 
Git 回退或临时关闭严格模式

**预计时间**: 4-6 小时

---

### 2.3 补全核心模块单元测试

**阶段目标**: 为斗地主联机、围棋 AI、模块加载器添加单元测试

**修改范围**:
- `app/src/test/java/com/gamecenter/app/games/doudizhu/`:
  - 添加 `DouDiZhuNetworkHandlerTest.java`
  - 添加 `DouDiZhuSeatManagerTest.java`
  - 添加 `DouDiZhuSyncManagerTest.java`
- `app/src/test/java/com/gamecenter/app/games/go/`:
  - 添加 `GoGameTest.java`（终局判定）
  - 添加 `GoAITest.java`
- `app/src/test/java/com/gamecenter/app/modules/`:
  - 添加 `ModuleLoaderTest.kt`
  - 添加 `ModuleManagerTest.kt`

**修改原因**: 
核心模块缺少单元测试，存在回归风险

**验证方式**:
1. 新增测试全部通过
2. 测试覆盖率达到 80%+
3. CI 构建正常

**风险点**: 
- 部分代码依赖 Android 框架，需要使用 Robolectric
- 测试编写耗时

**回退方式**: 
Git 回退或删除测试文件

**预计时间**: 6-8 小时

---

### 2.4 修复设置界面美国 VPS 选项

**阶段目标**: 移除或隐藏设置界面中的美国 VPS 选项

**修改范围**:
- `app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java`:
  - 移除美国 VPS 选项
  - 若用户已选择 US VPS，自动回退到 AUTO
  - 更新 UI 文案

**修改原因**: 
用户可能选择已废弃的美国 VPS 选项，导致更新失败

**验证方式**:
1. 设置界面不显示美国 VPS 选项
2. 已选择 US VPS 的用户自动回退到 AUTO
3. 更新功能正常

**风险点**: 
- 需要处理已存储的旧值
- UI 布局可能需要调整

**回退方式**: 
Git 回退

**预计时间**: 1-2 小时

---

### 2.5 改进模块加载错误处理

**阶段目标**: 提供详细的模块加载错误信息和解决建议

**修改范围**:
- `app/src/main/java/com/gamecenter/app/modules/ModuleDownloader.kt`:
  - 添加错误码枚举
  - 提供详细错误信息
- `core/moduleloader/src/main/kotlin/com/gamecenter/app/moduleloader/ModuleLoaderV2.kt`:
  - 添加错误码枚举
  - 提供解决建议
  - 添加重试机制

**修改原因**: 
模块加载失败时错误信息不够详细，用户体验差

**验证方式**:
1. 模块加载失败时显示详细错误
2. 提供解决建议（如重试、清理缓存等）
3. 重试机制正常工作

**风险点**: 
- 需要定义完整的错误码体系
- 错误信息需要国际化

**回退方式**: 
Git 回退

**预计时间**: 3-4 小时

---

## 阶段三：P2 中等问题修复（建议执行）

### 3.1 创建统一文档索引

**阶段目标**: 创建 `docs/INDEX.md` 统一文档索引

**修改范围**:
- 创建 `docs/INDEX.md`
- 按主题分类文档（架构、发布、测试、维护等）
- 在 `README.md` 中链接到文档索引

**修改原因**: 
文档分散，新开发者难以快速上手

**验证方式**:
1. 文档索引可访问
2. 所有文档都有链接
3. 分类清晰

**风险点**: 
- 文档链接可能失效
- 需要持续维护

**回退方式**: 
Git 回退或删除索引文件

**预计时间**: 2-3 小时

---

### 3.2 制定 Java/Kotlin 混合编码规范

**阶段目标**: 制定并记录 Java/Kotlin 混合编码规范

**修改范围**:
- 更新 `CODE_WIKI.md` 第 10 章
- 添加命名规范、注释规范、错误处理规范
- 添加示例代码

**修改原因**: 
代码风格不统一，影响可维护性

**验证方式**:
1. 编码规范文档完成
2. 团队评审通过
3. 新代码遵循规范

**风险点**: 
- 需要团队共识
- 历史代码需要逐步迁移

**回退方式**: 
Git 回退

**预计时间**: 2-3 小时

---

### 3.3 处理 TODO/FIXME 注释

**阶段目标**: 逐个检查并处理 TODO/FIXME 注释

**修改范围**:
- `app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java`
- `app/src/main/java/com/gamecenter/app/utils/SoundManager.java`
- `app/src/main/java/com/gamecenter/app/utils/SystemInfoCollector.java`
- `app/src/main/java/com/gamecenter/app/monitor/PerfMonitor.java`
- `app/src/main/java/com/gamecenter/app/games/whack/WhackActivity.java`
- `app/src/main/java/com/gamecenter/app/modules/ModuleDependencyResolver.java`
- `app/src/main/java/com/gamecenter/app/games/doudizhu/DouDiZhuLogHelper.java`
- `app/src/main/java/com/gamecenter/app/games/doudizhu/AIBot.java`

**修改原因**: 
TODO/FIXME 注释表明存在未处理的问题

**验证方式**:
1. 所有 TODO/FIXME 已处理或创建 Issue
2. 代码中不再包含未处理的 TODO/FIXME

**风险点**: 
- 部分 TODO 可能涉及复杂功能
- 需要评估优先级

**回退方式**: 
Git 回退

**预计时间**: 4-6 小时

---

### 3.4 补全英文翻译

**阶段目标**: 补全 `values-en/strings.xml` 缺失的 568 个翻译

**修改范围**:
- `app/src/main/res/values-en/strings.xml`

**修改原因**: 
英文环境下显示中文，影响国际化体验

**验证方式**:
1. 英文翻译完整
2. 切换到英文后所有界面正常显示
3. 无中文残留

**风险点**: 
- 翻译质量需要保证
- 部分专业术语需要准确翻译

**回退方式**: 
Git 回退

**预计时间**: 6-8 小时

---

## 阶段四：P3 轻微问题修复（可选执行）

### 4.1 规范资源文件命名

**阶段目标**: 重命名不符合规范的资源文件

**修改范围**:
- `app/src/main/res/drawable/` 下不符合规范的文件
- 更新代码中的引用

**修改原因**: 
资源文件命名不符合 Android 规范

**验证方式**:
1. Lint 检查无资源命名警告
2. 所有引用正常

**风险点**: 
- 需要更新大量引用
- 可能遗漏

**回退方式**: 
Git 回退

**预计时间**: 2-3 小时

---

### 4.2 更新依赖版本

**阶段目标**: 更新较旧的依赖版本

**修改范围**:
- `app/build.gradle`
- `build.gradle`

**修改原因**: 
部分依赖版本较旧，可能存在已知漏洞

**验证方式**:
1. 依赖更新后编译通过
2. 功能测试正常
3. 无新引入的问题

**风险点**: 
- 新版本可能引入 breaking changes
- 需要充分测试

**回退方式**: 
Git 回退

**预计时间**: 2-3 小时

---

### 4.3 统一注释语言

**阶段目标**: 统一代码注释语言（建议英文）

**修改范围**:
- 全项目 Java 和 Kotlin 文件

**修改原因**: 
注释语言不统一，影响可读性

**验证方式**:
1. 注释语言统一
2. 翻译准确

**风险点**: 
- 工作量巨大
- 需要翻译工具辅助

**回退方式**: 
Git 回退

**预计时间**: 8-10 小时

---

## 执行顺序

1. **阶段一**（P0）: 立即执行，阻塞发布
   - 1.1 清理美国 VPS 残留配置
   - 1.2 启用证书绑定
   - 1.3 启用 R8 混淆

2. **阶段二**（P1）: 优先执行，影响质量
   - 2.1 启用 ABI Splits
   - 2.2 严格 Lint 模式
   - 2.3 补全单元测试
   - 2.4 修复设置界面
   - 2.5 改进错误处理

3. **阶段三**（P2）: 建议执行，提升体验
   - 3.1 创建文档索引
   - 3.2 制定编码规范
   - 3.3 处理 TODO/FIXME
   - 3.4 补全英文翻译

4. **阶段四**（P3）: 可选执行，优化细节
   - 4.1 规范资源命名
   - 4.2 更新依赖
   - 4.3 统一注释语言

---

## 风险控制

### 通用风险
1. **编译失败**: 每次修改后立即编译验证
2. **测试失败**: 每次修改后运行单元测试
3. **功能异常**: 关键修改后进行功能测试
4. **性能下降**: 性能敏感修改后进行性能测试

### 回退策略
1. 所有修改使用 Git 管理
2. 每个阶段完成后提交 commit
3. 出现问题可快速回退
4. 保留修改记录和回退方案

### 测试策略
1. **单元测试**: 每次修改后运行
2. **集成测试**: 每个阶段完成后运行
3. **功能测试**: 关键功能手动测试
4. **性能测试**: 性能敏感修改后测试

---

## 时间估算

| 阶段 | 预计时间 | 优先级 |
|------|----------|--------|
| 阶段一（P0） | 7-10 小时 | 立即 |
| 阶段二（P1） | 15-22 小时 | 优先 |
| 阶段三（P2） | 14-20 小时 | 建议 |
| 阶段四（P3） | 12-16 小时 | 可选 |
| **总计** | **48-68 小时** | - |

---

## 成功标准

1. **P0 问题全部修复**: 证书绑定启用、R8 启用、美国 VPS 清理
2. **P1 问题大部分修复**: ABI splits 启用、Lint 严格模式、测试覆盖率 80%+
3. **项目可正常构建和运行**: Debug 和 Release 构建成功
4. **功能测试通过**: 核心功能正常
5. **文档完善**: 文档索引完整

---

**规划时间**: 2026-06-19  
**规划版本**: versionCode=451, versionName=1.4.0
