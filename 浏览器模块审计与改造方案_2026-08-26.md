# 浏览器模块（browser）全方位审计与改造方案

> 审计日期：2026-08-26 ｜ 模块版本：v1.4.1 (vc 587) ｜ 代码量：~9,980 行（71 文件）
> 审计方式：4 路并行切片（架构/质量、安全、数据层/构建一致性、UI/UX）。UI/UX 因子代理限流改由主理人直接完成。

---

## 0. 模块概况（关键事实）

| 项 | 现状 |
|---|---|
| 包路径 | `app/src/main/java/com/gamecenter/app/browser/` |
| 入口 | `BrowserModuleEntryPoint.kt`（实现 `ModuleInterface, FeatureModule`），bottom-nav 槽位 |
| 规模 | core 3663 / ui 4369 / security 605 / data 1046 / bridge 46 / util 96 行 |
| **打包现实** | **源码直接编译进宿主 `:app`，不是独立 APK 模块** |
| catalog 声明 | `fileName:"feature_browser_v100.apk"`、`sha256:"cdb7…"`、`fileSize:840871`、`downloadUrl:""` |

⚠️ **最重要的元发现**：catalog.json 声称浏览器是独立 APK 模块（`feature_browser_v100.apk` + sha256 白名单），但源码已并入宿主、全仓无任何 `feature_browser` 模块工程（无 `module-store/feature/browser`、无 dynamic-feature）。`settings.gradle` 只有 games/tools 子模块。运行时 `ModuleLoader.loadBuiltInModule`（`ModuleLoader.kt:212-241`）对 builtin 且磁盘无 APK 的浏览器，直接用 `context.classLoader.loadClass(entryClass)` 加载宿主类——无 DexClassLoader、无签名/sha 校验。这是一次**未完成的半迁移（half-migrated）**：catalog 元数据与构建事实不一致。

---

## 1. 四路审计发现汇总

### 1.1 架构 / 代码质量（core + 生命周期）
| # | 严重度 | 文件:行 | 问题 |
|---|---|---|---|
| A1 | **P0** | `BrowserFragment`(无 onSaveInstanceState)、`BrowserWebViewPool:140-156` | 旋转/配置变更时 `releaseAll()` 清空 `releasedStates`，整个浏览会话（滚动/前进后退/JS/登录态）丢失。模块自称"保留页面状态"在旋转场景下不成立。 |
| A2 | **P1** | `BrowserController:253-258`、`BrowserFragment:1650-1653`、`BrowserWebViewPool:249` | 多 Tab 模式 `getChromeClient()` 返回 null，`setupChromeClientCallbacks()` 直接 return；Pool 内 `BrowserChromeClient` 从未设 FileChooser/Fullscreen/Permission 回调 → 文件上传、全屏视频、相机/麦克风权限在多 Tab 下静默失效。 |
| A3 | **P1** | `BrowserCacheManager:53-60` | `clearAppBrowserCache` 直接 `deleteDir(getCacheDir())`，误删整个应用缓存（Glide/OkHttp/其他模块），且可能主线程递归删除 → ANR。 |
| A4 | **P1** | `BrowserWebViewPool:48-52,70-289` | `activePool/releasedStates/lastAccessMap` 非线程安全 HashMap，无同步；`onTrimMemory` 调用会 CME。`saveTabState()` 全库从未被调用。 |
| A5 | **P1** | `BrowserController:36,278-303` | `jsBridgeInjected` 是跨 Tab 共享单 boolean，多 Tab 切换时不会按目标 Tab 重建/移除 bridge，状态与各 WebView 实际不一致。 |
| A6 | **P1** | `BrowserFragment:324-365` | `findInPageHelper`/`readerModeHelper` 仅绑定初始 WebView，切 Tab 后操作已失效 WebView。 |
| A7 | **P1** | `BrowserFragment`(多处 ioExecutor→Toast) | `mainHandler.post(() -> Toast.makeText(getContext(),…))` 未判 `getContext()==null`，Fragment detach 后 NPE。 |
| A8 | **P1** | `BrowserTabManager:28-178` | 单例持有非线程安全 `ArrayList tabs`；favicon 为内存 Bitmap 永不清算，Tab 越多内存无上限。 |
| A9 | P2 | `BrowserFragment`(1817 行) | God Class，职责过多。 |
| A10 | P2 | `BrowserTabManager:62-185` | `TabChangeListener` 永不赋值，`notifyListeners` 恒 no-op（死机制）。 |
| A11 | P2 | `BrowserZoomHelper:101-111`、`BrowserMultiFingerGestureHelper:62-101` | 未使用 `ZoomListener`；三指截图/双指下拉为近似实现，左边缘长按显式 no-op。 |
| A12 | P2 | `BrowserFragment:150,614`、`BrowserCacheManager:24-27` | `defaultUserAgent` 死字段；为读 UA / 清缓存每次 `new WebView` 仅用完即 destroy，浪费内核初始化。 |
| A13 | P2 | `BrowserController:88-105` vs `BrowserWebViewPool:235-252` | 两处 WebSettings 配置完全重复，易漏改。 |
| A14 | P2 | `BrowserWebViewClient:106-142` | `shouldInterceptRequest` 同步读 SharedPreferences + 后台 IO 统计，拖慢资源加载。 |

### 1.2 安全
| # | 严重度 | 文件:行 | 问题 |
|---|---|---|---|
| S1 | **High** | `BrowserDownloadManager:79` / `BrowserFragment:630` | 下载文件名未净化即 `setDestinationInExternalPublicDir(DIRECTORY_DOWNLOADS, fileName)`，URL/Content-Disposition 可控，`../` 路径穿越写/覆盖任意位置。 |
| S2 | Medium | `BrowserFragment:1287,1298` | 外部 scheme 用 `new Intent(ACTION_VIEW, uri)` + `startActivity`，未 `resolveActivity` 校验目标组件，网页可触发宿主特权深链。 |
| S3 | Medium | `BrowserFragment:296-308` vs `UrlUtils:30-57` | `initialUrl` 未经地址栏那套危险协议清洗直接 `loadUrl`，内部调用方可借 `javascript:` 在浏览器上下文执行任意 JS。 |
| S4 | Medium | `BrowserSecurityPolicy:71-73` | `BROWSER_SECURITY_POLICY=false` 时对**所有** URL（含 javascript:/file:/content:）直接 ALLOW，防御纵深缺失。 |
| S5 | Medium | `BrowserController:88-95`、`BrowserWebViewPool:235-242` | 两处均未显式 `setMixedContentMode(NEVER_ALLOW)`，依赖平台默认。 |
| S6 | Medium | `BrowserController:265-272`、`BrowserFragment:1313-1315` | 退出/无痕清理不完整；无痕切换 `removeAllCookies` 误清**全部** Cookie。 |
| S7 | Low | `UrlUtils:94` | `isSecure` 仅前缀判断，作为 JS Bridge 信任一环不够稳健。 |
| S8 | Low | `DomainTrustManager:12` | 类名误导，非 X509TrustManager，全模块无调用（死代码）。 |
| — | 已确认安全 | — | JS Bridge 受 flag+可信域双重门控且当前关闭 ✅；`onReceivedSslError` 拒绝无效证书 ✅；`file://`/`content://` 已关闭 ✅；`intent://` 因未用 `parseUri` 天然不可利用 ✅；广告/追踪拦截器真实生效 ✅。 |

### 1.3 数据层 / 构建一致性 / 打包
| # | 严重度 | 文件:行 | 问题 |
|---|---|---|---|
| D1 | **P1** | `catalog.json:48-50` | builtin 浏览器却带独立 APK 元数据（fileName/sha256/fileSize），与 `games_hall`（空 fileName/sha256）矛盾；源码已并入宿主，无此产物。 |
| D2 | **P1** | `ModuleLoader.kt:26-32,43-69,212-241`、`ModuleVerifier.kt:38-53` | 若某次预装/OTA 真把 `feature_browser_v100.apk`（version>587）放入 `modules/current/`，会改走 DexClassLoader 并执行 `verifySha256("cdb7…")` → 必然 mismatch → 文件被删、模块**静默加载失败**（sha 非空，`allowEmpty` 救不了）。"半迁移"让浏览器处于"放错文件即崩"的脆弱平衡。 |
| D3 | **P1** | `BrowserDownloadManager:76-115` | 同 S1：路径穿越/覆盖写（数据层视角）。 |
| D4 | **P1** | `BrowserDownloadManager:117-123` | `isDangerousFile()`（.apk/.exe/.bat/.sh/.js/.vbs/.msi…）计算结果**只赋值从未用于拦截**，危险文件直落公共 Downloads。 |
| D5 | P2 | `BrowserDatabase:31,77-83`、`BrowserHistoryEntity:34` | 无 `fallbackToDestructiveMigration()`；`isDeleted` 列 v1→v3 迁移均未添加，老用户升级查询可能失败。 |
| D6 | P2 | `BrowserDatabase:77` | 未 `allowMainThreadQueries()`，DAO 全同步，无编译期保证，新增主线程调用即崩。 |
| D7 | P2 | 多文件 | 下载表由 DownloadManager 直操 DAO，书签/历史/阅读列表在 UI/Helper 直调 DAO，绕过 4 个 Repository；4+ 独立单线程 Executor，访问路径不统一。 |
| D8 | P2 | `BrowserModuleEntryPoint.kt:50` | `getIconResId()` 返回 0 → 底部导航"浏览器"项图标缺失。 |
| 开关 | — | `app/build.gradle:368-406` | 实际定义 **19 个** BROWSER_* 开关，全部被代码引用并生效、无死开关；但除 JS_BRIDGE/WEBVIEW_DEBUG 外当前**全为 true**（实际"全开"）。 |

**打包结论**：浏览器是货真价实的**宿主内代码**。catalog 的 `feature_browser_v100.apk` 元数据是过期残留。推荐处置（二选一，强烈建议前者）：
- **A（轻量）**：把 catalog 浏览器条目改为 `fileName=""`、`sha256=""`、`fileSize=0`，与 `games_hall` 对齐，并同步 `modules.json` 同名条目。
- **B（重）**：在 `module-store/feature/` 新建真正的 browser 模块工程产出 APK 并登记真实 sha——与"源码已在宿主"冲突，不推荐。

### 1.4 UI/UX 与资源（主理人直接审计，子代理限流）
| # | 严重度 | 文件:行 | 问题 |
|---|---|---|---|
| U1 | **P1** | `BrowserFragment:392` | 起始页"阅读列表"按钮是**桩**：只弹 `browser_reading_list_empty` Toast，未启动 `ReadingListActivity`（尽管 `BROWSER_READING_LIST=true` 且该 Activity 在菜单可达，`:1206`）。功能半接线。 |
| U2 | P2 | 8 个 Adapter（Bookmark/Download/History/ReadingList/Tab/OfflineCache/PrivacyDashboard/BottomBarCustomize） | 全部 `notifyDataSetChanged()`，无 DiffUtil/ListAdapter → 无项动画、全量重绑、大列表闪烁。 |
| U3 | P2 | `activity_browser.xml`(0)、`item_bookmark.xml`(0) | 部分布局 icon-only 按钮缺 `contentDescription`，无障碍覆盖不一致（fragment_browser 14、item_tab 2）。 |
| U4 | P2 | `BrowserModuleEntryPoint.kt:50` | 底部导航图标缺失（同 D8）。 |
| U5 | P2 | `BrowserHomeHelper:242`、`BrowserMultiFingerGestureHelper:99` | 长按快捷菜单、左边缘长按手势声明为"未来功能/不实现"，但文档/特性开关暗示可用。 |
| U6 | P2 | `BookmarkActivity:96` 等 TextWatcher | `addTextChangedListener` 未见对应 `removeTextChangedListener`，销毁期清理不严。 |
| ✅ 正向 | — | 全部 browser 布局 | **无硬编码十六进制颜色**，均用 `@color` 引用 → 暗色主题友好；代码层无用户可见硬编码中文字符串（仅注释），i18n 基础良好。 |

---

## 2. 优先级总览

- **P0（侵蚀核心能力 / 数据丢失）**：A1（旋转丢会话）、A2/A6（多 Tab 文件上传/全屏/权限/查找/阅读模式失效）。
- **P1（真实缺陷 / 高危）**：S1 下载路径穿越（High）、S2/S3 外部/初始 URL 校验缺失、S5 混合内容、S6 隐私清理、A3 缓存误删、A4 Pool 线程安全、A5 bridge 状态、A7 Toast NPE、A8 内存、D1/D2 打包元数据、D3/D4 下载安全、U1 阅读列表桩。
- **P2（技术债 / 加固 / 体验）**：God Class 拆分、死代码/死机制清理、WebSettings 去重、Executor 生命周期、Room 迁移安全、DiffUtil、无障碍、导航图标、桩手势。

---

## 3. 改造路线图（分阶段）

**Phase 0 — 元数据与打包对齐（低风险，先解锁）**
- D1/D2：catalog.json + modules.json 浏览器条目改为 `fileName=""`、`sha256=""`、`fileSize=0`（对齐 games_hall）；或在 `ModuleLoader` builtin 的 DexClassLoader 分支强制 `allowEmpty` 跳过校验，杜绝"放错文件即崩"。

**Phase 1 — 稳定性与数据完整性（P0/P1）**
- A1：实现 `onSaveInstanceState`/`onViewStateRestored`，按 tabId 对活跃 WebView `saveState` 到 Bundle；或 `ViewModel` 持池；`releaseAll()` 改为保留待恢复。
- A2/A6：Pool 内每个新建 WebView 的 `BrowserChromeClient` 共享 Fragment 的 FileChooser/Fullscreen/Permission 回调；`switchToTabById` 重新 `bind` Find-in-Page/Reader-Mode。
- A4：Pool 三张映射改 `ConcurrentHashMap`/统一加锁；真正接 `onTrimMemory` + 切 Tab 前 `saveTabState`。
- A3：缓存清理仅限浏览器专属子目录 + 后台线程。
- A5/A7/A8：bridge 状态按 tabId 维护；所有 `mainHandler.post` UI 回调先判 `isAdded()/getContext()!=null`；TabManager 改 `CopyOnWriteArrayList`，favicon 限 LruCache。

**Phase 2 — 安全加固（P1）**
- S1/D3/D4：下载文件名 basename 化 + 字符黑名单；`isDangerousFile()` 真正用于确认/拦截或改存私有目录。
- S2/S3：外部 scheme 启动前 `resolveActivity` 校验目标包/组件；所有 `loadUrl` 入口统一过 `UrlUtils.processInput`，拒绝 javascript:/file:/content:/intent:。
- S4/S5：即使 `BROWSER_SECURITY_POLICY=false` 也始终拦截 `BLOCKED_SCHEMES`；两处 `configureWebView` 显式 `MIXED_CONTENT_NEVER_ALLOW`。
- S6：提供退出/无痕自动清理 `clearAllBrowsingData`；无痕切换改为上下文隔离而非清全局 Cookie。

**Phase 3 — 架构与质量（P2）**
- 拆分 `BrowserFragment` God Class（DownloadDelegate / SuggestionsController / PermissionsDelegate / LifecycleBinder）。
- 删除死代码/死机制（TabChangeListener、ZoomListener、defaultUserAgent、DomainTrustManager 误导性类）。
- 抽出统一 `applyCommonWebSettings(WebView)`；收敛 Executor 生命周期；`BrowserDatabase` 加 `fallbackToDestructiveMigration` + 补 `isDeleted` 迁移。

**Phase 4 — UI/UX 完善（P2）**
- U1：起始页"阅读列表"按钮接 `ReadingListActivity.start()`。
- U2：8 个 Adapter 迁移 DiffUtil/ListAdapter（含 payloads）。
- U3/U4：补全 `contentDescription`；提供有效导航图标资源 id。
- U5/U6：补齐或显式关闭桩手势；TextWatcher 配对移除。

---

## 4. 执行建议（待确认）

模块约 10k 行、P0/P1 项多为"动一处牵全局"的底层逻辑。**建议先 Phase 0（低风险、立即解除静默失效风险），再按 Phase 1→2→3→4 推进**，每阶段遵循你的 SOP：逻辑自测 → `:app` 构建 → `adb install -r` → 模拟器冒烟（旋转、多 Tab、下载、外部链接、无痕）。

下一步需你确认执行范围（见附带的提问）。

---

## 5. 复核更新（2026-08-26 主理人二次核对）

> 以下结论经对实际代码与工作区状态逐一核实后更新，**覆盖/修正**上文 0、1.3 节中与事实不符的描述。

### 5.1 修正 D1/D2：`feature_browser_v100.apk` 真实存在

- 实测 [/app/src/main/assets/modules/feature_browser_v100.apk](app/src/main/assets/modules/feature_browser_v100.apk) 存在，840871 字节，SHA-256 = `CDB77557F5462E1ADD1E4A060FCEF88B68EB5700F2AE291A00965B0053FC4059`，与旧 catalog 元数据**完全一致**，且被 git 追踪。
- 修正结论：浏览器不是"无产物半迁移"，而是**宿主内代码 + 冗余预装 APK 并存**。旧 catalog 元数据并非过期残留，而是对应真实文件。
- 运行时路径核实：[/app/src/main/java/com/gamecenter/app/modules/ModuleLoader.kt](app/src/main/java/com/gamecenter/app/modules/ModuleLoader.kt) `loadModule()` 对 builtin 模块优先走宿主 ClassLoader（`L26-32`）；仅当 `installedVersion > builtInVersionCode` 才走 DexClassLoader。SHA 校验 `allowEmpty = builtIn`（`L58`）。
- D2 的"放错文件即崩"在 catalog 清空 sha 后**已缓解**：空 sha + allowEmpty → 校验放行，不会误删文件。

### 5.2 工作区现状（4 项未提交改动，均为既有用户资产）

| 文件 | 改动 | 状态 |
|---|---|---|
| `catalog.json` / `modules.json` | browser 条目 fileName/sha256/fileSize 清空（Phase 0 已落地） | 未提交，受保护资产 |
| `BrowserFragment.java` | 移除获焦自动弹 URL 建议（`b204e2c` 提交延续） | 未提交，普通代码 |
| `version.properties` | versionCode 678 → 680 | 未提交，受保护资产 |

### 5.3 修正后的 Phase 0 结论与残留不一致

- **残留不一致**：catalog 已清空 APK 元数据，但 `assets/modules/feature_browser_v100.apk` 仍随宿主打包 → 约 840KB 死重 + 元数据再次脱节（方向反转："声明无、实际有"）。
- **处置（二选一，推荐 A）**：
  - **A（推荐）**：删除冗余 `feature_browser_v100.apk`，彻底对齐"宿主内代码"事实；执行前已记录 SHA-256，删除后按 AGENTS.md 要求核对受保护资产。
  - **B**：保留 APK，接受死重与元数据脱节。
- **受保护资产合规**：改动 `catalog.json`/`modules.json`/`version.properties` 前应 `verify_protected_assets.py snapshot` 留档基线。

### 5.4 执行范围确认

- 按"Phase 0（清理死重 + 资产基线）→ Phase 1（P0：A1 旋转会话 + A2/A6 多 Tab 回调桥接）→ 逐阶段验证"推进。
- 每阶段遵循：逻辑自测 → `:app` 编译 → 受保护资产核对；完整实机冒烟按 AGENTS.md 约定进行。

### 5.5 执行记录（2026-08-26）

**Phase 0 ✅ 完成**
- 删除冗余 `assets/modules/feature_browser_v100.apk`（840871 字节，SHA-256 `CDB77557…`，用户确认 Option A）。
- 受保护资产快照已重建：`verify_protected_assets.py snapshot` + `verify` PASS（6 文件）。

**Phase 1-P0 ✅ 完成（A1/A2/A6）**
- A1 旋转会话保留：`BrowserWebViewPool.saveAllStates/collectStateMap/restoreStateMap` + `BrowserController` 转发 + `BrowserFragment.onSaveInstanceState` 保存、`onViewCreated` 恢复。
- A2 多 Tab ChromeClient 桥接：`BrowserWebViewPool.chromeClientConfigurator` 应用于每个新建 WebView；`BrowserController.setChromeClientConfigurator`；Fragment 拆出 `configureChromeClient()` 单/多 Tab 共用。
- A6 切 Tab 重绑：`BrowserFindInPageHelper.bind()` 支持重绑新 WebView（FindListener 跟随当前 WebView、UI 监听仅挂一次）；`BrowserFragment.switchToTabById` 末尾重绑 Find/Reader + 先退出旧 Tab 阅读模式。
- 验证：`:app:compileDebugJavaWithJavac` 通过；`git diff --check` 通过；受保护资产编译后无变化。

**待办（后续 Phase）**
- Phase 1 余项：A3 缓存清理限域、A4 Pool 线程安全、A5 bridge 按 tabId、A7 Toast NPE、A8 favicon 内存。
- Phase 2 安全加固（S1 下载路径穿越、S2/S3 URL 校验、S5 混合内容、S6 无痕隔离）。
- Phase 3/4 架构与 UI。

**⚠️ 工作区并发现象**：执行期间 `git status` 出现一批非本次会话产生的改动（`ModuleLifecycleManager.java` 与 `core/moduleloader`、`core/modulestore` 相关删除，`ModuleManager.kt`/`App.kt`/`ModuleAdapter.java` 修改，新增 `BuiltInModuleUpdater.kt`/`verify_isolation.py` 等），疑似并行"模块加载器隔离重构"，未触碰。
