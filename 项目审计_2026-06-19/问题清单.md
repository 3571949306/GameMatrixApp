<!-- flutter-store-doc-sync: 2026-07-22; historical -->
> Historical snapshot: preserved for context and not current release truth. Flutter-first module store production completion is 100%; current facts: /docs/CURRENT_STATE.md.

# 问题清单

> 生成时间：2026-06-19
> 任务背景：用户要求"app 分发只保留 HK vps 和 GitHub，部分设置需要更新"

---

## 问题等级定义

| 等级 | 含义 | 处理优先级 |
|------|------|-----------|
| P0 | 阻塞构建/启动 | 立即修复 |
| P1 | 影响核心功能 | 高优先级 |
| P2 | 影响次要功能 | 中优先级 |
| P3 | 代码质量/文档 | 低优先级 |
| P4 | 优化建议 | 可选 |

---

## 一、分发渠道相关问题（本次任务核心）

### 问题 1.1：UpdateChecker 三级源逻辑冗余
- **等级**：P1
- **涉及文件**：`core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java`
- **位置**：L52-57（常量定义）、L98-101（SSL 信任）、L318-378（`buildUpdateUrls()`）
- **当前状态**：实现了 HK VPS → US VPS → GitHub 三级源
- **可能原因**：历史遗留，原部署了美国 VPS 作为备用更新源
- **建议修复**：移除 US_BASE_URL 相关逻辑，改为 HK VPS → GitHub 两级源
- **风险**：需保持 `US_BASE_URL` 字段存在（为空字符串），避免编译错误；旧版本 App 仍可能请求 US VPS，服务器端处理

### 问题 1.2：UpdateDownloader 三级源 URL 构建
- **等级**：P1
- **涉及文件**：`core/update/src/main/java/com/gamecenter/app/update/UpdateDownloader.java`
- **位置**：L310-326（`buildDownloadUrls()`）、L319（US URL 构建）、L300-303（注释）
- **当前状态**：构建下载 URL 列表时包含 US VPS 源
- **可能原因**：与 UpdateChecker 配套实现三级源
- **建议修复**：移除 US URL 构建逻辑，保留 HK + GitHub 两级
- **风险**：低，US_BASE_URL 为空时已跳过

### 问题 1.3：SettingsManager 含 US VPS 选项常量
- **等级**：P2
- **涉及文件**：`core/common/src/main/java/com/gamecenter/app/SettingsManager.java`
- **位置**：L75 `UPDATE_SOURCE_VPS_US = 2`
- **当前状态**：定义了美国 VPS 更新源选项
- **可能原因**：历史遗留
- **建议修复**：保留常量定义（避免编译错误），但在 UI 中不展示；或将常量标记为 `@Deprecated`
- **风险**：低，需保持常量值不变（已存储的 SharedPreferences 值不能错位）

### 问题 1.4：AppSettingsDialog 显示"美国 VPS"选项
- **等级**：P2
- **涉及文件**：`app/src/main/java/com/gamecenter/app/settings/AppSettingsDialog.java`
- **位置**：L49-56（`getUpdateSourceNames()`）、L420-438（`showUpdateSourcePicker()`）
- **当前状态**：更新源选择器显示 4 个选项（自动/香港/美国/GitHub）
- **可能原因**：与 SettingsManager 配套
- **建议修复**：移除"美国 VPS"选项，改为 3 个选项（自动/香港/GitHub）
- **风险**：中，需处理用户已选择"美国 VPS"的情况（应回退到"自动"）

### 问题 1.5：RecoveryDownloader 含 US fallback 源
- **等级**：P2
- **涉及文件**：`app/src/main/java/com/gamecenter/app/recovery/RecoveryDownloader.kt`
- **位置**：L21-31（`DOWNLOAD_SOURCES`）
- **当前状态**：恢复模式下载源列表含 US fallback
- **可能原因**：与 UpdateDownloader 配套
- **建议修复**：移除 US fallback 插入逻辑
- **风险**：低，fallback 为空时已跳过

### 问题 1.6：App.java 注入 fallback host
- **等级**：P2
- **涉及文件**：`app/src/main/java/com/gamecenter/app/App.java`
- **位置**：L103 `SecureOkHttpFactory.setHosts(BuildConfig.MODULE_HOST, BuildConfig.MODULE_FALLBACK_HOST)`
- **当前状态**：将主源和备用源 host 注入安全模块
- **可能原因**：证书固定需要信任所有更新源 host
- **建议修复**：保持调用，但 `MODULE_FALLBACK_HOST` 将为空字符串（回退到主源 host）
- **风险**：低，需验证 `SecureOkHttpFactory.setHosts()` 对空字符串的处理

### 问题 1.7：strings.xml 含"美国 VPS"文案
- **等级**：P3
- **涉及文件**：
  - `app/src/main/res/values/strings.xml` L249
  - `app/src/main/res/values-en/strings.xml` L240
- **当前状态**：定义了 `settings_source_us_vps` 字符串
- **可能原因**：与 UI 配套
- **建议修复**：保留字符串定义（避免引用错误），但 UI 不再使用；或直接删除并确认无引用
- **风险**：低

### 问题 1.8：local.properties.template 含 fallback 配置
- **等级**：P3
- **涉及文件**：`local.properties.template` L21-22
- **当前状态**：模板含 `server.url.fallback` 配置项
- **可能原因**：历史遗留
- **建议修复**：移除 `server.url.fallback` 配置项，或注释说明"已废弃，仅保留向后兼容"
- **风险**：低

### 问题 1.9：build.gradle 含 SERVER_URL_FALLBACK BuildConfig
- **等级**：P3
- **涉及文件**：
  - `app/build.gradle` L198、L209
  - `core/update/build.gradle` L80
  - `app/version.gradle` L15、L31、L67
- **当前状态**：生成 `SERVER_URL_FALLBACK` 和 `MODULE_FALLBACK_HOST` BuildConfig 字段
- **可能原因**：历史遗留
- **建议修复**：保留 BuildConfig 字段（避免编译错误），但默认值为空字符串；添加注释说明"已废弃"
- **风险**：低

---

## 二、文档相关问题

### 问题 2.1：README.md 三级下载源架构图过时
- **等级**：P3
- **涉及文件**：`README.md` L166-246
- **当前状态**：描述"三级下载源：香港 VPS → GitHub Releases → 美国 VPS"
- **建议修复**：更新为"两级下载源：香港 VPS → GitHub Releases"

### 问题 2.2：PROJECT_CONTEXT.md 含 US VPS 说明
- **等级**：P3
- **涉及文件**：`docs/PROJECT_CONTEXT.md` L349
- **当前状态**：`SERVER_URL_FALLBACK` → "备用更新源地址（美国）"
- **建议修复**：更新说明为"已废弃，保留空值"

### 问题 2.3：PUBLISH_GUIDE.md 含 fallback 配置示例
- **等级**：P3
- **涉及文件**：`docs/PUBLISH_GUIDE.md` L80
- **当前状态**：`server.url.fallback=https://backup-server.example.com`
- **建议修复**：移除或注释标记为已废弃

### 问题 2.4：track-vpn.md 三级源描述与代码不一致
- **等级**：P4
- **涉及文件**：`docs/refactor/track-vpn.md` L201
- **当前状态**：描述顺序为"GitHub Releases → HK VPS → US 备用"，与代码相反
- **建议修复**：更新为两级源描述

---

## 三、其他已知问题（本次任务不处理，仅记录）

### 问题 3.1：双 ModuleDownloader 实现
- **等级**：P3
- **涉及文件**：
  - `app/src/main/java/.../modules/ModuleDownloader.kt`（多源）
  - `app/src/main/kotlin/.../modular/ModuleDownloader.kt`（单源）
- **当前状态**：两个实现行为不一致
- **建议**：本次不处理，记录到《最终报告.md》

### 问题 3.2：双 ModuleManifest 实现
- **等级**：P3
- **涉及文件**：
  - `app/src/main/java/.../modules/ModuleManifest.kt`（含 githubUrl）
  - `core/module-host/.../ModuleManifest.kt`（不含 githubUrl）
- **当前状态**：字段不一致
- **建议**：本次不处理，记录到《最终报告.md》

### 问题 3.3：R8/ProGuard 临时关闭
- **等级**：P3
- **涉及文件**：`app/build.gradle` L236-237
- **当前状态**：`minifyEnabled false`、`shrinkResources false`
- **建议**：本次不处理，记录到《最终报告.md》

### 问题 3.4：modules.json 模块单源问题
- **等级**：P3
- **涉及文件**：`app/src/main/assets/modules.json`
- **当前状态**：所有模块 `fallbackUrl`、`githubUrl` 为空
- **建议**：本次不处理（需确认 GitHub Releases 是否上传模块 APK）

### 问题 3.5：GitHub URL 硬编码
- **等级**：P4
- **涉及文件**：`UpdateChecker.java` L57、`UpdateDownloader.java` L361/363、`app/build.gradle` L210
- **当前状态**：`https://github.com/3571949306/GameMatrixApp/releases` 硬编码多处
- **建议**：本次不处理，可未来抽取为 BuildConfig

---

## 四、问题统计

| 等级 | 数量 | 本次处理 |
|------|------|---------|
| P0 | 0 | - |
| P1 | 2 | ✅ 全部处理 |
| P2 | 4 | ✅ 全部处理 |
| P3 | 7 | ✅ 处理分发相关（1.7-1.9、2.1-2.3），其余记录 |
| P4 | 2 | ❌ 不处理，仅记录 |
| **合计** | **15** | **本次处理 11 项** |


---
[🔙 返回文档索引](/docs/DOCUMENTATION_INDEX.md)