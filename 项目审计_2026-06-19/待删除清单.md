<!-- flutter-store-doc-sync: 2026-07-22; historical -->
> Historical snapshot: preserved for context and not current release truth. Flutter-first module store production completion is 100%; current facts: /docs/CURRENT_STATE.md.

# 待删除清单

> 生成时间：2026-06-19
> 任务目标：记录所有建议删除的内容，等待用户确认后处理

---

## 待删除项 1：settings_source_us_vps 字符串（中文）

- **路径**：`app/src/main/res/values/strings.xml` 第 250 行
- **类型**：代码片段（字符串资源）
- **建议删除原因**：美国 VPS 已下线，此字符串无代码引用（已标注废弃）
- **删除后影响**：无（无代码引用）
- **是否影响核心功能**：否
- **是否影响项目启动**：否
- **替代方案**：保留现状（已标注废弃）
- **删除风险等级**：低
- **删除优先级**：低（建议保留 1-2 版本后删除）
- **是否需要用户确认**：是
- **当前状态**：未删除（已标注废弃）

---

## 待删除项 2：settings_source_us_vps 字符串（英文）

- **路径**：`app/src/main/res/values-en/strings.xml` 第 241 行
- **类型**：代码片段（字符串资源）
- **建议删除原因**：美国 VPS 已下线，此字符串无代码引用（已标注废弃）
- **删除后影响**：无（无代码引用）
- **是否影响核心功能**：否
- **是否影响项目启动**：否
- **替代方案**：保留现状（已标注废弃）
- **删除风险等级**：低
- **删除优先级**：低（建议保留 1-2 版本后删除）
- **是否需要用户确认**：是
- **当前状态**：未删除（已标注废弃）

---

## 待删除项 3：ModuleDownloaderTest.java.disabled

- **路径**：`app/src/test/java/com/gamecenter/app/modules/ModuleDownloaderTest.java.disabled`
- **类型**：文件（已禁用的测试文件）
- **建议删除原因**：预存在编译错误较多（Kotlin data class val 字段不可变），已暂时禁用
- **删除后影响**：无（测试文件，不影响生产代码）
- **是否影响核心功能**：否
- **是否影响项目启动**：否
- **替代方案**：重写为正确的 Kotlin data class API 后恢复（推荐）
- **删除风险等级**：中（丢失测试逻辑）
- **删除优先级**：中（建议重写后恢复，而非删除）
- **是否需要用户确认**：是
- **当前状态**：未删除（已重命名为 .java.disabled）

---

## 待删除项 4：UPDATE_SOURCE_VPS_US 常量

- **路径**：`core/common/src/main/java/com/gamecenter/app/SettingsManager.java` 第 81 行
- **类型**：代码片段（常量定义）
- **建议删除原因**：美国 VPS 已下线，此常量已标记 @Deprecated
- **删除后影响**：历史选择 US VPS 的用户读取会异常；依赖此常量的代码编译失败
- **是否影响核心功能**：是（向后兼容）
- **是否影响项目启动**：否
- **替代方案**：保留 @Deprecated 注解，自动重定向到 AUTO（当前方案）
- **删除风险等级**：高
- **删除优先级**：低（建议保留 3-5 版本后删除）
- **是否需要用户确认**：是
- **当前状态**：未删除（已标记 @Deprecated）

---

## 待删除项 5：US_BASE_URL 常量

- **路径**：`core/update/src/main/java/com/gamecenter/app/update/UpdateChecker.java` 第 62 行
- **类型**：代码片段（常量定义）
- **建议删除原因**：美国 VPS 已下线，此常量已标记 @Deprecated
- **删除后影响**：UpdateChecker 中仍引用此常量，删除需同步修改代码
- **是否影响核心功能**：否（已移除使用逻辑）
- **是否影响项目启动**：否
- **替代方案**：保留 @Deprecated 注解（当前方案）
- **删除风险等级**：高
- **删除优先级**：低（待 SERVER_URL_FALLBACK 删除后一并清理）
- **是否需要用户确认**：是
- **当前状态**：未删除（已标记 @Deprecated）

---

## 待删除项 6：SERVER_URL_FALLBACK BuildConfig 字段

- **路径**：`app/build.gradle` 第 199 行、`core/update/build.gradle`
- **类型**：代码片段（BuildConfig 字段定义）
- **建议删除原因**：美国 VPS 已下线，此字段保留空值向后兼容
- **删除后影响**：UpdateChecker.US_BASE_URL 引用此字段，删除需同步修改
- **是否影响核心功能**：否（值为空字符串）
- **是否影响项目启动**：否
- **替代方案**：保留空值，标注废弃（当前方案）
- **删除风险等级**：高
- **删除优先级**：低（建议保留 3-5 版本后删除）
- **是否需要用户确认**：是
- **当前状态**：未删除（保留空值，标注废弃）

---

## 待删除项 7：MODULE_FALLBACK_HOST BuildConfig 字段

- **路径**：`app/build.gradle` 第 211 行
- **类型**：代码片段（BuildConfig 字段定义）
- **建议删除原因**：美国 VPS 已下线，此字段当 fallback 为空时回退到主源 host
- **删除后影响**：App.java:103 中 SecureOkHttpFactory.setHosts() 引用此字段
- **是否影响核心功能**：是（证书固定 host 配置）
- **是否影响项目启动**：否
- **替代方案**：保留现状（回退到主源 host，仍有用途）
- **删除风险等级**：高
- **删除优先级**：低（不建议删除，仍有用途）
- **是否需要用户确认**：是
- **当前状态**：未删除（保留，回退到主源 host）

---

## 待删除项 8：upload_config_us.json（如存在）

- **路径**：`local_private/vps/upload_config_us.json`（被 .gitignore 排除）
- **类型**：配置文件
- **建议删除原因**：美国 VPS 已下线，此配置文件不再使用
- **删除后影响**：无
- **是否影响核心功能**：否
- **是否影响项目启动**：否
- **替代方案**：保留作为历史记录
- **删除风险等级**：低
- **删除优先级**：低
- **是否需要用户确认**：是
- **当前状态**：未删除（未确认是否存在）

---

## 【等待用户确认删除】

请列出所有建议删除项，并按照风险排序：

### 1. 低风险：可删除
- `settings_source_us_vps` 字符串（中/英）— 建议保留 1-2 版本后删除
- `upload_config_us.json`（如存在）— 可立即删除

### 2. 中风险：建议确认后删除
- `ModuleDownloaderTest.java.disabled` — 建议重写后恢复，而非删除

### 3. 高风险：不建议自动删除
- `UPDATE_SOURCE_VPS_US` 常量 — 保留 @Deprecated，至少 3-5 版本
- `US_BASE_URL` 常量 — 保留 @Deprecated
- `SERVER_URL_FALLBACK` BuildConfig 字段 — 保留空值
- `MODULE_FALLBACK_HOST` BuildConfig 字段 — 保留（仍有用途）

**在我明确确认之前，不要真正删除任何文件。**


---
[🔙 返回文档索引](/docs/DOCUMENTATION_INDEX.md)